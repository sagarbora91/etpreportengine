# ETP Reporting Engine — Master Audit and Phased Rebuild Plan

Version 1.4 — 16 September 2026 (all owner decisions D1–D11 frozen; D12–D21 recorded OPEN; Phase 0 CLOSED at `6791c9e`; Phase 1 CLOSED at `e1ef5f3`; Phases 7 and 8 added from the three-stage roadmap assessment). Author: Claude (audit and plan). Executor: Codex. Decision owner: Sagar.

This document replaces every earlier "acceptance", "sprint ledger", "handoff" and "review plan" document under `docs/` as the single working plan. Detailed evidence lives in `docs/audit/claude-audit-2026-09/01..06-*.md`.

Version history: 1.0 (15 Sep) six audits and Phases 0–6 · 1.1–1.2 (15 Sep) D1–D11 decided · 1.3 (15 Sep) Phase 0 closed · **1.4 (16 Sep) roadmap assessment encoded: Phase 2 tasks 12–15, Phase 4 task 6 rewritten (no certificate purchase), Phase 5 Accounting row extended, Phases 7 and 8 added, D12–D21 opened, Section 8 crosswalk added.** Changes to this document happen only through Claude's phase audits and owner-accepted assessments recorded in Section 7.

---

## 1. Verdict in one paragraph

The application is real, not vapour: it builds, runs against SQL Express, imports four ETP workbook types, and renders 22 distinct report queries across 29 catalogue entries with Excel and PDF export. But it was built to satisfy its own documents rather than the shop. The three things the owner actually needs every evening — the DSR sheet, the closing-stock-by-brand sheet and the cash book — cannot be produced correctly today: sales are reported ex-GST while the shop reports GST-inclusive, targets and walk-ins are keyed in a way that makes MTD/YTD blank, brand rows and cash tenders do not exist, and only 4 of the 30 ETP exports can be imported. The UI hid content when the window was not exactly 1366x768, and every screen opened on a date with no data. On top of that sits a heavy process layer (128 markdown documents, generated graph files and 47 MB installers committed to git, 37 test files that assert on source text) that costs time and hides regressions. The plan below strips the process, fixes the data truth, builds the six reports the shop uses, and then makes the shell touch-first. Secondary modules (Accounting/Tally, Archive sharing, Registers, OCR, Approvals) are decided in Phase 5 — finished or hidden, never half-shown to staff.

## 2. How we work

**Roles.** Sagar decides business rules and priorities. Codex implements each phase on a branch. Claude audits the result by building, launching and screenshotting the app, running the suites and comparing report numbers with the source workbooks; a phase is closed only when every acceptance criterion is marked PASS by Claude.

**Rules for Codex (read before every phase).**
1. Work on branch `phase-N/<slug>` from `main` after Phase 0 lands. Small commits with imperative messages. No force-push.
2. Do not write new process documents. The only documents Codex maintains are `README.md`, `docs/USER-GUIDE.md` (from Phase 3), `docs/OPERATIONS.md` (from Phase 4) and one `PHASE-N-REPORT.md` per phase at `docs/audit/claude-audit-2026-09/`. Do not touch `knowledge/`, `graphify-out/`, `artifacts/`, `verification/`, `prototypes/` except to delete them when the plan says so.
3. Every behaviour change gets a behavioural test (arrange data → call service or run SQL against a temp database → assert values). Do not add tests that read source files or count lines or controls. Do not "fix" a source-text ratchet by changing the number it asserts; delete the ratchet.
4. Run the app before reporting. Every phase report contains screenshots at 1366x768 maximised and at 816x480 for every screen touched, the test summary lines, and the exact commands used.
5. Business rules come from Section 3 and from the owner's Excel sheets in `..\Reports to be generated\`. If code, docs and the sheets disagree, the sheets win; record the decision in the phase report, not in a new document.
6. Never change financial calculations without a golden test that reproduces the owner's sheet numbers for 25 August 2026 from the real workbooks in `..\ETP Source Data\`.
7. Ask instead of guessing on anything in Section 3 marked OPEN.
8. Read `docs/audit/IMPORT-FAILURE-REGISTER.md` at the start of every phase; close every OPEN row mapped to the phase or state in the phase report why it is deferred. Imports must become seamless: one folder, one button, correct numbers, no manual store or date entry.
9. Never edit a migration script that has been committed, even on the same branch; the runner is checksum fail-closed, so an edited script bricks any database that applied the earlier version. Fix a migration by adding the next-numbered script. (Learned in Phase 0: `0016` was corrected in place.)
10. Source data under `..\ETP Source Data\` contains customer names and phone numbers. Tests read it by absolute path and skip with a clear message when it is absent; never copy it into the repository. A sanitised sample (names replaced, phones masked, ≤ 50 rows per family) may be committed under `tests-dotnet/fixtures/etp-sample/` for CI.

**Phase report format** (`docs/audit/claude-audit-2026-09/PHASE-N-REPORT.md`): What changed (files) · How to verify (commands) · Test results (pasted summary lines) · Screenshots (paths) · Decisions taken · Known gaps · Acceptance checklist copied from this plan with Codex's self-assessment.

**Claude audit protocol per phase.** Build Debug and Release · run all five .NET suites · launch the app on Sagar's PC · screenshot every touched screen at 1366x768 maximised and at 816x480 · click through every acceptance criterion with UI Automation · recompute report figures from the source workbooks with Python and compare · check the security items listed for the phase · write `PHASE-N-AUDIT.md` with PASS/FAIL per criterion. FAIL items go back to Codex; the phase reopens.

## 3. Business rules and owner decisions

Verified facts (from the real workbooks, not from docs):
- In SDB-VariantwiseSales, `NETVALUE = NETAMOUNT − TAX`. NETVALUE is **ex-GST**. NETAMOUNT is the GST-inclusive invoice value. Every document in `docs/` and `knowledge/` that says "NETVALUE includes GST" is wrong.
- The owner's DSR VALUE, customer-wise list and MTD/YTD figures use the **GST-inclusive** amount (25 Aug Titan: 5 invoices in the export = ₹34,215 NETAMOUNT vs ₹28,996 NETVALUE; the sheet's 8 invoices / ₹69,880 includes 3 sales made after the 8:51 PM export).
- Stores: WLMHW = Titan World (channel WOT), HEMW = Helios. Financial year starts 1 April. Business date = invoice date.
- ETP export must be taken after the last bill of the day; otherwise every report for that day is short. This is a shop procedure, not a software fix.

Decisions (Sagar answers before the phase that needs them):

| ID | Question | Needed by | Default if no answer |
|---|---|---|---|
| D1 | **DECIDED 15 Sep 2026:** Reports use GST-inclusive NETAMOUNT as "Value" everywhere; NETVALUE and TAX kept as separate columns for GST reports. | Phase 1 | — |
| D2 | **DECIDED 15 Sep 2026:** LY = same calendar date last year (29 Feb → 28 Feb). | Phase 2 | — |
| D3 | **DECIDED 15 Sep 2026:** Store customer name and full phone number. Consequence: Phase 4 task 2 (backup folder ACLs) and task 5 (privacy scrub) are mandatory before staff use the app; customer columns are excluded from diagnostics and the support package. | Phase 1 | — |
| D4 | **DECIDED 15 Sep 2026:** Both. Codex lists every distinct BRAND/BRANDNAME/CLUSTER from the real exports with a proposed DSR row in `PHASE-2-REPORT.md` and Sagar ticks each; AND Phase 2 ships an Owner-editable **Brand rows** master in Settings (store, DSR row label, order, mapped ETP brand codes) so rows can change later without code. Rows: Titan = NEBULA, EDGE, XYLYS, AUTOMATIC, RAGA; Helios = SEIKO, CITIZEN, CERRUTI. | Phase 2 | — |
| D5 | **DECIDED 15 Sep 2026:** Monthly store target and per-CRO target entered once per month. DAY TGT = monthly ÷ days in month; MTD BLA = monthly − MTD actual; REQ ADS = BLA ÷ days remaining including today. Codex shows the calculation in the DSR help. | Phase 2 | — |
| D6 | **DECIDED 15 Sep 2026:** Keep and finish all four: Accounting/Tally export, report sharing by WhatsApp and email (contacts + delivery history), Digital Registers (all seven types), Approvals workflow (adjustments and restatements). OCR/Source Inbox scaffolding is still deleted. See the revised Phase 5 table. | Phase 5 | — |
| D7 | **DECIDED 15 Sep 2026:** Owner plus staff on one shared Store Manager Windows login on the shop PC; Owner uses their own login for admin. Audit shows the shared account. | Phase 3/4 | — |
| D8 | **DECIDED 15 Sep 2026:** Yes, Sagar will export 2025-26 for both stores. Phase 2 adds a one-time historical import (multi-month files, same profiles) so LY, growth and LY YTD fill in. Sagar exports the files before Phase 2 starts. | Phase 2 | — |
| D9 | **DECIDED 15 Sep 2026:** SQL backup encryption with a certificate. Phase 4 must: create the certificate, export it with a password to two off-PC locations chosen by Sagar, verify a restore on a second machine from the exported certificate, and document the custody steps in `docs/OPERATIONS.md`. The recovery drill fails loudly if the certificate backup is missing. | Phase 4 | — |
| D10 | **DECIDED 15 Sep 2026:** Staff key Display / Backstock / Defective / Y Loc per brand each evening in the app, yesterday's values pre-filled; System from ETP Closing Stock; Difference computed. | Phase 2 | — |
| D11 | **DECIDED 15 Sep 2026; owner clarification same day:** Cash-book modes include Cash, Card, UPI, CN (Credit Note), Gift Card, Bank, TC, Service Cash, Service Card, Service UPI. Gift Card → **Gift Card**; Cheque / RTGS → **Bank**, superseding their proposed TC mappings. Codex lists every PAYMENTTYPE column with its AGENCYNAME from the Payment Type Report in `PHASE-1-REPORT.md`; Sagar ticks the mode for each (PAYMENTTYPE25 = Airpay → UPI, PAYMENTTYPE20 = PhonePe → UPI). This clarification does not approve the remaining mappings. | Phase 1 | — |
| D12 | **OPEN — freeze when Phase 5 closes (owner + accountant).** One Tally company per store (Titan World, Helios) or one company for the firm with store cost centres / godowns? Options: (a) two companies; (b) one company, store as cost centre; (c) one company, store as godown only. | Phase 7 (7a) | Engine holds one `tally_profiles` row per store with company name NULL; every batch for a store without a company name is `BLOCKED` with reason "Tally company not decided (D12)". Fixtures carry both shapes. |
| D13 | **OPEN — freeze when Phase 5 closes.** Sales voucher granularity: one Sales voucher per ETP invoice (INV/SR/BC, invoice number carried as reference) or one daily summary Sales voucher per store? | Phase 7 (7a) | 7a builds per-invoice (the only shape that supports duplicate-replay proof); the daily-summary composer is a fixture-tested alternative switched by profile, disabled until D13. |
| D14 | **OPEN — freeze when Phase 5 closes.** Party ledger policy: named customer ledgers created from R024 customer name/phone, or one "Cash Sales / Retail Customers" ledger per store? | Phase 7 (7a) | Single retail-customer ledger per store; customer name and phone never leave the engine (D3 scope); named-ledger option exists only as a blocked profile value with fixture. |
| D15 | **OPEN — freeze when Phase 5 closes (accountant).** GST treatment in Tally: output-tax ledgers per rate (CGST/SGST at 1.5/6/9 %, IGST) or one tax ledger; do items carry HSN and taxable value per line? Source: `sales_lines.source_tax_amount`, R018/R019 (`etp_r018`, `etp_r019`). | Phase 7 (7a) | Per-rate ledgers derived from `source_tax_amount / source_net_amount`; a line whose derived rate is not in the profile's rate list puts the batch in `BLOCKED` with "Tax rate not configured (D15)". No HSN emitted. |
| D16 | **OPEN — freeze when Phase 5 closes (accountant).** Tender policy: tender allocated inside the Sales voucher (cash/bank/clearing ledger per mode) or a clearing ledger per mode (UPI, Card, CN, Gift Card, Bank) settled by separate Receipt vouchers when the settlement arrives (Phase 8)? | Phase 7 (7b), Phase 8 (8d) | Clearing ledger per `tender_modes.mode`, no Receipt vouchers; CN and Gift Card tenders leave the batch `BLOCKED` until D16 names their ledgers. |
| D17 | **OPEN — freeze when Phase 5 closes.** Inventory-integrated vouchers (stock items = ETP variant codes, godown per store, quantities from `sales_lines`) or accounting-only vouchers? | Phase 7 (7b) | Accounting-only; inventory columns are captured in the fixture but no `ALLINVENTORYENTRIES.LIST` is written; the reconciliation's inventory check reports `NOT_VERIFIABLE`. |
| D18 | **OPEN — freeze when Phase 5 closes.** Exact installed TallyPrime build (Help → About string), the TEST company name (must contain "TEST"), the machine that runs Tally and whether its HTTP loopback port is enabled. | Phase 7 (7a) | All live items in `PHASE-7-REPORT.md` stay `NOT_RUN`; the engine refuses any export whose profile environment is `PRODUCTION`; probes run only against `localhost`. |
| D19 | **OPEN — freeze when Phase 7 closes.** Collection channels and providers in scope per store — cash deposits (which bank accounts), Airpay (PAYMENTTYPE25), PhonePe (PAYMENTTYPE20), card acquirer(s) named in R020 AGENCYNAME, Paytm if used — and one sanitised sample layout per bank account and provider (Phase 2 task 15). | Phase 8 (8a) | Import families exist only for layouts with a sample under `..\ETP Source Data\collections-samples\`; every other channel shows "No statement source configured (D19)" and its obligations stay `Unmatched`. |
| D20 | **OPEN — freeze when Phase 7 closes.** Settlement window per channel (e.g. UPI T+1, card T+2, cash deposit same/next day), overdue threshold per channel, cash shortage/excess tolerance per store-day. | Phase 8 (8b, 8c) | Windows and tolerance NULL → nothing becomes `Overdue`, every non-zero cash difference is an exception case; the statement screen shows "Settlement windows not set (D20)". |
| D21 | **OPEN — freeze when Phase 7 closes.** Who approves write-offs, shortage resolutions and adjustment proposals (Owner only, or Owner + accountant), and the amount above which a second approval is required. | Phase 8 (8c, 8d) | Owner only, no threshold; every resolution needs a reason; adjustment proposals reach a Phase 7 batch only after Owner approval. |

## 4. Audit summary by module

Severity: C critical (wrong money or data loss), H high (feature unusable), M medium, L low. Full detail with file:line in the numbered audit files.

### 4.1 Reports engine (audit 01)
- C — All sales values ex-GST; NETAMOUNT dropped at import (`R025SqlImportOrchestrator.cs:54`).
- C — DSR "Service WDC" reads a field code that does not exist in `manual_input_definitions` (`OperationalReportRepository.cs:673`).
- H — Monthly target must be re-keyed per business date; staff targets need an exact period match.
- H — MTD/YTD walk-ins and service go blank unless every single day is keyed; YTD impossible with July-start data.
- H — Excel "Executive Summary" KPIs sum FTD+MTD+YTD across stores and print percentages ×100; every Excel export goes through this path.
- H — Pack/tabular PDFs clip numbers to ~10 characters and print ₹ and dashes as `?`.
- H — No DSR brand rows, no STORE/DAY target, BLA, REQ ADS; closing stock grouped by cluster not brand; cash report only sees the CASH tender and never carries forward the previous closing balance; customer-wise list impossible (names not stored).
- H — No index on `sales_invoices.transaction_date`; per-line correlated snapshot lookups.
- 7 alias catalogue entries (29 shown, 22 real). Dead `InitialReportCatalogue` with tests that only test the dead code.

### 4.2 Dashboard and Daily Workflow (audit 02)
- H — Four dashboard cards (health warnings, backup space, failed imports, latest import) are built but never attached; backup-critical warnings are invisible.
- H — "% imported" divides completed batches by files; a 6-file batch shows 17% forever.
- H — Reopen day demands a Windows-elevated token on top of the Owner role and then tells the Owner they lack permission.
- M — Finalise writes a generation row before validating; reads insert `daily_reporting_days` rows; pack generation allowed to Viewers; staff targets are write-only (no list); reasons validated only in the repository so raw `ArgumentException` text reaches the status bar.
- Startup `MainWindow_Loaded` is `async void` and only catches database-availability errors.

### 4.3 Imports, Source Inbox, Registers (audit 03)
- C — `PAYMENTTYPE25` in the Revenue Report is **Airpay UPI**: ₹12.71 lakh of ₹21.43 lakh (59%) of Titan tender value on 258 of 404 invoices, hard-quarantined from every tender report and reconciliation (`R022PersistenceProjection.cs:59-61`, `0014:432`). The Payment Type Report's AGENCYNAME column resolves the mode.
- C — R025 line identity is the workbook **row number**; any re-export with shifted rows duplicates facts and silently drops "conflict" rows while the batch still reports Completed (`R025SqlImportOrchestrator.cs:52`, `0014:385-404`).
- C — Nothing stops two different files being current for the same report/store/date; facts double and restatement then throws (`OperationalCompletionRepository.cs:51-77`).
- H — Business scope is one date (`MAX(transaction_date)`); the real exports span 55 days and the user must type exactly that max date or the import is rejected. Batch import applies one store and one date to every file.
- H — R013 (CRO Wise Sales) SR rows carry positive NETVALUE with negative QTY; the staff report sums them, overstating staff sales by returns; CRO NAME is discarded. R003/R013 have no row dedupe.
- H — AdvanceOrder Collection has the identical header as Revenue Report and would register as an empty R022 import.
- H — "OCR" and "native PDF extraction" do not exist: a regex over raw PDF bytes and an external PaddleOCR exe at an Owner-editable path that is not in the repo.
- M — Only 6 of 30 ETP workbook types importable. **Closing Stock with Display/Backstock/Defective does not exist in any ETP export** (Closing Stock has no location column; ledger LOCATION is RETAILBIN on all rows) — location split must be a manual entry. Service sales has no ETP export. No staff master table.
- M — Registers: real CRUD but Courier type missing from UI, every save forced to DRAFT, no server-side access check.
- Zero tests touch SQL Server or open a real workbook.

### 4.4 Security (audit 04)
Verdict: parameterised SQL everywhere, allow-listed audit vocabulary, fail-closed migrations, checksum-verified backups. Material gaps:
- H — Day lock state is protected only in C#: a Store Manager can `UPDATE daily_reporting_days SET status='OPEN'` in SSMS with their own login; no trigger. The "must be Windows-elevated" reopen check is client-side theatre and pushes the Owner to run the whole app as administrator.
- H — Owner-editable `ocr_helper_path` is executed by the automation task as SYSTEM: arbitrary code as SYSTEM for any db_owner. (Verified on this PC: SYSTEM is **not** SQL sysadmin, so the DENY model holds; Sagar's login is sysadmin.)
- H — **Verified on this PC:** `C:\ProgramData\EtpReporting` and `\Backups` grant BUILTIN\Users Read and Write. Unencrypted full backups of all sales and customer data are readable by any local account; anyone can plant a `.bak` that the recovery drill will restore.
- M — Store Manager has `db_datawriter` on every table, broader than the app's rules (supersede imports, edit facts on unlocked days, mark documents verified, write audit rows with any actor name). Owner can rewrite the audit table.
- M — **Verified on this PC:** only the monthly recovery-drill task is installed, running as Sagar with a limited token; the daily backup and ETP automation tasks are not installed at all. Last backup age is therefore whatever was run manually.
- M — Nothing is Authenticode-signed; PowerShell runs with `-ExecutionPolicy Bypass`; installer allows a user-writable install directory that SYSTEM tasks then execute from.
- L — Connection string accepts remote hosts and `Encrypt=False`; `powershell.exe` resolved by search path; ZIP bomb guard trusts declared sizes; CI "23 security tests" are Android JS tests.
- Licensing: nothing implemented; the deferred design would lock the shop out on a motherboard swap unless a grace mode is added first.
### 4.5 Accounting, Archive, Operations, Settings, Help, navigation (audit 05)
- C — Operations Center refresh will throw a CHECK-constraint violation for every Owner and Store Manager as soon as one failed import or one restatement exists: the data-quality sync writes severities `FAIL`/`INFORMATION` into a column that only allows `INFO`/`WARNING`/`CRITICAL` (`ProductisationRepository.cs:417-420`, `0014:189`).
- C — Approving any adjustment permanently blocks Accounting for that day: the batch source emits an `ADJUSTMENT` event but the mapping screen can only approve three other event types.
- H — No last-Owner guard: an Owner can demote or deactivate themselves and lock everyone out. `--initialize-database` and `--automation-once` ignore `settings.json` and always target `.\SQLEXPRESS\EtpReporting`. Task screens are assembled by positional child indexes into the XAML (`TaskNavigator.ResolveTaskView:422-481`); one inserted control shifts every index and breaks the workspace for the session.
- Aliases and stubs: Mapping Review, Validation, Export History and Accounting Reconciliation are the same two screens under four names; Shared Reports has no history (`share_attempts` is write-only); WhatsApp sharing cannot attach a file; SMTP settings are stored but nothing ever sends mail; Stores, Master Data and Tender Rules are write-only tables with no consumer and every store-dependent feature hard-codes WLMHW/HEMW; Approval Centre only ever contains adjustments; System Health under Settings is a subset of the dashboard; there is no background watch-folder poller; backup, recovery drill and support package work only in installer-packaged builds and the drill needs sysadmin rights. Batch approval reason is validated then discarded. Help content is real but promises features that do not exist.
- Viewer-visible items route to Import screens that deny Viewers ("Access restricted" drawer).
- Tests: no test opens a SQL connection; all T-SQL in the three big repositories is untested. 39 of 55 desktop test files contain source-text assertions.
### 4.6 UI/UX (audit 06)
Fixed on 14 Sep already: window off-screen at 1366x768, dashboard hiding cards when short, date defaulting to yesterday, "1 tasks". Remaining:
- C — Shell chrome uses 231 of 728 px before any content: 135 px header (search box row plus a second row of 48 px breadcrumb buttons, store and date), 65 px footer, 31 px caption. Content area is 497 px at full screen.
- C — The DSR, the one screen staff open daily, scrolls at full screen in the default density: store tabs need ~350 px and get ~279. Other reports show three detail rows.
- H — Every status/result message is forced to one ellipsis-trimmed line with no tooltip (`TaskBodyLayout.cs:20-21`); import results and report status are unreadable.
- H — Three levels of identical text tiles before any work: Home → module → category → task. Importing today's files is 12 taps; the DSR is 4; walk-ins entry is 7.
- H — First screen after a pointless "Continue" overlay is an IT dashboard (file counts, SQL health), not today's sales.
- H — Raw .NET text reaches staff: "A value is required. (Parameter 'reason')".
- H — DSR paints negative growth green (`DailySalesReportControls.cs:48,60`).
- H — About 40% of the shell is dead: the whole legacy panel stack, the context sidebar (populated then hidden on every route), four dashboard card builders, six overlapping navigation registries, "Stock Reports"/"Masters" routes.
- Touch: ComboBox items, radio buttons and scrollbars are default size; no `InputScope=Number` so the touch keyboard opens QWERTY for numbers; rail icons have no labels and no selected state; explanations live in tooltips on disabled controls (unreachable).
- Culture: no UI culture set, so date pickers show US format while labels show "13 Sep 2026"; grids show PascalCase headers, four-decimal numbers and 00:00:00 times; three rupee formats on one screen.
- Header date and store copy to task screens only until first visit; changing the header afterwards does nothing, so staff will save walk-ins against the wrong day. Five vocabularies for two shops (WLMHW, Titan, Titan World; HEMW, Helios; All/Combined/COMBINED/Both).
- No confirmation before Finalise, Reopen, Waive, Reject, Cancel batch, Run backup. Exports and finalise give no progress; the whole view is disabled instead.
- No high-contrast support; no per-monitor DPI manifest (125% scaling makes the 1366 panel behave like 1093x582).

### 4.7 Repository and process
- 1,993 tracked files; 973 are generated `graphify-out/`, 89 `artifacts/` (three 47 MB installers and a 41 MB evidence zip force-added despite `.gitignore`), 143 legacy Android `www/`, 92 legacy JS tests still run in CI. `.git` is 200 MB. Remote ref `origin/ui/uiux-v4-touch-first-redesign` is corrupt.
- 128 markdown documents (~100,000 words) describing acceptance that never happened on a running UI.
- 37 of 107 .NET test files assert on source text (line counts, `x:Name` counts, regexes) instead of behaviour; they block refactoring and catch nothing.
- Uncommitted fixes from 14 Sep are in the working tree (see `CLAUDE-AUDIT-PROGRESS-2026-09-14.md`).

## 5. Phases

Effort is Codex working time. Each phase ends with Claude's audit; the next phase starts only after PASS.

### Phase 0 — Stabilise and clear the ground (1–2 days)
Goal: a clean, fast repository where the only tests are ones that fail for real reasons, and the 14 Sep fixes are committed.

Tasks:
1. Commit the 14 Sep working-tree fixes as one commit: maximised window, latest-data business date default, dashboard reflow, "1 task" wording.
2. Remove from git (keep on disk if wanted): `graphify-out/`, `artifacts/`, `www/`, `prototypes/`, `verification/`, `tests/*.mjs`, `package.json`/`package-lock.json`/`node_modules`, `build-overrides/`, `capacitor.config.json`, `.graphify*`, `.code-review-graph*`, `EXPORT-*.txt`. Add them to `.gitignore`. Rewrite history is NOT required; a single removal commit is enough. Fix the corrupt remote ref (`git remote prune origin` / re-fetch).
3. Delete every source-text ratchet test (the 37 files or the individual `[Fact]`s that use `File.ReadAllText`/`ReadLines` on `src`). Keep behavioural tests. Delete `ReportCatalogueTests` sections that test `InitialReportCatalogue`, and delete `InitialReportCatalogue`, `IReportExecutor`, `ReportSourceRegistry` and the unused `VisualReportRegistry` ID scheme.
4. Move `docs/` to `docs/_archive-2026-09/` except: `14_WINDOWS_QUICK_START.md` (renamed `INSTALL.md`), `03_DATABASE_SCHEMA.md`, `04_ETP_IMPORT_PROFILES.md`, `05_MAPPING_REGISTER.md` (with the GST line corrected), this plan and the `claude-audit-2026-09/` folder. Delete `knowledge/`, `AGENTS.md` graphify instructions, `.codex/hooks.json` graph hooks. Replace `README.md` with a one-page description of the Windows app only.
5. CI (`.github/workflows/ci.yml`): remove Node/npm steps and the JS "security" tests; keep restore, build Release, `dotnet test`, PowerShell parse check, dependency scan.
6. Add SQL indexes via migration `0016_reporting_indexes.sql`: `IX_sales_invoices_date (transaction_date, store_code) INCLUDE (document_number, sales_invoice_id)` and `IX_stock_snapshots_product (store_code, product_code, snapshot_date DESC)`.
7. Fix startup robustness: `MainWindow_Loaded` catches all exceptions and shows a friendly "Cannot reach SQL Server: …" panel with a Retry button instead of crashing; default connection string gets `Connect Timeout=5`; `DesktopFriendlyError` distinguishes login failure, server unreachable and permission denied instead of one generic sentence.
8. Fix the two first-week crashes from audit 05: map data-quality severities to the CHECK-allowed set (`FAIL→CRITICAL`, `INFORMATION→INFO`) in `ProductisationRepository.SyncDataQualityIssuesAsync`; make `--initialize-database` / `--automation-once` read `settings.json` like the interactive app.
9. Add a last-Owner guard: SQL trigger on `application_users` that refuses to demote or deactivate the final active OWNER.
10. Ship `scripts/` with every build (csproj Content include) so Backup / Support package / Recovery drill work outside the installer, or hide those buttons with an explanation when the folder is absent.
11. Add a SQL integration test project that runs against a throwaway database on `.\SQLEXPRESS` (created and dropped per run): apply all migrations, then exercise every stored procedure and trigger touched in Phases 0–2. This is the foundation every later phase builds on.

Acceptance (Claude verifies):
- A0.1 `git ls-files | wc -l` < 600 and no file in git larger than 2 MB (`git ls-files -z | xargs -0 du -b`).
- A0.2 `dotnet test Etp.Reporting.slnx -c Release` passes with zero tests referencing `File.ReadAllText`/`ReadLines` of `src` paths.
- A0.3 CI workflow has no `npm`/`node` steps.
- A0.4 App launches maximised with title bar visible on 1366x768; with SQL Server service stopped the app shows the friendly panel and does not crash; with it running, DSR opens on 25 Aug 2026 with data.
- A0.5 DSR query plan uses the new index (Claude checks `SET STATISTICS IO` or the actual plan on `EtpReporting`).
- A0.6 With one failed import batch in the database, the Operations Center opens for the Owner without error.
- A0.7 `Etp.Reporting.Desktop.exe --initialize-database` against a `settings.json` pointing at a renamed database bootstraps that database, not `EtpReporting`.
- A0.8 Attempting to set the only Owner to Viewer is refused with a clear message.
- A0.9 The SQL integration test project runs green locally and in CI (CI uses the `mcr.microsoft.com/mssql/server` service container or LocalDB on `windows-latest`).

### Phase 1 — Data truth: import everything the reports need (1 week)
Goal: one click imports a day's ETP export folder for both stores, correctly, with the columns the six reports need.

Tasks:
1. **Value columns.** Persist NETAMOUNT (GST-inclusive), NETVALUE (ex-GST) and TAX as three columns on `sales_lines` (migration `0017_sales_value_columns.sql` adding `source_gross_amount`/`source_tax_amount` and back-filling from lineage where possible; for existing rows, re-import is acceptable). Reporting policy selects NETAMOUNT for "Value" per D1. Correct `docs/05_MAPPING_REGISTER.md`.
2. **Auto-detect store and business date.** From `STORE CODE` and the max `INVDATE`/`INVOICEDATE`/snapshot date in the workbook; the Import screen shows the detected values read-only with an "override" toggle for restatements. Remove the requirement to pick the store first.
3. **Folder import.** "Import today's folder" picks the two store folders (or the parent), imports all recognised workbook types in dependency order, skips duplicates (SHA-256) with a per-file result list, and never fails the batch because one unknown workbook type is present.
4. **Import every ETP report family, in one go** (owner decision 15 Sep: "drop the folder in, everything loads"). Two tiers, both keyed by exact header signature captured from the real files in `..\ETP Source Data`:
   - **Typed profiles** (staging → typed table → lineage, golden test per family on the real two-year Helios file): R025 SDB-VariantwiseSales, R022 Revenue Report, R013 CRO Wise Sales, R003 All Discount Type, R030 Variant Stock Ledger, R011 Closing Stock, **R010 BinWise Stock** (RETAILBIN/SERVICEBIN/DEFECTIVEBIN/REPLACEMENTBIN/CLOSINGBALANCE, snapshot date = export date), **R020 Payment Type Report** (AGENCYNAME → tender mode), **R014 Daywise Collection**, **R008/R009 Banking Details/Summary**, **R029 Transactionwise Bank**, **R012 CN Register**, **R024 SDB Document Wise** (customer name and phone per D3), **R018/R019 GST Tax Report Issue/Receipt** (needed for Accounting in Phase 5).
   - **Generic landing tables** for every other family (R001, R002, R004–R007, R015–R017, R021, R023, R026–R028, R031): one `etp_landing_<family>` table with the export's columns as typed text/number/date, plus file, row and lineage. Nothing is dropped, nothing blocks, and a later phase can promote a family to a typed profile without re-importing.
   - A workbook whose header matches no family is reported as **Unknown layout** with the closest family named; it never fails the rest of the folder. Empty files (header only) are recorded as "empty export" and succeed.
5. **Manual input definitions.** Seed `SERVICE_WDC`, `WCC_WALKIN`, `WCC_SALES`, `WDC_BILLS`, and monthly `SALES_TARGET` keyed by (store, month) in a new `monthly_targets` table; staff monthly targets in `staff_sales_targets` keyed by (store, cro, month) with overlap lookup.
6. **Completeness rule.** MTD/YTD manual-input aggregates return the sum of available days plus a "missing days" count; never null the whole period.
7. **Tender modes.** Map every `PAYMENTTYPEnn` column to its real mode using the Payment Type Report AGENCYNAME (PAYMENTTYPE25 = Airpay UPI, PAYMENTTYPE20 = PhonePe, …) in a `tender_modes` master table editable in Settings; stop quarantining PAYMENTTYPE25. Tender reconciliation compares GST-inclusive invoice value with tender totals.
8. **Invoice and line identity.** ETP restarts invoice numbers every financial year and its INVOICEYEAR column is the FY-end year (register IF-012). Invoice identity = (store, FY-end year: ETP INVOICEYEAR where the report has it, else derived from the date with April as month 1, invoice number). Replace the row-number line identity for R025/R013/R003 with a content key (invoice identity, item number, transaction type, quantity, net amount, sequence within invoice). Re-importing a re-exported workbook with shifted rows must produce zero new facts. Conflict rows fail the file with a visible count, never commit silently. Migration re-keys existing `sales_invoices.invoice_year` values.
9. **One current file per report/store/period.** Importing a second different file for an already-imported report and period is rejected with "already imported on … (hash …); use Restate" — unless it is a superset (later export of the same period), in which case it supersedes the earlier file automatically with a restatement record.
10. **Multi-day scope.** A workbook's scope is its full date range (min..max), not one date. Locking, evidence and restatement key on the range; single-day imports are the special case.
11. **R013 signs and stock ledger types.** (Register IF-013: approve STM Issue, STM Receipt, Stock Issue, Stock Receipt and BC as signed stock movements alongside INV/SR/Purchase Receipt/Purchase Return; an unknown type warns per row and skips it, never blocks the file.) Normalise SR rows to negative net value at staging; persist CRO NAME; add a staff master (`staff` table: code, name, store, active) seeded from the first import and editable in Settings.
   **Sales transaction types** (register IF-003): INV = sale, SR = sales return, **BC = bill cancellation** (own 3xxxxxxxx document series, references the original invoice via INVREFNO; treated exactly like SR for sales, tender and staff, and as a stock receipt). Add STM Dispatch to the approved stock types. Any other type is a per-row warning, never a period block.
12. **Ambiguous headers.** Distinguish AdvanceOrder Collection from Revenue Report by sheet name/file name, not header signature alone; unknown-but-harmless workbook types are reported as "Not needed" instead of failing the batch.
13. **Location split for closing stock** is manual: a touch-friendly per-brand entry (Display / Backstock / Defective / Y Loc) with yesterday's numbers pre-filled; System comes from Closing Stock.
14. Delete the OCR/PDF-extraction scaffolding and the Owner-editable helper path (audit 03 §D, audit 04 §1.8). Source Inbox keeps only "attach a scanned document to a day" with hash and viewer.
15. ZIP extraction counts actual inflated bytes; Excel lock files (`~$*.xlsx`) are skipped; temp folders are deleted after each batch.

Acceptance:
- A1.1 Importing the two real folders for 25 Aug 2026 on an empty database completes in one action; result list shows every file as Imported / Duplicate / Unsupported with counts.
- A1.2 SQL check: Titan 25 Aug lines sum NETAMOUNT = 34,215.00 and NETVALUE = 28,995.76; Helios 29,290.00 / 24,822.02; August MTD Titan NETAMOUNT = 938,197 and invoices = 182; Helios 774,869 / 38 (values recomputed by Claude from the workbooks).
- A1.3 Re-importing the same folder writes zero new rows.
- A1.4 CRO, tender-by-mode, banking, CN and customer header tables populated for 25 Aug with counts matching the workbooks.
- A1.5 Golden tests for every profile run in CI without SQL Server (parser level) and the SQL tests run against LocalDB/SQLEXPRESS on Claude's audit.
- A1.6 Tender report for Titan August shows UPI (Airpay + PhonePe + BHIM) as the largest mode and the sum of all modes equals the GST-inclusive invoice total to the paisa for every day; nothing sits in "quarantined".
- A1.7 Importing the Titan SDB workbook, then a copy with three rows moved to the top, adds zero facts and reports "Duplicate content".
- A1.8 Importing the July–August export once as a 55-day file works without the user typing any date; the Daily Workflow shows each day inside the range as "sources present".
- A1.9 Staff report for 25 Aug shows returns as negative and CRO names, and the staff total equals the store total.
- A1.11 **Whole-folder test:** importing `ETP Source Data\HEMW	ill 6 sep 26` (all 31 consolidated files, Info sheets present) in one action into an empty database ends with every file Imported or "empty export", zero Unknown-layout and zero blocked files, and the register gains no new OPEN rows.
- A1.9a Importing the two-year Helios R025 stores exactly 790 lines and 759 invoices (workbook counts) with zero CONFLICT outcomes; invoice 100000068 exists three times with FY-end years 2025, 2026 and 2027; every month in `golden-monthly-HEMW-R025.csv` matches. The two-year `R030_Variant_Stock_Ledger.xlsx` (3,955 rows, five transfer types) imports without a blocker.
- A1.10 `docs/audit/IMPORT-FAILURE-REGISTER.md`: every row mapped to Phase 1 is FIXED, and importing the complete two-year Helios consolidated folder (`ETP Source Data\HEMW	ill 6 sep 26`, Info sheets included, no manual store or date entry) plus the Titan folder produces zero new OPEN rows. Monthly totals match `ETP Source Data\HEMW\golden-monthly-HEMW-R025.csv`.

### Phase 2 — The six evening reports, matched to the owner's sheets (1–2 weeks)
Goal: after the import, the owner opens "Today" and sees the same numbers as the Excel sheets, and can export/share each as PDF and Excel.

Tasks (each report gets its own screen, Excel export, PDF export and a golden test against 25 Aug 2026):
1. **DSR** — per store block with STORE TGT, DAY TGT, MTD BLA, REQ ADS; VOL/VALUE/AUPT/AVPT × FTD, LY, GROWTH%, MTD, YTD, LY YTD; brand rows per D4; RETAIL WALKIN, INVOICE, CONVERSION%; WCC WALKIN/SALES, WDC BILLS; combined WOT+HELIOS block. INVOICE count = INV documents only (SR excluded from the denominator). LY per D2; "—" when no LY data.
2. **Closing stock by brand** — Display, Backstock, Defective, Y Loc, Physical (= sum of the four), System, Difference, Remark; grouped by BRAND (not cluster); physical entry screen keyed by brand with the previous day's counts pre-filled; totals row.
3. **Cash book** — Dr/Cr layout per store per day: opening balance carried from the previous day's closing (editable with reason), expenses, bank cash deposit, per tender mode from Payment Type/Daywise Collection (Cash, Card, UPI, CN, TC), service Cash/Card/UPI, total sale, closing balance; multi-day view for a month.
4. **Service sale report** — WDC, Cash, Card, UPI, Total for the day; MTD, LY MTD, TY YTD, LY YTD.
5. **Customer-wise invoices** — per store per day: customer name, invoice qty, net value (GST-inclusive), grand total; from SDB Document Wise / R022.
6. **CRO-wise sales** — target, net sales, net qty, discount, ATV, UPT, transaction count, with monthly targets from Phase 1.
7. **Exports** — replace the fixed-width tabular PDF with measured column widths and an embedded Unicode font (₹, —); Excel gets real number cells with Indian grouping (`[>=10000000]##\,##\,##\,##0;[>=100000]##\,##\,##0;##,##0`), dates as dates, freeze panes, autofilter; remove the "Executive Summary" KPI sheet or compute it correctly (no summing percentages). DSR PDF stays single A4 landscape but sizes tiles to content.
8. Fix C9–C16 from audit 01 (SR in counts, target halving, `TyInvoices ?? 0`, tender-variance sign, cash blocking on optional inputs, service SumIfAny).
9. Remove alias catalogue entries (`stock-group`, the five `exception-*` filters become filter chips on one Exceptions screen).
10. **Brand rows master** (D4): Owner-editable screen in Settings mapping ETP brand codes to DSR rows per store; the DSR reads from it.
11. **Historical import** (D8): import the 2025-26 exports for both stores through the same profiles (multi-month scope from Phase 1); LY, growth and LY YTD then populate. Acceptance: A2.6.
12. **Amount basis on every report.** Reporting project: `static class AmountBasis` in `src/Etp.Reporting.Reporting/ReportDefinition.cs` with exactly five constants — `GST-inclusive sales`, `tender received`, `quantity`, `stock units`, `variance` — and a `string AmountBasis` member on `ReportDefinition` and on `ExcelReportMetadata` (`OpenXmlReportExporter.cs`). Assignment: DSR value columns and Customer-wise invoices and CRO-wise net sales = `GST-inclusive sales`; DSR VOL/walk-ins/invoice counts and CRO qty = `quantity`; Closing stock by brand = `stock units`; Cash book and Service sale = `tender received`; Tender reconciliation / Exceptions = `variance`. A report with two bases labels each column group ("Value — GST-inclusive sales", "Vol — quantity"). `ReportsWorkspaceView.SetExport` passes the basis; the screen shows "Amounts: <basis>" under the report title; Excel writes it in the metadata block of the first sheet; PDF prints it in every page header; `DailyReportingPackService` pack sheets carry the same line. Test: `Etp.Reporting.Reporting.Tests/EveningReportExportTests.cs` asserts every one of the six reports and the Exceptions export emits a basis from the five constants and that no export contains the bare word "Revenue" as a column header.
13. **Report acceptance record** (document, not code). `PHASE-2-REPORT.md` gets a table with one row per report (DSR, Closing stock by brand, Cash book, Service sale, Customer-wise invoices, CRO-wise sales, Exceptions) and columns: report · business question (one sentence) · sources (tables/views read) · amount basis (task 12) · manual inputs (field codes) · control totals (the identity the golden test asserts, e.g. "brand rows sum = store VALUE") · missing-data behaviour ("—", Blocked, or "sum of available days + missing-days count") · screen route (Phase 3 section/tab) · export paths (Excel/PDF file name pattern) · golden test (class.method) · status (`DRAFT` / `GOLDEN_GREEN` / `AUDIT_PASS`). Claude's audit copies the table into `PHASE-2-AUDIT.md` and sets the status column; a row without a golden test cannot be `AUDIT_PASS`.
14. **Cash sent for deposit and bank credit confirmed as two states.** Next free migration: table `cash_deposits` (`cash_deposit_id bigint IDENTITY PK`, `store_code varchar(30) NOT NULL FK stores`, `business_date date NOT NULL`, `amount decimal(19,4) NOT NULL CHECK (amount >= 0)`, `deposit_reference nvarchar(80) NULL` (slip / bag number), `sent_by nvarchar(100) NULL`, `modified_by`, `modified_utc`, `change_reason nvarchar(500) NOT NULL`; `CK_cash_deposits_none`: `amount > 0 OR (deposit_reference IS NULL AND sent_by IS NULL)`; filtered unique index `UX_cash_deposits_none_per_day (store_code, business_date) WHERE amount = 0` — a zero row is the explicit "No cash sent today"), history table `cash_deposit_history` filled by trigger as `manual_operational_inputs`, and locked-day trigger `trg_cash_deposits_protect_locked` (next unused 51xxx) on INSERT/UPDATE/DELETE. Table `cash_deposit_confirmations` (`cash_deposit_id bigint NOT NULL FK`, `confirmed_date date NOT NULL`, `bank_reference nvarchar(80) NOT NULL`, `confirmed_by`, `confirmed_utc`, `withdrawn_utc datetime2(3) NULL`, `withdrawn_reason nvarchar(500) NULL`; filtered unique `(cash_deposit_id) WHERE withdrawn_utc IS NULL`; trigger refuses a confirmation for a zero-amount row) — **not** guarded by the day lock, because bank credit is confirmed days after the reporting day is finalised. View `cash_deposit_status` returns one row per deposit with `status` in exactly `SENT_FOR_DEPOSIT` / `BANK_CREDIT_CONFIRMED` / `NONE_SENT`. `manual_input_definitions.CASH_DEPOSIT` is set `is_active = 0, is_required_for_finalisation = 0` and every existing value becomes one `cash_deposits` row (`change_reason = 'Migrated from CASH_DEPOSIT'`). `DailyReportingWorkflowRepository` readiness adds "Cash sent to bank" to the missing list when the store-day has no `cash_deposits` row. `OperationalReportRepository.LoadCashBookAsync` (`CashBookRepository.cs`) reads `Deposit = SUM(amount)` for the day and `CashBookDay` gets `decimal DepositInTransit` = sum of `SENT_FOR_DEPOSIT` rows with `business_date <= day`; `CashBookTables.Create` adds the information row "Cash sent to bank, not yet credited" and `CashBalanceReconciliationService` keeps `closing = opening + retail cash + service cash − expenses − deposits sent + adjustment`. Screen (Today → Cash in Phase 3; the Cash book report screen until then): panel "Cash sent to bank" listing the day's rows (amount, slip no., status in plain words "Sent to bank" / "Bank credited on dd MMM" / "No cash sent"), buttons **Add deposit** (amount, slip no., who took it), **No cash sent today**, per row **Bank credited** (date, bank reference) and **Undo** (reason). Store Manager and Owner may add and confirm (`CanImport`); Viewer reads. Phase 8 slice 8b replaces the manual tick with a match to a bank-statement line; the table stays the fact. Tests: `Etp.Reporting.SqlServer.IntegrationTests/CashDepositsSqlTests.cs` (locked day refuses insert, allows confirmation; second zero row refused; confirmation of zero row refused; history row written; view statuses), unit test in `CashBalanceReconciliationServiceTests.cs` with two deposits and one in transit, golden 25 Aug cash book unchanged in totals.
15. **Collections samples for Stage 3** (owner, no code). Sagar exports one calendar month (August 2026) of: the bank statement for every account that receives shop money; Airpay and PhonePe settlement reports; the card acquirer statement for each AGENCYNAME code seen in R020 (`CC01 … CC10`); Paytm if used. Files go under `..\ETP Source Data\collections-samples\<bank-or-provider-key>\` with a `README.txt` per folder (export screen, period, which column is amount / value date / reference / fee). Sanitised: account numbers masked to the last four digits, no customer names; never copied into the repository (rule 10). Codex lists in `PHASE-2-REPORT.md` the files received (folder, file name, row count, period) — no content. Missing files do not block Phase 2 closure; they are logged in Section 7 and block Phase 8 slice 8a for that channel.

Acceptance:
- A2.1 For 25 Aug 2026 the DSR screen and PDF show, for the invoices present in the export: Titan VOL 5, VALUE ₹34,215, AUPT 1.00, AVPT ₹6,843; Helios VOL 2, VALUE ₹29,290; MTD Titan ₹9,38,197 / 182 invoices, Helios ₹7,74,869 / 38; brand rows sum to the store value; combined block = sum of stores. Claude recomputes each from the workbooks.
- A2.2 Closing stock by brand for 25 Aug matches the System column of the owner's sheet brand by brand (Titan total 812, Helios 501), with Physical entry working by touch.
- A2.3 Cash book for 25 Aug reproduces the owner's sheet structure with tender totals equal to the Revenue Report per-mode sums Claude computes.
- A2.4 Every export opens in Excel/Acrobat with no `?` characters, no clipped numbers, Indian grouping, and percentages shown once.
- A2.5 Each report has a golden test against the real workbooks; `dotnet test` green.
- A2.6 (moved from task 11 for numbering; text unchanged) After loading the 2025-26 files, the DSR for 25 Aug 2026 shows LY and GROWTH% values, and LY YTD for Titan equals the sum Claude computes from the 2025-26 workbook for 1 Apr–25 Aug 2025.
- A2.7 Every report screen shows "Amounts: <basis>" under its title and every Excel/PDF export of the six reports and Exceptions carries the same basis from the five allowed strings; Claude opens each export and greps the PDF text and the Excel metadata sheet. No export uses "Revenue" as a bare column header.
- A2.8 `PHASE-2-REPORT.md` contains the report acceptance record with all seven rows and every column filled; each named golden test exists and is green in `dotnet test`; Claude sets each status.
- A2.9 Cash book, Titan 25 Aug: Claude adds a deposit of 4,000.00 with slip "A1" → the screen shows a "Sent to bank" row, closing balance falls by exactly 4,000.00 and "Cash sent to bank, not yet credited" shows 4,000.00; Claude finalises (locks) the day, then clicks **Bank credited** with 27 Aug and reference "B1" → allowed, status "Bank credited on 27 Aug", in-transit 0.00; clicking **Add deposit** on the locked day is refused with the plain-words finalised message; `SELECT status FROM dbo.cash_deposit_status WHERE store_code='WLMHW' AND business_date='2026-08-25'` returns `BANK_CREDIT_CONFIRMED`; Close-day readiness lists "Cash sent to bank" as missing for a day with no deposit row and clears after **No cash sent today**.
- A2.10 `..\ETP Source Data\collections-samples\` holds at least one bank statement folder, one Airpay folder and one PhonePe folder each with a `README.txt` and a file covering August 2026; `PHASE-2-REPORT.md` lists them; the repository contains no file from that folder (`git ls-files | grep -i collections` is empty).

### Phase 3 — Touch-first shell, two levels deep (1 week)
Goal: shop staff reach any daily task in two taps; nothing scrolls at 1366x768 full-screen; nothing disappears when the window shrinks.

Tasks (from audit 06; §10 of that file has the full screen mapping):
1. **New shell.** Five large labelled buttons in the left rail, always visible, with a selected state and an attention badge: **Today · Import · Reports · Stock · Settings**. Level 2 is a row of tabs inside each section. No module tiles, no category tiles, no overview screens, no context sidebar, no welcome overlay (auto-continue when a role resolves; keep it only for "database setup required"). Delete `ShowTaskOverview`, `ShowReportCategories`, `LegacyWorkspaceScroll` and its twelve panels, `ContextSidebar`, `BreadcrumbText`, `AccessStatus`, `ModuleTile`/`StatusBadge`/`DetailDrawer`, `WorkspaceModuleOwnershipRegistry`, `WorkspaceLocation`/`WorkspaceNavigationHistory`, `ShellPresentationMetadata`, the "Stock Reports" and "Masters" routes, and the six navigation registries in favour of one `TaskNavigation` table plus a small route map.
2. **Chrome budget.** One 48 px header row (section title · business date · store) and a 28 px footer. Search moves behind a rail icon. Content area at 1366x728 must be at least 610 px.
3. **Today section** (landing screen): tabs Sales (DSR for the header date, auto-run, Export PDF and Share as visible primary buttons), Cash (cash book from Phase 2), Walk-ins (walk-ins field pre-selected, reason defaulted), Close day (readiness, Finalise, Generate pack). The DSR fits without scrolling: KPI strip, both store blocks side by side, service/targets column.
4. **Import section**: one flow — store and date detected from files, "Import today's folder" primary button, per-file result list, progress bar with Cancel. Problems tab merges quarantine/duplicates/conflicts/failures into one grid with a status filter.
5. **Reports and Stock sections** as mapped in audit 06 §10; every report has Export PDF / Export Excel as buttons, not a menu; detail grids get explicit columns with `dd MMM yyyy` dates, ₹ with lakh grouping, right-aligned two-decimal numbers, plain-English headers.
6. **One scope.** Header date and store are bound two-way to every task; no per-task date pickers except on multi-day reports (from/to). One vocabulary: "Titan World", "Helios", "Both stores".
7. **Touch.** 44 px minimum for ComboBox items, radio buttons, tab headers, DataGrid rows, calendar cells; 24 px scrollbars; `InputScope=Number` on every numeric box; `ToolTipService.ShowOnDisabled` replaced by inline help text; no hover-only information; density setting becomes Touch (44) / Desktop (36) with Touch as the default on the shop PC.
8. **Feedback.** Status and result messages wrap (max 3 lines, "More" opens details); required fields validated in the presentation session before any database call with a visible "*"; `DesktopFriendlyError` never shows "(Parameter …)" or RAISERROR text; success toast; progress bar plus Cancel for import, export, pack generation and finalise; confirmation sheet before Finalise, Reopen, Waive, Reject, Cancel batch, Run backup/drill/automation.
9. **Culture and consistency.** `en-IN` set at startup for UI culture and number parsing; `dd MMM yyyy` everywhere; one style system (`Themes/`) — delete `DsrUi` literals, `DashboardView` brushes, `ReportWorkspaceControls` literals, the duplicate `Card`/`NavigationButton` in `MainWindow.xaml`; sentence-case buttons; one spelling ("Centre"); copy rewritten for staff (≤ 12 words, no "governed/canonical/lineage/immutable"). Growth colours by sign.
10. **DPI and accessibility.** PerMonitorV2 manifest; one breakpoint (compact below 1000 DIP); `HeadingLevel` on page and card titles; F6 covers rail and footer; high-contrast fallback on the three custom templates.
11. Delete the dead dashboard card builders and the "Today overview" IT dashboard; its SQL/backup facts become a one-line health strip under Settings → Database and a badge on Today only when critical.

Acceptance: A3.1 Launch → DSR in 0 taps (it is the landing screen), → import today's folder in 2 taps, → walk-ins entry in 2 taps. A3.2 Screenshots at 1366x768 in Touch density show no scrollbars on Today/Sales, Today/Walk-ins, Import, Reports list, Stock/Closing stock, Settings. A3.3 At 816x480 and at 125% DPI every control remains reachable (scroll appears, nothing hidden). A3.4 UI Automation dump of every screen shows no interactive control under 44 px tall. A3.5 Changing the header date then opening Walk-ins shows that date. A3.6 Saving walk-ins without a value shows "Enter the walk-in count" inline, never a parameter name. A3.7 `grep -r "governed\|canonical\|lineage\|immutable" src/Etp.Reporting.Desktop --include=*.xaml --include=*.cs` finds no user-facing string. A3.8 A shop-staff walkthrough (open app → import today's folder → enter walk-ins and cash → view DSR → export PDF) completes with touch only, timed under 3 minutes.

### Phase 4 — Security and operations hardening (3–5 days)
Tasks (from audit 04, in priority order):
1. **Day lock in SQL.** `INSTEAD OF UPDATE` trigger (or a stored procedure that is the only granted path) on `daily_reporting_days`: LOCKED→OPEN only for members of an `etp_owner` database role, with a non-empty reason written to `daily_reporting_events` in the same statement. Delete the client `administratorApproved` flag and the elevated-token check (`DailyWorkflowWorkspaceView.xaml.cs:412-416`). The lock covers the reporting day (`daily_reporting_days.status`) only; Phase 8 settlement closure gets its own state and never reuses this flag.
2. **Folder ACLs and backups.** Bootstrap sets `C:\ProgramData\EtpReporting\{Backups,Documents,Share}` to SYSTEM, Administrators and the SQL service only (`icacls /inheritance:r`); backup rotation (keep 14 daily + 12 monthly); `BACKUP … WITH ENCRYPTION (ALGORITHM = AES_256, SERVER CERTIFICATE = EtpBackupCert)` per D9: a Settings → Database action creates the certificate and exports it plus its private key with a password to a location Sagar chooses (off the PC: USB or cloud folder), records the export hash, and the recovery drill fails loudly if no exported certificate is on record; the restore procedure is verified on a second machine and written in `docs/OPERATIONS.md`. Fix the ACL on the existing PC as part of the phase.
3. **Scheduled tasks.** Install the daily backup and ETP automation tasks (they are missing on the live PC); register all three with the same principal; run automation under a dedicated least-privilege local account with the STORE_MANAGER SQL role, not SYSTEM. Recovery drill restores the file named in the last verified receipt (hash-checked), compares against the backup's own metadata, and reports into System status. `backup-etp-database.ps1` records, in the same script run immediately before the `BACKUP` statement, a `rowCounts` block in `<backup>.receipt.json` and `<database>-latest-verified.json` with `COUNT(*)` of `sales_invoices`, `sales_lines`, `import_files`, `daily_reporting_days`; `invoke-etp-recovery-drill.ps1` reads the same four counts from the restored isolated `EtpRecovery_<guid>` database and fails the drill, naming the table and both numbers, on any difference.
4. **Store Manager grants.** Replace `db_datawriter` with explicit per-table grants matching the app's rules; INSERT on `operational_audit` only via a stored procedure that takes actor from `SUSER_SNAME()`; audit table append-only for everyone (INSTEAD OF UPDATE/DELETE trigger); maintenance script archives instead of deleting.
5. **Connection string.** Allow only local instance forms (`.\SQLEXPRESS`, `(local)`, `localhost`, `lpc:`, `np:`); reject `Encrypt=False`, `AttachDbFilename`; drop `TrustServerCertificate=True` default in favour of `Encrypt=Optional` for local; absolute `powershell.exe` path; `-x` on every sqlcmd call.
6. **Signing, SmartScreen path and hashes — no certificate purchase (owner decision 15 Sep 2026).** No commercial code-signing certificate is bought. `scripts/build-windows-release.ps1` and `scripts/build-windows-installer.ps1` sign the exe, every packaged `scripts/*.ps1` and the installer with a self-signed code-signing certificate created once (`New-SelfSignedCertificate -Type CodeSigningCert`, 10-year validity, private key never leaves the build PC and never enters the repository); the public `.cer` ships in `installer/` and `bootstrap-etp-prerequisites.ps1` installs it into `LocalMachine\TrustedPublisher` and `LocalMachine\Root` on the shop PC so `-ExecutionPolicy AllSigned` holds for the packaged scripts; the builders accept this thumbprint and treat the timestamp server as optional. Because the publisher is not a public CA, Windows still shows the SmartScreen "unknown publisher" warning: `docs/OPERATIONS.md` documents the path **More info → Run anyway** to be taken only after the installer's SHA-256 matches the published value. Every release publishes the SHA-256 of the installer and of `Etp.Reporting.Desktop.exe` in the `CHANGELOG.md` entry for that version and in the release receipt; `docs/OPERATIONS.md` gives the `Get-FileHash` command to compare. Remove `PrivilegesRequiredOverridesAllowed`; task installers refuse a non-admin-writable install folder.
7. Normalise line endings before hashing migrations; write the Owner break-glass procedure (`docs/OPERATIONS.md`); pin GitHub Actions to SHAs; CI security step runs the .NET boundary tests and names them honestly.

Acceptance: A4.1 As a Viewer login in SSMS, `UPDATE daily_reporting_days SET status='OPEN'` and `DELETE FROM operational_audit` both fail; as Store Manager, updating `import_files.is_superseded` and `sales_lines` fails. A4.2 `icacls` on the three ProgramData folders shows no BUILTIN\Users entry. A4.3 `Get-ScheduledTask` shows three ETP tasks with the same non-SYSTEM service principal, and a daily backup has run within 24 h. A4.4 Backup → drill restore → verify passes on the audit PC from the receipt-named file, **and** `<database>-latest-drill.json` lists for `sales_invoices`, `sales_lines`, `import_files` and `daily_reporting_days` the count recorded in the backup receipt and the count read from the restored copy, all four pairs equal and equal to `SELECT COUNT(*)` run by Claude on the live database at backup time; System status shows the drill as passed with those four counts. A4.4a Claude copies the receipt, changes `rowCounts.sales_lines` by one and runs the drill against the copy: the drill fails, the message names `sales_lines` and both numbers, `operational_audit` gets a `RestoreDrill` / `Failed` row and System status shows the failure; the original receipt still passes. A4.5 Support package and diagnostics for a session that imported the real files contain no customer name, phone, file path or SQL text. A4.6 Connection settings reject `Server=remotehost` and `Encrypt=False`. A4.7 `Get-AuthenticodeSignature` on the installed exe, every `scripts/*.ps1` and the installer returns `Valid` on the shop PC; `Get-ExecutionPolicy -Scope Process` inside a running task is `AllSigned`; the `CHANGELOG.md` entry for the installed version lists two SHA-256 values that match `Get-FileHash` of the installer and the exe; `docs/OPERATIONS.md` contains the "More info → Run anyway" step and no instruction to buy a certificate.

### Phase 5 — Secondary modules: finish, hide or delete (per D6; 3–10 days)
Disposition per D6 (decided 15 Sep 2026): all four business modules are kept and finished; OCR scaffolding is deleted.

| Module | Disposition | Work |
|---|---|---|
| Accounting / Tally export | **Keep and finish, Owner-only** (D6); **Stage 2 foundation (S2-01)** | Fix the ADJUSTMENT mapping gap (`AccountingEventInput` offers `ADJUSTMENT`; `LoadApprovedAccountingMappingsAsync` resolves it); persist the batch approval reason (`ProductisationRepository.ApproveAccountingBatchAsync` writes `@reason` to new `accounting_batches.approval_reason nvarchar(1000)` — next free migration); make mapping approval one transaction (`ApproveMappingAsync` create → decide → save on one connection); add Reject (`REJECTED`, reason mandatory). Batch status values become exactly `DRAFT` / `BLOCKED` / `APPROVED_READY` / `EXPORTED_AWAITING_IMPORT` / `REJECTED` (migration re-creates `CK_accounting_batches_status` and maps `REVIEW→DRAFT`, `APPROVED→APPROVED_READY`, `EXPORTED→EXPORTED_AWAITING_IMPORT`); `BLOCKED` is set with a reason when a mapping is missing or a D12–D18 value is unset; nothing in the engine, screens, exports or audit ever labels a batch "posted" or "imported" — only a Phase 7 read-back may. Batch business key = the Phase 1 invoice identity: new `accounting_batch_invoices (accounting_batch_id FK, store_code, invoice_year, document_number)` listing every INV/SR/BC invoice the day batch covers, with a trigger refusing an invoice already in a batch whose status is not `REJECTED` (THROW, next 51xxx). Export receipt persisted: new `accounting_export_receipts (accounting_batch_id FK, output_path nvarchar(500), sha256 char(64) hex CHECK, tally_company_name nvarchar(200), environment_label varchar(10) CHECK IN ('TEST','PRODUCTION'), exported_by, exported_utc)`, append-only trigger; company name and label come from new `product_settings.tally_company_name` / `tally_environment_label` (Owner-editable in Settings, default `TEST`; switching to `PRODUCTION` requires typing the company name), replacing the literal "Saagar Traders" in `AccountingWorkspaceView.xaml.cs`; `AccountingExportReceipt` gains `CompanyName` and `EnvironmentLabel`; SHA-256 of the file stays. Delete the four alias tasks (Mapping Review, Validation, Export History, Reconciliation) and show one screen **Prepare → Review → Export** with an export history grid (batch, time, hash, file, company, TEST/PRODUCTION). Voucher shape unchanged (one Journal per day via `TallyXmlExportService`); Sales vouchers, read-back and reconciliation are Phase 7. |
| Archive and sharing | **Keep and finish** (D6) | One screen: list of generated packs with Open / Excel / PDF / ZIP / Share on every row. Sharing: contacts list (name, WhatsApp number, email), WhatsApp Desktop launch with the PDF path copied, real SMTP sending (MailKit) using the stored settings with a test-send button, and a delivery history per pack (`share_attempts` gets a reader and a final outcome). Delete Compare and the Restatements filter. |
| Registers | **Keep and finish** (D6) | Fix Courier type, allow DRAFT → VERIFIED with reason, server-side access check, register entries visible from the day's Close-day tab; seven types. |
| Source Inbox / OCR / native PDF | **Delete** | Remove `DocumentIntakeService`, PaddleOCR helper path, `document_extractions`, OCR settings and Help topic. Keep "attach a scanned document to a business day" only if D6 asks. |
| Approvals / Adjustments | **Keep and finish** (D6) | Store Manager raises adjustment and restatement requests; Owner approves from one queue that also shows decided history; wire the RESTATEMENT producer (import restatement waits for approval); delete the unused REOPEN_DAY / MASTER_MAPPING / CONTROL_WAIVER types. |
| Stores / Master data / Tender rules | **Replace** | Stores table becomes the single source for every store list (delete all hard-coded WLMHW/HEMW); tender modes master from Phase 1 replaces "Tender rules"; delete `controlled_master_values` and its screens. |
| Scheduler / Watch folder | **Keep, honest** | Rename to "Automatic import"; show the installed task status and last run; remove `poll_minutes`; fix the app-lock release; a full ETP ZIP with unsupported workbook types must succeed with "not needed" entries. |
| Help centre | **Rewrite to match** | One topic per top-level tab of the Phase 3 shell, screenshots included; delete Coming Soon states. |
| Investigation search | **Keep** | Add click-through from a result row to the report or import it names. |

Acceptance: A5.1 Every navigation destination reachable by a role leads to a working screen for that role (Claude walks all of them as Owner, Store Manager and Viewer). A5.2 No screen in staff navigation is an alias of another. A5.3 Accounting: approve an adjustment for 25 Aug, then Prepare Batch for 25 Aug succeeds. A5.4 `grep -r "WLMHW\|HEMW" src/` returns only the seed migration and tests. A5.5 `SELECT DISTINCT status FROM dbo.accounting_batches` returns only values from `DRAFT / BLOCKED / APPROVED_READY / EXPORTED_AWAITING_IMPORT / REJECTED`; `grep -ri "posted\|imported into tally" src/Etp.Reporting.Desktop src/Etp.Reporting.Infrastructure.SqlServer --include=*.cs --include=*.xaml` finds no batch-status string. A5.6 Preparing a second batch for a day whose first batch is `APPROVED_READY` or `EXPORTED_AWAITING_IMPORT` is refused with the plain-words message naming the earlier batch; after the first is `REJECTED` it succeeds. A5.7 Exporting an approved batch writes one `accounting_export_receipts` row whose `sha256` equals `Get-FileHash` of the file, whose `tally_company_name` equals the Settings value and whose `environment_label` is `TEST`; the export history grid shows the same row; with the reason box empty, Approve is refused and `approval_reason` stays NULL.

### Phase 6 — Release (2–3 days)
Installer that installs/updates SQL Express and the app in one run on a clean Windows 10/11 PC; daily backup task; one-page `docs/USER-GUIDE.md` with screenshots; version 2.0.0. Acceptance: Claude installs on a clean VM or a second PC, imports the two folders, produces the six reports, upgrades from the previous build without data loss.

### Phase 7 — Tally transfer (Stage 2) (6–10 weeks, after Phase 6)
Goal: one approved ETP sales invoice travels from the Phase 1 facts through an approved, versioned mapping into a Sales voucher in an isolated TallyPrime 7.1 TEST company, the engine reads back what Tally actually stored and proves it equal to the source and to the file it wrote, and the same invoice can never become a second voucher — then the same path is widened to returns, split tenders, multi-line invoices, a full reconciliation engine and a connected (HTTP) mode, while PRODUCTION books stay disabled until the gate in task 26 passes.

Prerequisites:
- Decisions frozen by the owner and accountant when Phase 5 closes (recorded OPEN in Section 3 now): D12 one Tally company per store or one for the firm; D13 one Sales voucher per invoice or one daily summary per store; D14 named customer ledgers or one "Cash Sales / Retail Customers" ledger; D15 output-tax ledger names per GST component (CGST, SGST, IGST) and rate, and whether items carry HSN; D16 tender inside the Sales voucher or a clearing ledger per mode settled by separate Receipt vouchers; D17 inventory-integrated vouchers (stock items, godown per store) or accounting-only; D18 exact installed TallyPrime build, the TEST company names, the machine that runs Tally, the numbering method of the TEST company's Sales and Credit Note voucher types (Automatic / Manual / Multi-user auto) and whether "Prevent duplicates" is on. Until a decision is frozen the affected task builds validation and the `BLOCKED` state only. Slice 7a is written for D13 = `PER_INVOICE`, D16 = `IN_VOUCHER`, D17 = `ACCOUNTING_ONLY` and D14 = `SINGLE_LEDGER` (the only party policy task 1's CHECK allows with `IN_VOUCHER`); if D13 chooses a daily summary the business key becomes (store, FY-end year, business date) with `document_number = 'DAY'` and the "invoice" in tasks 6–12 is the day's summary.
- Environment: TallyPrime 7.1 on the D18 machine with Client/Server Configuration set to Both or Server and a port in 9000–9999 (setting labels and the default 9000: verify on the installed build, record in `PHASE-7-REPORT.md`). Tally cannot bind to loopback only, so a Windows Firewall inbound rule named "ETP Tally port" blocks that port from every address except 127.0.0.1; the owner captures `netsh advfirewall firewall show rule name="ETP Tally port"` as A7.2 evidence. Two isolated companies named `TEST - <firm> A` and `TEST - <firm> B` (B only for the wrong-company checks). The accountant keys the anchor vouchers by hand in company A and exports them as XML: for D17 = `ACCOUNTING_ONLY` one Sales voucher in accounting-voucher mode (Ctrl+H → As Voucher — verify the key on the installed build; its export shows `PERSISTEDVIEW` Accounting Voucher View, `ISINVOICE` No and `ALLLEDGERENTRIES.LIST`), for `INVENTORY` a second anchor in invoice mode (`LEDGERENTRIES.LIST` + `ALLINVENTORYENTRIES.LIST`), and for 7b one Credit Note, one ledger master and one voucher-type master (plus one stock item if D17 = `INVENTORY`). Originals are filed under `..\ETP Source Data\tally-samples\` (never in the repo); sanitised copies (synthetic party, no phone) under `tests-dotnet/fixtures/tally-golden/anchor-sales-voucher.xml`, `anchor-sales-invoice-view.xml`, `anchor-credit-note.xml`, `anchor-ledger.xml`, `anchor-voucher-type.xml`, `anchor-stock-item.xml` (rule 10). If the machine cannot be probed, every live item stays `NOT_RUN` with its gate closed; nothing is fabricated.
- Data and code from earlier phases: Phase 1 `sales_invoices` (UQ `store_code, invoice_year, document_number`; `document_number nvarchar(80)`; `transaction_date`), `sales_lines` (`line_identifier`, `product_code`, `source_gross_amount` GST-inclusive, `source_net_amount` ex-GST, `source_tax_amount`, `source_transaction_type`, `source_lineage_id`), view `reporting_sales_tenders`, `tender_modes` (`source_tender_code`, `mode`, `active`), `etp_r018` (per-line `cgst_rate/_amount`, `sgst_utgst_rate/_amount`, `igst_rate/_amount`, `cess_rate/_amount`, `hsn_code`), `etp_r024`, `etp_r025`; Phase 4 database roles `etp_owner`, `etp_store_manager`, `etp_viewer` and procedures `dbo.record_operational_audit`, `dbo.submit_approval_request`, `dbo.decide_approval_request` (branch `phase-4/security-operations`, 0021/0022 — if Phase 4 merges without a role, the same grants go through the grant list in `Phase2OperationsRepository.UpsertUserAsync`), folder ACLs and backup receipts; Phase 5 Accounting row closed (statuses `DRAFT / BLOCKED / APPROVED_READY / EXPORTED_AWAITING_IMPORT / REJECTED` on `accounting_batches.status`, approval reason persisted, one-transaction mapping approval, `ADJUSTMENT` mappable, export history row with intended company, `TEST`/`PRODUCTION` label, path and SHA-256, one screen Prepare → Review → Export); Phase 6 released.
- Frozen wording for every Phase 7 screen, report and test: the engine never labels anything "posted" or "imported"; "reconciled" appears only for a batch in status `RECONCILED` with a run on record; engineering completion never implies live verification and live verification never implies permission to change production books; never write "fully working", "Tally-compatible", "rollback supported" or "production ready" without scope and evidence. Verification labels: `UNIT_VERIFIED / SQL_VERIFIED / SIMULATED_TRANSPORT_VERIFIED / WINDOWS_VERIFIED / LIVE_TALLY_7_1_VERIFIED / NOT_RUN`; `OWNER_CAPTURE` marks owner screenshots attached as evidence and is never a verification label.
- All new migrations at the next free number; never edit a committed migration (rule 9). THROW numbers: Codex allocates a contiguous block of ten at the lowest 51xxx value above the highest number in the committed migrations at build time (51260 is already used by 0019; the Phase 4 branch uses 51300–51342; today the block would start at 51400) and records the mapping in `PHASE-7-REPORT.md`. This section names them symbolically: `E-KEY-DUP` (invoice already reserved), `E-KIND-CLASH` (day journal and sales vouchers for the same day), `E-TRANSITION` (disallowed status change), `E-IMMUTABLE` (append-only table), `E-SELF-APPROVE`, `E-MAP-OVERLAP`, `E-SEND-UNKNOWN`, `E-PROD-FIRST-DAY`, `E-RELEASE-UNPROVEN`. Column-by-column definitions of every new table live in the "Phase 7 schema" appendix at the end of this section; every table is added to `docs/03_DATABASE_SCHEMA.md` and to `PhaseZeroSqlTests.All_migrations_apply_once_…`.

Tasks:

**Slice 7a — controls, then one invoice in file mode: exported, read back, compared, replay-safe**
1. **Destination profiles (Owner-only).** Migration: `dbo.tally_profiles` and `dbo.tally_profile_stores` (appendix S7-1). A profile holds company name, `environment` (`TEST`/`PRODUCTION`), loopback `endpoint_url`, delivery (`FILE`/`HTTP`), format (`XML`/`JSON`), the policy columns for D13 (`voucher_granularity`), D14 (`party_policy`, `single_party_ledger`), D16 (`tender_model`), D17 (`posting_model`), `voucher_view` (`ACCOUNTING`/`INVOICE`, the view the anchor voucher was keyed in), a posting window, the probed build label (D18), `company_identity_json` (whatever identity fields task 5 verifies), `is_enabled`, `production_enabled_utc/by`. CHECKs: `NOT (party_policy='NAMED_LEDGERS' AND tender_model='IN_VOUCHER')` (save error "Named customer ledgers need a clearing ledger per tender mode"); `production_enabled_utc IS NULL OR production_enabled_by IS NOT NULL`. One store binds to at most one TEST and one PRODUCTION company (UNIQUE `store_code, environment`; D12). `endpoint_url`, when set, must be `http://127.0.0.1:<port>/` or `http://localhost:<port>/`; anything else is rejected by `TallyProfileRepository.SaveAsync` (Infrastructure.SqlServer, `RequireOwnerAsync`, audit `TallyProfileChange` through `dbo.record_operational_audit`) and `DesktopFriendlyError` shows "Only this PC's Tally can be used". Contract `Application/Accounting/TallyTransferContracts.cs`: `TallyProfile` record, `ITallyProfileService.LoadAsync / SaveAsync(profile, reason)`; service `SqlServerTallyProfileService`. Screen: Settings → "Tally" — one destination `"TallySettings"`, module `settings`, task id `tally-settings`, role 3, view `Modules/Accounting/TallySettingsView.xaml(.cs)` with tabs "Tally companies" (this task), "Ledger mappings" (task 13) and "Tally masters" (task 14). Wiring per the codebase convention: `ShellRouteRegistry["TallySettings"] = Route("TallySettings","settings",…)`; `WorkspaceModuleOwnershipRegistry.Destination("TallySettings","settings")`; `UiNavigation` settings module `Item("tally-settings","Tally","TallySettings", minimumRole: AccessRole.Owner)`; `TaskNavigator.ResolveTaskView` branch for `TallySettings`; `MainWindow.xaml` `<ContentControl x:Name="TallySettingsHost"/>`; `TallySettingsPresentationSession(Func<string, ITallyProfileService>, …)` in `Modules/Accounting/`; composed in `DesktopCompositionRoot.CreateMainWindow()`. Fields in plain words ("Tally company name", "Test books or live books", "Stores covered", "Where Tally answers on this PC"); Save with reason. The Owner may create and edit a PRODUCTION row, but `production_enabled_utc/by` is written only by "Enable live books" (task 26). Tests: `TallyProfileServiceBoundaryTests` (SQL-login string throws; Store Manager save throws "Owner permission is required."); SQL `TallyProfileSqlTests` (remote endpoint rejected; second TEST profile for the same store fails; `NAMED_LEDGERS` + `IN_VOUCHER` fails the CHECK); `TaskNavigationTests`, `UiNavigationTests`, `WorkspaceModuleOwnershipRegistryTests`, `ShellNavigationServiceTests`, `HelpCentreTests` updated.
2. **Voucher level, business key, reservations and grants.** Same migration (appendix S7-2): `dbo.accounting_batches` gains `tally_profile_id`, `batch_kind` (`DAY_JOURNAL` default for existing rows / `SALES_VOUCHERS`), `selection_json`, `selection_version`, `mapping_version_set_json`, `manifest_sha256`; new `dbo.accounting_vouchers` (one row per Tally voucher: `component_role` `SALES`/`CREDIT_NOTE`/`RECEIPT`, `voucher_type`, identity `store_code`, `invoice_year`, `document_number nvarchar(80)` — the same type as `sales_invoices` — `revision int NOT NULL DEFAULT 1`, `voucher_date`, `expected_total`, `correspondence_key nvarchar(200)` = `ETP:{store}:{invoice_year}:{document_number}:{component_role}:{revision}` enforced by `CK_accounting_vouchers_key CHECK (correspondence_key = CONCAT('ETP:',store_code,':',invoice_year,':',document_number,':',component_role,':',revision))`, `source_sha256`, `plan_sha256`, `voucher_status` in `PLANNED / BLOCKED / EXCLUDED / EXPORTED / SUBMITTED / OUTCOME_UNKNOWN / ACTUAL_LOCATED / RECONCILED / RECONCILED_WITH_WARNINGS / DIFFERENCE / CANCELLED`, `blocked_reason`); `dbo.accounting_entries` gains `accounting_voucher_id NULL` (day-journal rows stay NULL), `tax_rate`, `quantity`, `stock_item`; new `dbo.accounting_voucher_reservations` with filtered UNIQUE `UX_accounting_voucher_reservations_active (tally_profile_id, store_code, invoice_year, document_number, component_role, revision) WHERE released_utc IS NULL`. A `document_number` containing `:` or `|` is `BLOCKED KEY_UNSAFE`. Rules coded in `ProductisationRepository.SaveAccountingBatchAsync` (extended for `SALES_VOUCHERS`): batch, vouchers, entries, reservations and the first history row (task 3) insert in one transaction; a live reservation (2601/2627) is re-thrown as `E-KEY-DUP` "Invoice {n} is already in batch {id} ({status}); reject that batch first"; a `SALES_VOUCHERS` batch for a store/date is refused while a non-`REJECTED`/`CANCELLED` `DAY_JOURNAL` batch exists for the same store/date and vice versa (`E-KIND-CLASH`, shown as "Invoices for this day are already being sent as vouchers") so revenue can never leave through two batch kinds; batch id, file hash, mapping version and workbook row never enter any key. A reservation row is inserted only for vouchers whose `voucher_status` is `PLANNED`. A reservation is released (1) when its batch reaches `REJECTED` or `CANCELLED` and the batch has no `tally_attempts` row (task 4); (2) when a voucher becomes `BLOCKED` or `EXCLUDED` before any attempt exists for its batch — released in the same statement, `release_reason` = the `blocked_reason` or exclusion reason; (3) by the Owner action "Release invoice" on a `CANCELLED` batch that has an attempt, allowed only when a company-bound read-back acquired after the cancellation (`tally_readbacks.acquired_utc > accounting_status_history.changed_utc`, `is_complete=1`) contains no actual voucher with the key, `release_reason` = "Absence proven by read-back {id}", otherwise `E-RELEASE-UNPROVEN`. A reservation is never released while any actual voucher carries its key (a changed invoice goes through the 7c revision path, task 20). `UQ_accounting_batches_generation` stays. Grants, in the same migration, to the Phase 4 role `etp_store_manager`: `SELECT, INSERT` on `accounting_batches, accounting_entries, accounting_vouchers, accounting_voucher_reservations, accounting_validation_findings, accounting_status_history, tally_artifacts, tally_attempts, tally_readbacks, tally_actual_vouchers, tally_actual_ledger_entries, tally_reconciliation_runs, tally_reconciliation_differences, tally_master_snapshots, tally_masters`; `EXECUTE` on `dbo.usp_accounting_batch_transition`; `DENY UPDATE, DELETE` on all of them to `etp_store_manager` and `etp_viewer`; `SELECT` only on `tally_profiles`, `tally_profile_stores`, `accounting_mappings`, `tally_master_creation_plans`, `tally_backup_evidence`. Tasks 4, 7, 9, 10 and 14 refer to this list instead of repeating grants. Tests: SQL `AccountingVoucherSqlTests`: second save with the same invoice → `E-KEY-DUP`; after Reject with no attempt the save succeeds; `DAY_JOURNAL` after `SALES_VOUCHERS` → `E-KIND-CLASH`; existing `DAY_JOURNAL` saves still work with NULL voucher ids; a BC document number of the 3xxxxxxxx series and an 80-character number round-trip unchanged; a batch blocked by `NOT_IN_SCOPE_7A` leaves zero active reservations for its blocked vouchers and the same invoices save in a later `SALES_VOUCHERS` batch; SQL `TallyGrantsSqlTests` (`EXECUTE AS USER` = a member of `etp_store_manager`: INSERT into `tally_readbacks` succeeds, UPDATE fails, INSERT into `tally_profiles` fails).
3. **Status model, history and audit types.** Same migration: map legacy values first (`REVIEW→DRAFT`, `APPROVED→APPROVED_READY`, `EXPORTED→EXPORTED_AWAITING_IMPORT`; THROW if any other unknown value remains), then drop and re-add `CK_accounting_batches_status` with `'DRAFT','BLOCKED','APPROVED_READY','EXPORTED_AWAITING_IMPORT','SUBMITTED_AWAITING_RESULT','OUTCOME_UNKNOWN','PARTIALLY_APPLIED','IMPORT_REPORTED_AWAITING_RECONCILIATION','RECONCILIATION_INCOMPLETE','FAILED_RECONCILIATION','RECONCILED','RECONCILED_WITH_ACCEPTED_WARNINGS','RE_AUDIT_REQUIRED','REJECTED','CANCELLED'`. New `dbo.accounting_status_history` (appendix S7-3; `AFTER UPDATE,DELETE` trigger `trg_accounting_status_history_immutable` → `E-IMMUTABLE`). `SaveAccountingBatchAsync` writes the first history row (`from_status NULL`, `to_status 'DRAFT'`) inside its own transaction; every later write to `accounting_batches.status` goes through `dbo.usp_accounting_batch_transition(@batch_id, @to_status, @reason, @evidence_artifact_id)`, which validates the transition, then in one transaction updates the batch, inserts the history row and calls `dbo.record_operational_audit('AccountingBatch', …)`; a disallowed transition → `E-TRANSITION`. Allowed transitions: `DRAFT|BLOCKED → BLOCKED|APPROVED_READY|REJECTED|CANCELLED` (`APPROVED_READY` only from a validation with no FAIL and every WARN waived); `BLOCKED → DRAFT` on a re-validation with no FAIL; `APPROVED_READY → EXPORTED_AWAITING_IMPORT|SUBMITTED_AWAITING_RESULT|BLOCKED|REJECTED|DRAFT` (`DRAFT` only through an exclusion, task 17b; `DRAFT` is accepted from no other state); `EXPORTED_AWAITING_IMPORT → IMPORT_REPORTED_AWAITING_RECONCILIATION|CANCELLED`; `SUBMITTED_AWAITING_RESULT → IMPORT_REPORTED_AWAITING_RECONCILIATION|OUTCOME_UNKNOWN|PARTIALLY_APPLIED|APPROVED_READY` (the last only for `TRANSPORT_FAILED` with nothing sent, task 23); `OUTCOME_UNKNOWN → IMPORT_REPORTED_AWAITING_RECONCILIATION|PARTIALLY_APPLIED|RECONCILIATION_INCOMPLETE`; `IMPORT_REPORTED_AWAITING_RECONCILIATION|PARTIALLY_APPLIED|RECONCILIATION_INCOMPLETE → RECONCILED|RECONCILED_WITH_ACCEPTED_WARNINGS|FAILED_RECONCILIATION|RECONCILIATION_INCOMPLETE|PARTIALLY_APPLIED|OUTCOME_UNKNOWN`; `RECONCILED|RECONCILED_WITH_ACCEPTED_WARNINGS → RE_AUDIT_REQUIRED` (set automatically by every Compare on an already reconciled batch, history reason "Compared again", before the new outcome is applied); `RE_AUDIT_REQUIRED|FAILED_RECONCILIATION|RECONCILED_WITH_ACCEPTED_WARNINGS → RECONCILED|RECONCILED_WITH_ACCEPTED_WARNINGS|FAILED_RECONCILIATION|RECONCILIATION_INCOMPLETE`. Batch outcome after a read-back or run is derived from voucher statuses by pure `AccountingBatchStatusRules.Derive(IReadOnlyList<VoucherState> vouchers, bool hasHttpAttempt)` in `TallyTransferContracts.cs`, evaluated over vouchers not `EXCLUDED`/`CANCELLED` in this order: (1) any `OUTCOME_UNKNOWN` → `OUTCOME_UNKNOWN`; (2) any voucher still `EXPORTED`/`SUBMITTED` with no read-back covering its date and company, or any `NOT_VERIFIABLE` difference on a located voucher → `RECONCILIATION_INCOMPLETE`; (3) `hasHttpAttempt` and at least one `ACTUAL_LOCATED`/`RECONCILED*` voucher and at least one `DIFFERENCE` voucher whose only rows are `MISSING` with a per-object rejection recorded in `tally_attempts.tally_diagnostics_json` → `PARTIALLY_APPLIED`; (4) any other `DIFFERENCE` → `FAILED_RECONCILIATION`; (5) all `RECONCILED` → `RECONCILED`; (6) all `RECONCILED` or `RECONCILED_WITH_WARNINGS` and every warning row has `accepted_by` → `RECONCILED_WITH_ACCEPTED_WARNINGS`; (7) no voucher left → `CANCELLED`; anything else → `RECONCILIATION_INCOMPLETE`. `PLANNED` and `BLOCKED` vouchers never reach Derive (Save and Validate set `DRAFT`/`BLOCKED` directly); operator actions (Approve, Write file, Send, "I imported", Reject, Cancel) pass explicit targets. No HTTP status, Tally acknowledgement or import count ever sets a status; `IMPORT_REPORTED_AWAITING_RECONCILIATION` means "claimed, not proven". `CREATE OR ALTER TRIGGER dbo.trg_accounting_entries_approved` and new `dbo.trg_accounting_vouchers_approved` both THROW 51202 when the parent batch `status NOT IN ('DRAFT','BLOCKED')`, except an `UPDATE` touching only `voucher_status`/`blocked_reason` on `accounting_vouchers` (`IF UPDATE(...)` guard). Widen `CK_operational_audit_type` with `'TallyProfileChange','TallyAttempt','TallyReadback','TallyReconciliation'` and `OperationalAuditRepository.EventTypes` (constant `safe_detail`, no ids; written only through `dbo.record_operational_audit`). Widen `CK_approval_requests_type` with `'TALLY_MASTER_CREATION','TALLY_REVISION'` (Phase 5 deleted `MASTER_MAPPING` and `CONTROL_WAIVER`). Tests: unit `AccountingBatchStatusRulesTests` enumerating exactly the seven-step table (every voucher status combination × `hasHttpAttempt`); SQL `AccountingStatusHistorySqlTests` (every transition writes exactly one history row and one audit row; disallowed transition → `E-TRANSITION`; `DRAFT` from `APPROVED_READY` without a reason → `E-TRANSITION`; UPDATE/DELETE on history → `E-IMMUTABLE`; entry edit after approval → 51202; legacy `REVIEW` row is `DRAFT` after migration).
4. **Evidence root, artifacts and attempts.** `product_settings.tally_evidence_root nvarchar(400)` (default `C:\ProgramData\EtpReporting\TallyEvidence`, Owner-editable via `IAdministrationService.SaveProductConfigurationAsync`; bootstrap ACL = Phase 4 task 2 pattern plus Modify for the Owner login and the shared Store Manager login, no BUILTIN\Users). Folder per batch `<root>\<profile_code>\<store_code>\<yyyy-MM>\batch-<accounting_batch_id>\`. `TallyEvidencePaths.Validate(relativePath)` (`Infrastructure.SqlServer/Tally/`) accepts only segments matching `^[A-Z0-9][A-Z0-9_-]{0,29}$` for profile and store, `^\d{4}-\d{2}$` for the month, `^batch-\d+$`, the fixed folder names `attempts|actuals|reconciliation` and file names from the fixed list in task 21; anything else throws `ArgumentException` — no customer name or phone can ever form a path. New `dbo.tally_artifacts` (appendix S7-4; UNIQUE `(accounting_batch_id, relative_path)`, non-unique `IX_tally_artifacts_hash (sha256)` for `VerifyAsync` lookups — equal hashes across batches are routine and allowed; immutable trigger → `E-IMMUTABLE`) and `dbo.tally_attempts` (appendix S7-4; append-only). Files are written once (`FileMode.CreateNew`), hashed with `SHA256.HashDataAsync` lowercase hex, registered in `tally_artifacts` in the same call (`TallyEvidenceStore.WriteAsync` in `Infrastructure.SqlServer/Tally/TallyEvidenceStore.cs`); empty files are never created; a file-mode batch has no `attempts\` folder. The full `manifest.json` package is task 21. Grants per task 2. Tests: unit `TallyEvidenceStoreTests` (temp folder): second write of the same path throws; two batches registering byte-identical `validation.json` both succeed; `TallyEvidencePaths` rejects a person-name segment (synthetic, e.g. `Ramesh Kumar`), a ten-digit segment and `..`; SQL: UPDATE on `tally_artifacts` → `E-IMMUTABLE`.
5. **Read-only probe of the installed build (owner-run with Codex, before tasks 8–10 rely on it).** With the anchor Sales voucher and read-only requests to the configured port, record in `PHASE-7-REPORT.md` section "Compatibility and protocol": Windows build, app version and commit, SQL version, exact TallyPrime build string (D18), port and setting names, the view tags observed (`OBJVIEW`, `PERSISTEDVIEW`, `ISINVOICE`) and which ledger list the view uses, which tags beyond those the Journal exporter already writes (`DATE`, `NARRATION`, `ALLLEDGERENTRIES.LIST` / `LEDGERNAME` / `ISDEEMEDPOSITIVE` / `AMOUNT`) a Sales voucher import needs (party ledger, `VOUCHERTYPENAME`, voucher number, reference, effective date, GST or inventory lists — as the anchor voucher shows them), whether `VOUCHERNUMBER` is kept or replaced after import (numbering method, D18), which identifier tags the export returns (candidates only; verify), the response encoding and BOM, what a rejected import response looks like, how a not-open company is reported, whether the HTTP Export response envelope echoes `SVCURRENTCOMPANY` in `REQUESTDESC` and whether a manual Day Book export does the same, whether `NARRATION` and any reference tag come back unchanged in the Day Book export, whether cancelled vouchers are returned (candidate tags `ISCANCELLED`, `ISOPTIONAL`, `ISPOSTDATED`), which read request returns full voucher objects (Export Data with `REPORTNAME` Day Book and `SVFROMDATE`/`SVTODATE`/`SVEXPORTFORMAT`, or a Vouchers collection export), how the list of open companies is requested, and whether a request that omits the company routes to the open company. Default format XML, default delivery FILE, JSON `NOT_RUN`. If the machine is unavailable the section says so and tasks 8–12 are built against the anchor voucher only.
6. **Invoice source and voucher composition.** Extend `AccountingContracts.cs`: `AccountingVoucherDraft(int VoucherSequence, string ComponentRole, string VoucherType, string StoreCode, int InvoiceYear, string DocumentNumber, int Revision, long? SalesInvoiceId, DateOnly VoucherDate, decimal ExpectedTotal, string CorrespondenceKey, string SourceSha256, string PlanSha256, string VoucherStatus, string? BlockedReason, IReadOnlyList<AccountingEntry> Entries)`; `AccountingBatchDraft` gains `string BatchKind` and `IReadOnlyList<AccountingVoucherDraft> Vouchers` (empty for day journals); `AccountingEntry` gains `int? VoucherSequence, decimal? TaxRate, decimal? Quantity, string? StockItem`; `SaveAccountingBatch` gains `int? TallyProfileId` and `AccountingSelection Selection` (`IReadOnlyList<InvoiceKey>? Keys`, null = all invoices of the day; `InvoiceKey(string StoreCode, int InvoiceYear, string DocumentNumber)`); `AccountingBatchControls.IsBalancedAndComplete` also checks that every non-blocked voucher balances individually. `InvoiceAccountingSource` record plus a twin in `ProductisationModels.cs` and a `Map` overload (as for `AccountingBatchDraft`); `ProductisationRepository.LoadInvoiceAccountingSourceAsync(store, businessDate, selection)` requires the latest `daily_report_generations.is_final=1` (THROW 51220 as today) and returns per invoice: identity; invoice date = business date = `sales_invoices.transaction_date` (7a); party per D14 (`SINGLE_LEDGER` → `single_party_ledger`; `NAMED_LEDGERS` → `etp_r024.customer_name` joined on (`store_code`, `inv_number = document_number`, FY-end year from `inv_date` with April as month 1), never `customer_phone`); lines (`line_identifier`, `product_code`, `source_gross_amount`, `source_net_amount`, `source_tax_amount`, `source_quantity`, `source_transaction_type`); tax components per line from `dbo.[etp_r018] r` joined on `r.store_code = i.store_code`, `r.invoice_year = i.invoice_year`, `r.doc_invoice_no = i.document_number`, `r.item_number = l.product_code`, `r.transaction_type = 'INV'` (each `nvarchar(max)` side `CAST` to `nvarchar(80)` with `COLLATE DATABASE_DEFAULT`); tender rows from `reporting_sales_tenders` joined to `dbo.tender_modes` on `tender_type = source_tender_code` (`active=1`; a `tender_type` without a row → `BLOCKED TENDER_MODE_UNKNOWN`). `InvoiceAccountingSource` never contains `etp_r018.customernumber`, `customer_phone` or address columns; the customer name is present only when `party_policy='NAMED_LEDGERS'`. New pure `TallySalesVoucherComposer` (`Infrastructure.SqlServer/Tally/TallySalesVoucherComposer.cs`, reusing the two-line and sign-swap pattern of `AccountingBatchComposer`) called by `SqlServerAccountingService.PreviewAsync` when `batch_kind='SALES_VOUCHERS'`, mapping lookup through the existing `LoadApprovedAccountingMappingsAsync` with business events: `PARTY` (`CLEARING_LEDGER` only); `TENDER_<MODE>` where `<MODE>` = `tender_modes.mode` upper-cased with spaces replaced by underscores (`TENDER_CASH`, `TENDER_UPI`, `TENDER_SERVICE_UPI`; one mapping per mode, every `source_tender_code` of a mode shares it); `ROUND_OFF` for tender rows whose `source_tender_code` is `ROUND_OFF` (debit when positive, credit when negative, `Abs` amount — never composed as Cash and never counted as a tender; the composer never adds a balancing line of its own); `SALES_REVENUE` for `source_net_amount`; `OUTPUT_CGST_<rate>`, `OUTPUT_SGST_<rate>`, `OUTPUT_IGST_<rate>` (rate = the `etp_r018` component rate at scale 2, e.g. `OUTPUT_CGST_9`; amount = the component amount; the rate is read from the event code, no `tax_attributes_json` needed in 7a; D15 records the ledger per component and rate). Rows with `source_tender_code IN ('CASH_REFUND','CHEQUE_RTGS_REFUND','NO_REFUND','ISSUED_CREDITNOTE')` → `BLOCKED NOT_IN_SCOPE_7A` (refund tenders, 7b). `IN_VOUCHER`: the debit line (`PARTYLEDGERNAME` in invoice view, the debit `ALLLEDGERENTRIES` line in accounting view) is the tender ledger of the invoice's mode, as the anchor cash sale shows; the D3 customer name goes only into narration and, if D14 wants it, the buyer-details tag the anchor shows (`BASICBUYERNAME` — verify on the installed build), never into a ledger name. Tax is never derived: no `etp_r018` row for a line → `BLOCKED TAX_ROW_MISSING`; component amounts not summing to `source_tax_amount` to the paisa → `BLOCKED TAX_SPLIT_MISMATCH`; `cess_amount > 0` → `BLOCKED CESS_NOT_SUPPORTED` (7a; an `OUTPUT_CESS_<rate>` event is added in 7b only if the accountant needs it); a missing tax amount is never zero. 7a composes only `INV` documents with exactly one non-round-off tender and one rate set; anything else is `BLOCKED NOT_IN_SCOPE_7A` naming the reason (return, split tender, multi-rate) and stays visible in the selection. `decimal` at scale 4, money compared at scale 2, quantity at scale 3, `MidpointRounding.AwayFromZero`. Canonical JSON for hashes = `System.Text.Json`, properties in ordinal key order, decimals as strings at scale 4 (`ToString("F4", InvariantCulture)`), dates `yyyy-MM-dd`, UTF-8 without BOM, lines ordered by `sales_lines.line_identifier`; `source_sha256` over the invoice facts, `plan_sha256` over the planned entries. Tests: unit `TallySalesVoucherComposerTests` from `tests-dotnet/fixtures/tally-golden/G01-ordinary-sale/` (`source.json` with component amounts, `expected-postings.json` written by hand and reviewed by the accountant) and `G05-round-off/` (CASH + `ROUND_OFF` 0.40): balanced; tender/revenue/CGST/SGST/round-off equal the fixture; each blocked case; SQL `TallyInvoiceSourceSqlTests` on the sanitised sample (a day with n INV invoices yields n vouchers; gross sum equals the day's GST-inclusive DSR value; the serialised snapshot contains no digit run of 10 or more).
7. **Validation findings and the gate.** New `dbo.accounting_validation_findings` (appendix S7-5). Pure rules in `Application/Accounting/AccountingValidationRules.cs`; `AccountingValidationService` in `Infrastructure.SqlServer/Tally/` calls them and writes the findings at Save, Approve and again immediately before Write file / Send: `RULE-SRC-001` day has a final generation; `RULE-SRC-002` transaction type supported (INV; SR/BC from task 15); `RULE-SRC-003` 7a scope; `RULE-SRC-004` `source_sha256` still equals a fresh recompute (else `SOURCE_CHANGED`); `RULE-MAP-001` every event mapped by an approved mapping effective on the voucher date; `RULE-MAP-002` tender mode known and mapped; `RULE-MAP-003` `mapping_version_set_json` unchanged since approval; `RULE-AMT-001` debits = credits per voucher (ledger entries plus accounting allocations, task 17a); `RULE-AMT-002` gross = net + tax per line and per voucher to the paisa; `RULE-AMT-003` sum of tender lines plus round-off = gross to the paisa (FAIL when `IN_VOUCHER`); `RULE-AMT-004` WARN when invoice gross exceeds ₹5,00,000 (constant in `AccountingValidationRules`); `RULE-TAX-001` component amounts observed for every line and summing to `source_tax_amount`; `RULE-KEY-001` key reservable, `KEY_UNSAFE` clear, not already sent; `RULE-ENV-001` profile enabled, company non-empty, `PRODUCTION` only with `production_enabled_utc` set; `RULE-ENV-002` `posting_model='INVENTORY'` with `IN_VOUCHER` and more than one tender is not composable (task 16); `RULE-DAT-001` voucher date inside the profile's posting window and not after today (the reporting-day lock is untouched: a `LOCKED` day is allowed, a missing generation is not); `RULE-DAT-002` WARN when the voucher date is more than 30 days before today; `RULE-PAY-001` re-parsed payload equals plan and carries no Tally-assigned tag (task 8). Any FAIL → batch `BLOCKED`, vouchers `BLOCKED` with `blocked_reason` (reservations released per task 2 rule 2); a WARN needs the Owner's "Accept with reason" (writes `waived_*`) before `APPROVED_READY`; a batch with any blocked voucher is blocked as a whole until task 17b's exclusions. Grants per task 2. Tests: unit per rule (`AccountingValidationRulesTests`, including `Production_profile_without_enable_fails` and a WARN that blocks `APPROVED_READY` until `waived_by` is set); SQL: a blocked batch cannot be approved (`E-TRANSITION`).
8. **Sales voucher XML export.** Extend `TallyXmlExportService` (`AccountingAndSharingServices.cs`) with `ExportSalesVouchersAsync(path, profile, batch, vouchers, ct)`: one `<VOUCHER VCHTYPE="{voucher_type}" ACTION="Create">` per `accounting_vouchers` row inside the existing envelope; the list tag and view tags exactly as the anchor for the profile's `posting_model`/`voucher_view` (`ALLLEDGERENTRIES.LIST` for `ACCOUNTING`, `LEDGERENTRIES.LIST` + `ALLINVENTORYENTRIES.LIST` for `INVOICE`); `SVCURRENTCOMPANY` always from the profile (in file mode this tag is informational — the UI import goes into whichever company the operator has open; the read-back company check is the only proof); `<NARRATION>` = `{correspondence_key} | ETP {store} invoice {document_number} dated {dd-MMM-yyyy}`; the reference tag carries the invoice number only when task 5 confirmed it imports; `VOUCHERNUMBER` is written as the ETP `document_number` only when D18 says the voucher type is Manual, otherwise omitted so correspondence relies on `NARRATION`/`REFERENCE`. Never write `REMOTEID`, `VCHKEY`, `GUID`, `MASTERID`, `ALTERID`, `VOUCHERKEY`, `OLDAUDITENTRIES.LIST`, `ACCOUNTAUDITENTRIES.LIST` or any tag whose value Tally assigns; the tags copied from the anchor are limited to an allow-list recorded in `PHASE-7-REPORT.md` "voucher tags used" (view tags, `VOUCHERTYPENAME`, `PARTYLEDGERNAME`, `DATE`, `EFFECTIVEDATE`, `NARRATION`, `REFERENCE`, ledger/inventory lists), each marked verified on the installed build or not. `XmlWriter`, `UTF8Encoding(false)`, `.tmp` + `File.Move` as today; file written into the batch's evidence folder as `payload.xml` (task 4; the Save-as dialog offers a copy only). The file is then re-parsed independently by `TallyPayloadReader` (`Infrastructure.SqlServer/Tally`, `XmlReaderSettings { DtdProcessing = Prohibit, XmlResolver = null, MaxCharactersInDocument = 50_000_000 }`, over-size = failure never truncation) and compared with the plan (`RULE-PAY-001`, which also fails on any forbidden tag); a mismatch deletes nothing, registers the file, sets the batch `BLOCKED` `PAYLOAD_MISMATCH`. Pre-export revalidation (task 7) plus profile, company and environment unchanged; any failure returns `APPROVED_READY` to `BLOCKED` with the reason. `RecordAccountingExportAsync` records, in one transaction after the file exists, the `PAYLOAD_XML` artifact, a `tally_attempts` row (`FILE`, `FILE_WRITTEN`), the Phase 5 export-history row extended with `tally_profile_id`, `plan_sha256` and the artifact id, and the transition to `EXPORTED_AWAITING_IMPORT`; the receipt on screen reads "File written for {company} ({TEST/LIVE}). Nothing has been sent to Tally — open the {company} books in Tally before importing, import the file, then press 'I imported this file into Tally'." Tests: `TallySalesVoucherXmlTests` assert the element tree against `G01/expected-payload.xml` with `XDocument` (element by element, not string equality), one VOUCHER per voucher, correspondence key present, debits negative, none of the forbidden tags present, Unicode ledger name (₹, Devanagari) survives, hash equals file bytes, a DTD in a read file is rejected; existing `Tally_export_rejects_unbalanced_batch_and_emits_controlled_xml` kept.
9. **Read-back gateway.** Port `ITallyReadbackGateway` in `TallyTransferContracts.cs` (`FetchVouchersAsync(TallyReadbackRequest(CompanyName, FromDate, ToDate), ct) → TallyReadbackResult`; `FetchOpenCompaniesAsync(ct)`; Application never references `System.Net.Http`). New project `src/Etp.Reporting.Infrastructure.Http` (`net10.0`, references Application only, added to `Etp.Reporting.slnx`) with `HttpTallyReadbackGateway(Uri endpoint, TimeSpan timeout)` posting the read-only request task 5 verified (Export Data with `REPORTNAME` Day Book, `SVFROMDATE`/`SVTODATE`, `SVEXPORTFORMAT` `$$SysName:XML`, or a Vouchers collection export — whichever returns full voucher objects with `NARRATION`), naming the company; 30 s timeout; read-only requests may retry twice on transport failure; `AllowAutoRedirect = false` (3xx = failure); responses over 50 MB fail; HTML or non-XML → "Tally answered, but not with voucher data". Fallback `FileTallyReadbackGateway` in `Infrastructure.SqlServer/Tally/` (no HTTP, so the SQL integration tests reach it without the Http project) reads a Day Book XML the operator exported from Tally by hand ("Load a file exported from Tally"). Desktop never calls HTTP directly; composed in `DesktopCompositionRoot`. Migration: `dbo.tally_readbacks`, `dbo.tally_actual_vouchers`, `dbo.tally_actual_ledger_entries` (appendix S7-6; all append-only → `E-IMMUTABLE`). `TallyReadbackParser` (same hardened reader as task 8) stores raw XML as a `READBACK_XML` artifact. `company_name_reported` = the `SVCURRENTCOMPANY` value found in the response envelope's `REQUESTDESC` (HTTP and manual file alike); when absent → `is_complete=0`, `incomplete_reason 'COMPANY_NOT_REPORTED'`, every voucher check `NOT_VERIFIABLE`, screen "Tally did not say which company answered" — the engine never fills the field from the request; `company_name_reported` ≠ profile company → `is_complete=0`, reason `WRONG_COMPANY`, nothing matched; an error response instead of voucher data (company not open, unknown request) → `is_complete=0`, reason `COMPANY_NOT_OPEN` or `TALLY_ERROR`, response stored. Identifier tags are stored when present and treated as `NOT_VERIFIABLE` until task 5 confirms them stable. Correspondence key regex `\bETP:[A-Z0-9_-]{1,30}:\d{4}:[^:|\s]{1,80}:(SALES|CREDIT_NOTE|RECEIPT):\d+\b` applied to `NARRATION` first, then the reference tag; two different keys in one voucher → `AMBIGUOUS_MATCH`. A voucher is `ACTUAL_LOCATED` when exactly one actual voucher in the right company carries its key; two or more → `DUPLICATE`; equal amount or date alone never matches. Quantities and rates arrive as text with unit symbols: `quantity_text`/`rate_text` stored raw, `quantity decimal(19,3)` and `unit` parsed by `TallyQuantityParser` (number then unit symbol, culture-invariant; parse failure → `NOT_VERIFIABLE`, never zero). Role: the CanImport gate (`RequireImportAsync` in `SqlServerAccountingService`, "Owner or Store Manager permission is required."). Grants per task 2. Tests: `HttpTallyReadbackGatewayTests` in new `tests-dotnet/Etp.Reporting.Infrastructure.Http.Tests` with a stub `HttpMessageHandler` (success, redirect refused, HTML refused, timeout retried twice then failed, over-size refused, company-not-open error response classified) — `SIMULATED_TRANSPORT_VERIFIED`; `TallyReadbackParserTests` on `G01/actual-readback.xml` (key regex round-trips an 80-character document number; `1 Nos` parses to 1.000 / `Nos`); SQL `TallyReadbackSqlTests` (wrong company and missing company stored incomplete with the right reason; rows immutable).
10. **Three-way comparison of one invoice.** Pure `TallyReconciliationEngine.Run(ReconciliationInput a, ParsedPayload b, ReadbackSnapshot c, ToleranceSet t) → TallyReconciliationResult(Outcome, Differences)` in `Application/Accounting/TallyReconciliation.cs` (no I/O; A = source snapshot + expected postings, B = re-parsed `payload.xml`, C = actual vouchers). `TallyReconciliationRepository` in `Infrastructure.SqlServer/Tally/` persists `dbo.tally_reconciliation_runs` and `dbo.tally_reconciliation_differences` (appendix S7-7; `rule_set_version '7a.1'`; both append-only) and writes the `RECONCILIATION` artifact via `TallyEvidenceStore`; `SqlServerAccountingService.CompareAsync(batchId)` orchestrates load (A from `accounting_vouchers`/`accounting_entries`, B via `TallyPayloadReader`, C from `tally_actual_*`) → `Run` → persist → transition: on a batch in `RECONCILED*` it first transitions to `RE_AUDIT_REQUIRED` (history reason "Compared again"), then applies the run outcome; earlier run rows are untouched. 7a checks: coverage (every selected voucher is planned, excluded or blocked), header (company, voucher type, date, key; voucher number only when D18 says Manual, else `NOT_VERIFIABLE`), accounting (per ledger amount A = B = C at scale 2, exact), tax (per component ledger), tender, integrity (artifact hashes unchanged; `source_sha256` unchanged); identifiers compare exactly; no percentage tolerance. Zero matches → `MISSING`; more than one → `DUPLICATE` (no auto-pick); reported company ≠ profile → `WRONG_COMPANY` on every voucher; a field Tally did not return → `NOT_VERIFIABLE`, never zero. Voucher `RECONCILED` only when A = B = C on every check with the actual voucher located; else `DIFFERENCE` with one row per failed check; batch status per task 3. `required_action` text comes from a fixed table (e.g. `MISSING` → "Voucher not found in Tally. Do not write the file again; check Tally's import log, then Read back again"). Grants per task 2. Tests: unit `TallyReconciliationEngineTests` from G01 and fixtures `readback-match.xml`, `-missing.xml`, `-duplicate.xml`, `-wrong-company.xml`, `-amount-off-by-one-paisa.xml`, `-ledger-renamed.xml`, `-field-absent.xml`; SQL: runs and differences persist and cannot be updated; a second Compare on a `RECONCILED` batch writes two history rows and leaves the first run byte-identical.
11. **One screen, five steps.** Extend `AccountingWorkspaceView` and `AccountingPresentationSession` (the Phase 5 single screen; no new shell): a "Tally company" selector listing every profile with a badge `TEST` (grey), `LIVE` (red, bold) or `LIVE (not enabled)` (red outline; Prepare then produces the `RULE-ENV-001` FAIL finding); steps "1 Prepare → 2 Review and approve → 3 Write Tally file → 4 Check what Tally recorded → 5 Differences"; selection "All invoices of the day" or one chosen invoice (number, GST-inclusive value); buttons "Prepare", "Validate", "Save" (CanImport), "Approve" with reason (Owner), "Write Tally file" (Owner), "I imported this file into Tally" (CanImport; reason = Tally's own import message typed or pasted), "Read back from Tally", "Load a file exported from Tally", "Compare", "Open evidence folder" (CanImport); the Phase 5 mapping row is replaced by a link "Change ledger mappings" to Settings → Tally (task 13). Always-visible header strip: company, TEST/LIVE, voucher date, source invoices, expected vouchers, actual vouchers found, unresolved — `—` for not tested, `0` only for a real zero, "missing" in amber. Voucher grid (invoice, date, gross, status, reason), findings grid (severity, plain sentence, what to do), differences grid (type, level, A, B, C, delta, what to do). Every disabled button shows its reason inline; enablement re-evaluated on every refresh and role change (fixes audit 05:226). Replace the positional `body`/`actions` index arrays for `Accounting` in `TaskNavigator.ResolveTaskView` with named regions (`x:Name`d panels resolved by `FocusedTaskLayout.Show(view, …, namedParts)`) or extend the mechanism Phase 5 introduced. Session gains `PrepareInvoicesAsync`, `ValidateAsync`, `ReportImportedAsync`, `ReadBackAsync`, `CompareAsync`. `HelpCentre` topic "Sending sales to Tally" rewritten to these steps; the "tax treatment" and "Tally import exceptions" promises are delivered or removed. Tests: `ProductisationPresentationSessionTests` (button state and reason per status and role); `ExtractedWorkspaceUiSmokeTests` asserts every named region is present.
12. **Golden G01, replay G10 and the report section.** SQL integration `TallyGoldenFixtureTests` seeds a TEST profile `profile_code='GOLDEN'`, `company_name='TEST - ETP Golden'`; every `tests-dotnet/fixtures/tally-golden/*/actual-readback*.xml` reports that company except `readback-wrong-company.xml` (`TEST - Other`). `G01_ordinary_sale_reconciles_end_to_end` (seed fixture → prepare → approve → write file → `FileTallyReadbackGateway` on `actual-readback.xml` → `RECONCILED`) and `G10_same_invoice_in_second_batch_creates_nothing` (second save → `E-KEY-DUP`; a re-exported workbook with shifted rows changes nothing because the business key, not the file, is compared; after the operator imports the same file twice by hand the read-back shows two vouchers and the batch becomes `RE_AUDIT_REQUIRED` then `FAILED_RECONCILIATION` with `DUPLICATE`, never a silent second voucher). `PHASE-7-REPORT.md` "Compatibility and protocol" (task 5) plus the format × delivery matrix (7a: XML file mode; HTTP read-only) and the one live round trip labelled `LIVE_TALLY_7_1_VERIFIED` with the Tally screen capture naming company and build, or `NOT_RUN` with the reason.

**Slice 7b — mappings, masters, returns, tenders, multi-line, golden set**
13. **Versioned mapping administration.** Settings → Tally → "Ledger mappings" tab: grid of `accounting_mappings` versions (event, store, debit, credit, effective from/to, version, approved by, active). Delete the mapping row and `ApproveMappingAsync` from `AccountingWorkspaceView`/`IAccountingService`; the only write paths are `ITallyMappingService.ProposeAsync(ProposeAccountingMapping(Scope, BusinessEvent, StoreCode?, DebitLedger, CreditLedger, NarrationTemplate, TaxAttributesJson?, EffectiveFrom, Reason))` (CanImport) → `dbo.submit_approval_request` type `ACCOUNTING_MAPPING`, and `DecideAsync(approvalRequestId, approve, reason)` (Owner) → `dbo.decide_approval_request` followed by `SaveAccountingMappingAsync` in the same transaction, inside which `requested_by <> ORIGINAL_LOGIN()` is enforced (`E-SELF-APPROVE` "Someone else must approve this mapping"; under D7 proposals come from the shared Store Manager login and approvals from the Owner login). Overlapping effective windows for the same (event, store) → `E-MAP-OVERLAP`, and `effective_to` is set on the superseded version; global (`store_code NULL`) mappings allowed from the UI; `ApprovedAccountingMapping` gains `string? TaxAttributesJson` (added to the SELECT in `LoadApprovedAccountingMappingsAsync`) holding `{ "hsn": "…" }` where D15 needs HSN. Any new mapping version for an event used by a batch at or beyond `APPROVED_READY` and before any attempt returns that batch to `BLOCKED` (`RULE-MAP-003`, history reason "Mapping changed"); batches with an attempt keep their `mapping_version_set_json` untouched. Fuzzy suggestions (`TallyMasterSuggester`, Levenshtein ≤ 2 over read masters) are "Did you mean" only, never saved automatically. Tests: unit overlap and self-approval; SQL `E-SELF-APPROVE`/`E-MAP-OVERLAP`; `Mapping_approval_is_owner_only_and_keeps_request_decision_save_order` rewritten to the two-step flow; failure test "mapping change after approval".
14. **Master read and validation; approved creation.** `ITallyReadbackGateway.FetchMastersAsync(company) → TallyMastersResult(CompanyNameReported, AcquiredUtc, IReadOnlyList<TallyMaster(Kind, Name, Parent, AttributesJson, Guid)>, RawXml)` (read-only list requests for ledgers with parent group, voucher types and, per D17, stock items, units, godowns — request shape verified as in task 5) into `dbo.tally_master_snapshots` and `dbo.tally_masters` (appendix S7-8). Tab "Tally masters": every ledger, voucher type and item a batch needs is `Found` / `Not found` / `Wrong group` against the newest snapshot of the profile's company (expected groups per D15); missing or wrong → `RULE-MST-001` FAIL (also "Customer ledger is not bill-wise" when D14 = `NAMED_LEDGERS` and the anchor shows bill-wise on); snapshot older than 24 h or from another company → `RULE-MST-002` FAIL "Masters list is stale; read masters again". Never create or alter a master silently. Owner action "Plan missing ledgers": `dbo.tally_master_creation_plans` (appendix S7-8; approval type `TALLY_MASTER_CREATION`) exports a create-only masters XML (tags from `anchor-ledger.xml`); the operator imports it with Tally's option "Behaviour of import if master already exists" set to "Ignore duplicates" (label: verify on the installed build; `docs/OPERATIONS.md` shows the screen; the pre-import screen text and `approvals.json` record the required option); a masters read-back afterwards verifies each created master and compares every pre-existing master's `attributes_json` with the snapshot taken before import — any difference → `ACTUAL_CHANGED` exception, never merged. Golden G08 (missing master blocks only the dependent voucher). Tests: unit readiness rules; SQL staleness; pre/post attribute compare.
15. **Returns and cancellations (Credit Note).** `SR` and `BC` documents compose as `component_role='CREDIT_NOTE'`, `voucher_type` from `anchor-credit-note.xml`, ledger roles reversed: party/tender ledger credited, `SALES_REVENUE` and tax ledgers debited, using the absolute source values (SR lines are stored negative, Phase 1 task 11); the XML keeps Tally's convention (debit lines negative with `ISDEEMEDPOSITIVE` Yes). Original invoice = `etp_r025.reference_invoice_number` / `reference_invoice_date` joined to the SR/BC line on `sales_lines.source_lineage_id = etp_r025.source_lineage_id`, falling back to `etp_r024.invrefno` / `invrefdate` joined on (`store_code`, `inv_number = document_number`, `inv_date = transaction_date`); the original's FY-end year is derived from the reference date (April = month 1); if Phase 1 has since persisted `reference_invoice_number` on `sales_invoices`, use that column and say so in `PHASE-7-REPORT.md`. `REFERENCE` = the original invoice number plus the GST original-invoice tags the anchor credit note shows (verify on the installed build). `RULE-RET-001` FAIL when the reference is NULL, when no `sales_invoices` row exists for (store, ref FY, ref number), or when that invoice was never `RECONCILED` under the same profile (the narration would name a voucher Tally does not hold). Exchanges, zero-value documents and tender codes without a `tender_modes` row → `RULE-SRC-005` FAIL `POLICY_MISSING` naming the code. Golden G07, G09.
16. **Tender split (D16).** `IN_VOUCHER`: one debit line per tender mode from `reporting_sales_tenders`, plus the round-off line, sum = gross to the paisa or `RULE-AMT-003` FAIL; more than one tender requires `voucher_view='ACCOUNTING'` — a profile with `INVENTORY` + `IN_VOUCHER` + multi-tender is `BLOCKED` by `RULE-ENV-002` until D16 chooses `CLEARING_LEDGER`. `CLEARING_LEDGER`: the Sales voucher debits each mode's clearing ledger (or the customer ledger with a `BILLALLOCATIONS.LIST` New Ref = invoice number when D14 = `NAMED_LEDGERS`); `RECEIPT` components (clearing → bank/cash on settlement) wait for Phase 8, which knows when the bank credit arrived; until then the batch shows the clearing balance as "awaiting settlement". Test asserts revenue once and tender once with no ledger twice. Golden G03.
17. **Multi-line, discounts, round-off, tax patterns, Unicode.** Lines grouped per component rate into one `OUTPUT_<component>_<rate>` entry each while the plan keeps every source line (G02, repeated item matched by `sales_lines.line_identifier`, never by row position); discounts only as the source shows them (G04); explicit round-off only (G05); an alternative approved tax pattern passes and an unsupported one blocks (G06); G18 Unicode ledger and item names through file, database and read-back.
17a. **Inventory lines when D17 = `INVENTORY`.** `dbo.accounting_voucher_lines` (appendix S7-9: item code, quantity `decimal(19,3)`, unit, rate, taxable amount, godown from a `GODOWN` mapping per store) and the inventory list tags copied from `anchor-sales-invoice-view.xml`. The `SALES_REVENUE` credit is written only inside `ACCOUNTINGALLOCATIONS.LIST` of each inventory line (`LEDGERNAME` = the `SALES_REVENUE` mapping, `AMOUNT` = the line's `source_net_amount`); no separate `SALES_REVENUE` ledger entry is written; tax ledgers stay as ledger entries as the inventory anchor shows (verify on the installed build). `RULE-AMT-001` sums ledger entries plus allocations; the 7c `INVENTORY` check compares per-item quantity, rate, unit and allocation amount A = B = C. Otherwise (`ACCOUNTING_ONLY`) the `INVENTORY` level records `NOT_VERIFIABLE`. Test: `TallySalesVoucherXmlTests` INVENTORY case — exactly one occurrence of the revenue ledger per item line and zero at ledger-entry level.
17b. **Exclusions with reason.** The Owner may exclude a voucher (`voucher_status='EXCLUDED'`, history row `PLANNED→EXCLUDED`, `selection_version` +1, reservation released with the reason per task 2 rule 2) only while the batch is `DRAFT` or `BLOCKED` and no `tally_attempts` row exists; excluding from an `APPROVED_READY` batch first runs `usp_accounting_batch_transition` to `DRAFT` (reason "Voucher excluded: …"), invalidating the approval. After any attempt, exclusion is refused with "This batch has already been written; use the recovery plan". Excluded vouchers are never counted as sent and the coverage check lists them. SQL test: exclusion after an attempt refused; exclusion from `APPROVED_READY` leaves the batch `DRAFT` with two history rows.
18. **Mapping register and golden set.** `PHASE-7-REPORT.md` "Mapping and policy register": business event → ledgers → D-number → approved / open / blocked; transaction support matrix (INV, SR, BC, exchange, cancellation, zero-value, every tender mode: supported / blocked with rule id). Golden G01–G10 and G18 green in CI (`SQL_VERIFIED`) with live re-runs of G03 and G07 recorded `LIVE_TALLY_7_1_VERIFIED` or `NOT_RUN`.

**Slice 7c — reconciliation engine, evidence package, re-audit, recovery plan**
19. **Full check set.** Rule set `'7c.1'` adds `INVENTORY` (quantity, unit and item per line, D17), `AGGREGATE` (per store/date gross, tax and tender totals A vs C — recorded but never sufficient: equal totals with any differing voucher still `FAILED_RECONCILIATION`), `SOURCE_CHANGED`, `ACTUAL_CHANGED` (located voucher's `fragment_sha256` differs from the earlier read-back), `STATUS_MISMATCH` (the located voucher's cancelled/optional/post-dated flags set — candidate tags `ISCANCELLED`, `ISOPTIONAL`, `ISPOSTDATED`, verified in task 5; if the build omits cancelled vouchers from the export a cancelled voucher surfaces as `MISSING` and the report records that), `EXTRA` (an actual voucher in range carrying an ETP key no voucher claims). Tolerances table `dbo.tally_reconciliation_tolerances` (appendix S7-7; `absolute_tolerance` default 0, no percentage column exists). Snapshot consistency for HTTP read-backs: two read-backs ≥ 60 s apart with identical `fragment_sha256` sets, else `RECONCILIATION_INCOMPLETE` "Tally was being edited; try again"; a `MANUAL_FILE` read-back is one snapshot by definition and the run records a WARN "single snapshot, taken from a file" the Owner may accept. Truncation = a response that does not parse to the closing `ENVELOPE`, or two consecutive read-backs of the same range differing in voucher set → `NOT_VERIFIABLE`, never zero. Tests: G11 (`SOURCE_CHANGED`), G16 (`ACTUAL_CHANGED`), G17 (missing / cancelled / duplicated → `MISSING` / `STATUS_MISMATCH` / `DUPLICATE`, three distinct rows), zero-versus-missing, equal-aggregate-but-different-vouchers, two fixture read-backs with equal fragment sets for the HTTP path.
20. **Re-audit and revision.** "Check again" (CanImport) on any `RECONCILED*` batch: new read-back and run, earlier run untouched; a changed outcome sets `RE_AUDIT_REQUIRED` and lists both runs; a voucher absent while other keys' counts also dropped carries `required_action` "Cannot tell deletion from a filtered export; export the full Day Book and check again". Warnings accepted by the Owner only, per difference row, with reason → `RECONCILED_WITH_ACCEPTED_WARNINGS`. Revision path for `SOURCE_CHANGED`: the revision-n reservation stays live for ever (it points at the voucher Tally actually holds); the Owner records an `approval_requests` row type `TALLY_REVISION` (subject = voucher) which allocates `revision n+1` for the invoice; the corrective batch holds a `CREDIT_NOTE` (revision n+1) that reverses the located actual voucher and a fresh `SALES` voucher with revision n+1 — never an edit, never a silent second voucher; the coverage check treats revision n as superseded and reconciles revision n+1. Tests: SQL second run leaves the first run's rows byte-identical; G11 continues: after revision the read-back holds two `SALES` vouchers for the invoice with different keys and the run is `RECONCILED`, not `DUPLICATE`.
21. **Evidence package.** `TallyEvidencePackage.BuildAsync(batchId)` completes the task 4 folder with the fixed file list: `manifest.json` (intended company, environment, profile code, selection, exclusions with reasons, voucher count, control totals gross/taxable/tax/tender, mapping version set, rule-set version, schema and app version, delivery mode, format, SHA-256 of every other file; its own hash stored in `accounting_batches.manifest_sha256`, not inside itself), `source-snapshot.json` (the `InvoiceAccountingSource` record of task 6, so it never holds a phone), `expected-postings.json`, `validation.json`, `payload.xml`, `attempts\attempt-<id>-request.xml|response.xml` (HTTP only), `actuals\readback-<id>.xml`, `reconciliation\run-<id>.json`, `approvals.json`; each registered in `tally_artifacts`, kind `MANIFEST` for the manifest. Customer names may appear inside `payload.xml` and `source-snapshot.json` only when D14 = `NAMED_LEDGERS`, never in `manifest.json` or a path. `TallyEvidencePackage.VerifyAsync(batchId)` re-hashes every registered file → OK / CHANGED / MISSING, surfaced under "Open evidence folder". Tests: unit (temp folder): manifest covers every file; byte-flipped file → CHANGED.
22. **Exceptions and recovery plan.** Differences grid with filter chips per difference type; drill-down from a row to the source invoice (Phase 2 customer-wise route) and to the evidence file. `RecoveryPlanBuilder` lists only vouchers with status ≠ `RECONCILED`, proposing per voucher `READ_BACK_AGAIN`, `MANUAL_CORRECTION_IN_TALLY`, `REVERSE_AND_REISSUE` (new batch: Credit Note plus fresh Sales voucher at revision n+1 per task 20, approval required), `RESEND_VOUCHER` (7d only, absence proven), `ACCEPT_WITH_REASON` (Owner); saved as a `RECOVERY_PLAN` artifact and approved by the Owner before any action; the whole batch is never resent; the engine never deletes, alters or re-dates a Tally voucher; before/after evidence linked in the package. `docs/OPERATIONS.md` section "When Tally and ETP disagree" in staff words: stop writing, find out what Tally holds, isolate, approved plan, apply, check again.

**Slice 7d — connected mode, formats, hardening, UAT, production gate**
23. **HTTP submission with durable attempts.** Port `ITallySubmissionGateway.PostAsync(TallyPostRequest(CompanyName, PayloadXml), ct)` in `TallyTransferContracts.cs`, `HttpTallySubmissionGateway` in the Http project (same hardening as task 9; no retry of writes). `SqlServerAccountingService.SubmitAsync(batchId)` (Owner): (0) read-only `FetchOpenCompaniesAsync()` (request shape verified in task 5; exact envelope in the report) — Send is refused with "Tally is running but the company {name} is not open" when the profile company is absent and with "Tally is not answering on this PC" on transport failure, and nothing is inserted into `tally_attempts` in either case; (1) revalidate as task 8 and re-hash `payload.xml` against the artifact immediately before sending; (2) `EXEC sp_getapplock @Resource=N'ETP_TALLY_SUBMIT_<profile_code>', @LockMode='Exclusive', @LockOwner='Transaction', @LockTimeout=0` (return < 0 → `InvalidOperationException("Another send is running for this Tally company.")`), insert `tally_attempts` `RECORDED` → `SENT`, vouchers `SUBMITTED`, batch `SUBMITTED_AWAITING_RESULT`, commit (the lock releases with the commit); (3) POST with no SQL transaction open; (4) store the response as `HTTP_RESPONSE` artifact and set the attempt `RESPONDED` / `TIMED_OUT` / `TRANSPORT_FAILED`: `TRANSPORT_FAILED` only when the exception is `HttpRequestException` whose `HttpRequestError` is `ConnectionError` or `NameResolutionError` (connection never established) — that alone returns the batch to `APPROVED_READY`; every other exception after `SendAsync` began (`TaskCanceledException` timeout, `IOException`, `HttpRequestException` with a response, unparseable body) is `TIMED_OUT` or `RESPONDED`-but-unparseable; (5) those set vouchers and batch `OUTCOME_UNKNOWN`; a second Send while `OUTCOME_UNKNOWN` → `E-SEND-UNKNOWN`; the only way out is a complete company-bound read-back that locates the vouchers or proves absence with two consistent read-backs, after which the Owner may approve `RESEND_VOUCHER` for absent vouchers only. No automatic retry of writes, no retry in another format, no cross-format fallback. Tally's import counts and per-object messages are stored in `tally_diagnostics_json` only and never set a status. "Cancel" stops future dispatch and the screen says "Vouchers already sent to Tally are not undone by cancelling". Tests: `HttpTallySubmissionGatewayTests` (success; stub throwing `SocketException(ConnectionRefused)` → `TRANSPORT_FAILED`; stub delaying past the timeout → `TIMED_OUT`; stub throwing `IOException` mid-response → `OUTCOME_UNKNOWN`; HTML; redirect); SQL `TallySubmissionSqlTests` (attempt row exists before any response; `E-SEND-UNKNOWN`); failure tests: process killed between (2) and (3) leaves `SUBMITTED_AWAITING_RESULT` recoverable by read-back; two processes racing → one holds the applock, the other re-reads the batch and is refused by `usp_accounting_batch_transition` because it is already `SUBMITTED_AWAITING_RESULT` ("Another send is running or has run; read back first"); G14 (one voucher rejected by Tally → `PARTIALLY_APPLIED`, recovery plan lists only it); G15 (timeout after acceptance resolved by read-back, no second voucher).
24. **Native JSON — only if verified.** A task-5-style probe on the installed build with the G01 fixture; if the JSON exchange is confirmed live, add `TallyJsonExportService` behind the same re-parse rule (`RULE-PAY-001`) and enable `payload_format='JSON'` per profile; otherwise JSON profiles are refused at save with "This Tally build has not been verified for JSON", the option is hidden, and the format × delivery matrix marks the cell `NOT_RUN`. Same for `delivery_mode='HTTP'` writes until task 23's live test passes. No second posting merely to compare formats.
25. **Parser and path hardening, boundary tests.** Endpoint validation re-enforced at send time; evidence root must lie under `%ProgramData%\EtpReporting`; manual read-back files must be under the operator-chosen folder, no reparse points, size cap; DTD/XXE prohibited in every reader; `tests-dotnet/fixtures/tally-golden/malformed/` fed to every parser; Store Manager cannot Approve, Write file, Send, accept warnings, approve plans, release invoices or edit profiles (service-level `RequireOwnerAsync`; boundary tests for `ApplicationRole.StoreManager` and `Viewer` on every gated method).
25a. **Owner-run restricted role, runbooks, regression and timing.** The owner creates the restricted Tally user the accountant specifies (D18) and re-runs every live step under it, limits recorded; `docs/OPERATIONS.md` gains "Sending sales to Tally" (setup, firewall rule, pre-flight, file mode, connected mode, read back, exceptions) and "Recovering a Tally transfer" (unknown outcome, partial, corrections, restore is not per-batch rollback); full `dotnet test` and the Phase 2 golden reports re-run after Phase 7 migrations; one full month of both stores timed through Prepare → Compare and recorded (no target unless slower than 60 s).
26. **UAT and the production gate.** Migration (next free number): `dbo.tally_backup_evidence` (appendix S7-10; `backup_kind` `TALLY_COMPANY`/`ENGINE_DATABASE`, `file_name_safe` name only, `sha256`, `taken_utc`, `restore_verified_utc`; append-only). Owner action "Record Tally backup" hashes the file the Owner picks (taken with Tally's own documented backup procedure — verify on the installed build) and inserts a `TALLY_COMPANY` row, `restore_verified_utc` set by the Owner after the isolated restore; the `ENGINE_DATABASE` row is inserted by the Phase 4 backup script from its `-ResultPath` receipt (`sha256`, `verifiedAtUtc`). Checklist in `PHASE-7-REPORT.md`, each row with an evidence path: task 5 probe recorded; G01, G03, G07 and one real day round-tripped in the TEST company; a deliberate mismatch demonstrated and drilled to source; an edited test voucher caught by "Check again"; a timeout simulated and resolved by read-back; restricted role tested; Tally company backup restored in isolation with its hash recorded beside a Phase 4 backup receipt < 24 h old with matching row counts; accountant's written acceptance of the posting profile (ledgers, tax, tender, granularity). Only then: Settings → Tally → "Enable live books" is enabled only when the profile has a `TALLY_COMPANY` row with `restore_verified_utc` set and an `ENGINE_DATABASE` row with `taken_utc` within 24 h, requires typing the company name and the accountant's acceptance reference, writes `production_enabled_by/utc`, a `TallyProfileChange` audit and a history row; the first PRODUCTION batch covers one day and must reach `RECONCILED` before a second is allowed (`E-PROD-FIRST-DAY`); until then `RULE-ENV-001` blocks and the badge reads `LIVE (not enabled)`. Documented limits: the engine cannot stop an operator importing a file into another company; idempotency covers only vouchers the engine wrote to the named company; company copies and other integrations are outside it. Completion matrix: every task and golden case marked implemented / verification label / blocked / deferred.

Acceptance (auditor evidence in brackets). Live items (A7.7, A7.9, A7.12, A7.13, A7.15, A7.17 and the restricted-role part of A7.18) are PASS only when Claude runs them on the D18 machine and records `LIVE_TALLY_7_1_VERIFIED`; owner captures are attached to Codex's self-assessment as `OWNER_CAPTURE` evidence and never turn an item to PASS; an item Claude could not run stays `NOT_RUN` and the phase stays open. Error numbers are checked against the `E-*` mapping recorded in `PHASE-7-REPORT.md`.
- A7.1 Migrations apply on an empty database and on a copy of the shop database; `tally_profiles`, `tally_profile_stores`, `accounting_vouchers`, `accounting_voucher_reservations`, `accounting_status_history`, `accounting_validation_findings`, `tally_artifacts`, `tally_attempts`, `tally_readbacks`, `tally_actual_vouchers`, `tally_actual_ledger_entries`, `tally_reconciliation_runs`, `tally_reconciliation_differences`, `tally_master_snapshots`, `tally_masters`, `tally_master_creation_plans`, `tally_backup_evidence` exist with the CHECK lists of the appendix; a legacy `REVIEW` batch reads `DRAFT`; `UPDATE dbo.tally_artifacts SET sha256='…'` and `DELETE dbo.accounting_status_history` fail with the `E-IMMUTABLE` number even as Owner in SSMS; as a member of `etp_store_manager`, `INSERT dbo.tally_readbacks` succeeds and `INSERT dbo.tally_profiles` fails. [SQL results and error numbers]
- A7.2 Settings → Tally → Tally companies as Owner: a profile with `endpoint_url = http://192.168.1.5:9000/` is refused with "Only this PC's Tally can be used"; `http://127.0.0.1:<port>/` saves; a second TEST profile for the same store is refused; `NAMED_LEDGERS` with `IN_VOUCHER` is refused with "Named customer ledgers need a clearing ledger per tender mode"; as Store Manager the Save button is absent and `TallyProfileRepository.SaveAsync` throws; the Accounting screen has no mapping editor, only the link "Change ledger mappings"; `netsh advfirewall firewall show rule name="ETP Tally port"` shows the port allowed for 127.0.0.1 only. [screenshots; `SELECT profile_code, environment, endpoint_url, voucher_view FROM dbo.tally_profiles`; netsh output]
- A7.3 Prepare one 25 Aug 2026 Titan INV invoice in the TEST profile: the voucher grid shows a tender debit equal to that invoice's `SUM(source_gross_amount)` less any round-off line, revenue equal to `SUM(source_net_amount)`, and CGST and SGST rows equal to `SUM(cgst_amount)` and `SUM(sgst_utgst_amount)` from `etp_r018` for that invoice, together equal to `SUM(source_tax_amount)`; Save writes one `accounting_batches` row (`batch_kind='SALES_VOUCHERS'`, `DRAFT`), one `accounting_vouchers` row (`revision=1`, key ending `:SALES:1`), one active reservation, one history row (`NULL→DRAFT`); a `DAY_JOURNAL` Save for the same store/day is refused with "Invoices for this day are already being sent as vouchers". [screenshot; SQL join; auditor recomputes the sums from the workbook]
- A7.4 Set the approved `SALES_REVENUE` mapping `is_active=0` in SQL, press Validate → `BLOCKED`, finding `RULE-MAP-001` FAIL in plain words with "what to do", zero active reservations for the blocked voucher; Approve disabled with the reason shown; `usp_accounting_batch_transition` to `APPROVED_READY` fails with the `E-TRANSITION` number; restore the mapping, Validate → `DRAFT` and the reservation is back. [screenshot; `SELECT rule_id, severity FROM dbo.accounting_validation_findings WHERE accounting_batch_id=@id`; reservation count]
- A7.5 Approve as Owner with a reason → `APPROVED_READY`, history row carries the reason; a `RULE-DAT-002` WARN (voucher dated > 30 days back) blocks Approve until "Accept with reason" writes `waived_by`; approving as Store Manager is refused with "Owner permission is required.". [SQL; screenshot]
- A7.6 "Write Tally file": `payload.xml` under `<root>\<profile>\<store>\<yyyy-MM>\batch-<id>\`, `Get-FileHash` equals `tally_artifacts.sha256`, the export-history hash and the on-screen hash; the XML re-parsed by the auditor with an independent tool holds exactly one `VOUCHER` with `SVCURRENTCOMPANY` = the profile company, the view tags of the anchor, the correspondence key in `NARRATION`, ledger amounts equal to A7.3, and none of `REMOTEID`, `VCHKEY`, `GUID`, `MASTERID`, `ALTERID`, `VOUCHERKEY`; one `tally_attempts` row (`FILE`, `FILE_WRITTEN`); batch `EXPORTED_AWAITING_IMPORT`; receipt text contains "Nothing has been sent to Tally". [hash; XML excerpt; SQL; screenshot]
- A7.7 Live round trip (TEST company A): import the file in Tally, press "I imported this file into Tally", "Read back from Tally" (then once more via "Load a file exported from Tally") → `tally_readbacks.company_name_reported` = company A, one actual voucher with the key; "Compare" → `RECONCILED`, zero difference rows, header strip expected 1 / found 1 / unresolved 0; Tally's Day Book shows the same amounts. [Tally screen capture naming company and build; `SELECT outcome FROM dbo.tally_reconciliation_runs`; screenshot]
- A7.8 Replay: Prepare the same invoice again → Save refused "Invoice … is already in batch …"; Prepare from a re-exported workbook with shifted rows → same refusal; import the same file into Tally a second time by hand (TEST voucher type on Automatic numbering, per D18) → two vouchers in Tally; Read back and Compare → `RE_AUDIT_REQUIRED` then `FAILED_RECONCILIATION` (two history rows) with one `DUPLICATE` row and its required action; `tally_artifacts` count unchanged; active reservations for the key = 1. [screenshots; SQL counts]
- A7.9 Wrong company: read back from TEST company B (or its manual export) → `tally_readbacks.is_complete = 0`, reason `WRONG_COMPANY`, no voucher matched, screen says "Tally answered for a different company"; a manual export with the `SVCURRENTCOMPANY` echo removed → reason `COMPANY_NOT_REPORTED`. [`SELECT source, company_name_reported, is_complete, incomplete_reason FROM dbo.tally_readbacks`; screenshot]
- A7.10 Changed payload and changed source: edit one byte of `payload.xml`, Compare → `RE_AUDIT_REQUIRED` then `FAILED_RECONCILIATION` with an `INTEGRITY` difference; restore the file, Compare → `RECONCILED` (three history rows, three run rows, first run byte-identical). After approval of a fresh batch, restate the day so one invoice amount differs → "Write Tally file" refused with `RULE-SRC-004`, batch `BLOCKED`; after an export, Compare shows `SOURCE_CHANGED` for that voucher and the reservation is still present. [hashes; run rows; SQL]
- A7.11 Mapping approval needs two logins (7b): propose and approve the same mapping from one login → the `E-SELF-APPROVE` message; proposed from the shared Store Manager login and approved by the Owner → new version active, previous `is_active=0` with `effective_to` set, one `approval_requests` row `APPROVED` with `decided_by <> requested_by`; an `APPROVED_READY` batch for that event with no attempt drops to `BLOCKED` with history reason "Mapping changed"; overlapping windows → `E-MAP-OVERLAP`. [screenshots from both logins; SQL]
- A7.12 Masters (7b): rename one mapped ledger in company A, "Read masters" → `Not found`, batch blocked by `RULE-MST-001`; plan and approve its creation, import the masters file with "Ignore duplicates", read masters again → `Found`; the same masters file imported with "Modify with new data" against a renamed group is caught as `ACTUAL_CHANGED` on the next read; a snapshot older than 24 h → `RULE-MST-002`. [screenshots; `SELECT master_name, parent_name FROM dbo.tally_masters` before/after]
- A7.13 Returns and split tender (7b): a day with one SR and one two-tender invoice → a `CREDIT_NOTE` voucher crediting the tender ledger and debiting revenue and tax with the original invoice in `REFERENCE`, and a Sales voucher with two tender lines (plus round-off if present) summing to gross; an SR whose original cannot be resolved → `RULE-RET-001`; an unknown tender code → `RULE-SRC-005` naming the code; both fixtures reconcile after the live round trip. [fixture output; XML excerpt; Tally capture]
- A7.14 Full engine (7c): one `SALES_VOUCHERS` batch per store for the same day (two batches, two evidence folders), both reconciled; then one voucher whose two ledger amounts are swapped in Tally by hand → `FAILED_RECONCILIATION` with `LEDGER_MISMATCH` on exactly that voucher, `AGGREGATE` PASS; a voucher missing its CGST ledger in Tally → `TAX_MISMATCH`, never a zero; a stray voucher carrying an ETP key → `EXTRA`. [difference rows; screenshot with drill-down to the source invoice]
- A7.15 Re-audit (7c): edit a reconciled voucher's amount in Tally, "Check again" → `ACTUAL_CHANGED`, `RE_AUDIT_REQUIRED`, first run rows unchanged (same `run_id`, same outcome, same row count); cancel it → `STATUS_MISMATCH`, or `MISSING` when task 5 recorded that the build omits cancelled vouchers; delete it → `MISSING` or `NOT_VERIFIABLE` with the "cannot tell deletion from a filtered export" action, never `RECONCILED`; a `SOURCE_CHANGED` voucher revised → read-back holds keys `…:SALES:1` and `…:SALES:2`, run `RECONCILED`. [two run rows; Tally capture; key list]
- A7.16 Evidence folder (7c) for a reconciled file-mode batch holds `manifest.json`, `source-snapshot.json`, `expected-postings.json`, `validation.json`, `payload.xml`, `actuals\`, `reconciliation\`, `approvals.json` and no `attempts\`; the auditor recomputes every hash listed in the manifest; the manifest hash equals `accounting_batches.manifest_sha256`; `findstr` over the directory listing and over the file contents (`findstr /s /r "[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]"`) finds no phone number and no customer name unless D14 = `NAMED_LEDGERS`, in which case names appear only inside `payload.xml` and `source-snapshot.json`; `icacls` shows no BUILTIN\Users entry. [listing; hash recompute; findstr; `icacls`]
- A7.17 Connected mode (7d, TEST company A): with company A closed but Tally running, "Send to Tally" is refused with "Tally is running but the company … is not open" and no attempt row exists; with Tally stopped, Send is refused with "Tally is not answering on this PC", no attempt row; with Tally running and the port refusing mid-test (stub) → `TRANSPORT_FAILED`, batch back at `APPROVED_READY`; with Tally running, Send → attempt `SENT` then `RESPONDED`, Read back and Compare → `RECONCILED`; with a simulated timeout (stub handler in tests, and live by closing Tally mid-request) → `OUTCOME_UNKNOWN`, second Send refused with the `E-SEND-UNKNOWN` number, only Read back offered; after read-back no second voucher exists; one voucher rejected by Tally among successes → `PARTIALLY_APPLIED` and the recovery plan lists only it. [attempt rows; screenshots; Tally Day Book counts before/after]
- A7.18 Roles: as Viewer every Tally screen is read-only; as Store Manager Prepare, Validate, Save, "I imported this file", Read back, Compare, Check again, Propose mapping and Open evidence folder work while Approve, Write Tally file, Send, accept warning, plan approval, Release invoice and profile edits are refused with "Owner permission is required."; button states refresh after a role change without restart; the restricted Tally user repeats A7.7 and A7.17 with the same outcome or the limit is written in `docs/OPERATIONS.md`. [screenshots per login; boundary test names]
- A7.19 Production gate: a PRODUCTION profile with NULL `production_enabled_utc` appears in the selector as `LIVE (not enabled)`; Prepare → `BLOCKED` by `RULE-ENV-001`; "Enable live books" is disabled without a `TALLY_COMPANY` row with `restore_verified_utc`, an `ENGINE_DATABASE` row < 24 h and the acceptance reference; after enabling, the badge reads `LIVE` in red and a second PRODUCTION batch before the first is `RECONCILED` is refused with the `E-PROD-FIRST-DAY` number. [screenshots; `SELECT environment, production_enabled_utc, production_enabled_by FROM dbo.tally_profiles`; `SELECT backup_kind, taken_utc, restore_verified_utc FROM dbo.tally_backup_evidence`]
- A7.20 `dotnet test Etp.Reporting.slnx -c Release` green including the Http test project (stub handlers only); fixtures under `tests-dotnet/fixtures/tally-golden/` contain no real name or phone (`grep` for ten-digit runs returns nothing); `PHASE-7-REPORT.md` carries the compatibility table (build, port, view tags, voucher tags used, identifiers, numbering, encoding), the `E-*` number mapping, the format × delivery matrix, the mapping register, G01–G18 (G19 deferred) with labels, the completion matrix, and `findstr` finds no "fully working", "Tally-compatible", "rollback supported" or "production ready" without scope. [test summary lines; document review]

Deferred inside this phase (optional 7e, only if 7a–7d prove the need):
- Local TDL package (namespaced `.tdl` source, version manifest, install notes, health report, in-Tally "ETP Import Audit" report, expected-manifest completeness check, UDF collision register, coexistence tests with other TDLs); checks that would need TDL record `NOT_VERIFIABLE` in 7a–7d.
- UDF origin metadata on vouchers and `$$ImportInfo`-style diagnostics; 7a–7d rely on the narration/reference correspondence key only.
- Account TDL distribution and compiled `.tcp`: out.
- `RECEIPT` components for clearing-ledger settlements: Phase 8 (Stage 3), which knows when the bank credit arrived; `component_role='RECEIPT'` is reserved in the CHECK now.
- `OUTPUT_CESS_<rate>` (only if the accountant meets cess); service income as a separate business event (no ETP source category; confirm with the owner before any code); cost centre, bill-wise reference and godown mappings beyond what D14/D16/D17 require; purchases, stock transfers, journals, opening balances: out.
- Native JSON beyond the single verification test in task 24; stronger evidence signatures (SHA-256 stands; no key-management mechanism exists); any display time-zone layer; automated Windows UI test harness (Claude's manual run remains the audit path).

Phase 7 schema appendix (all tables `dbo.`; hashes `char(64)` with `CHECK (col NOT LIKE '%[^0-9a-f]%')`; JSON columns `nvarchar(max)` with `CHECK ISJSON(col)=1`; `*_utc datetime2(3)`, `*_by nvarchar(100) DEFAULT ORIGINAL_LOGIN()` unless stated; "append-only" = `AFTER UPDATE,DELETE` trigger `trg_<table>_immutable` throwing `E-IMMUTABLE` plus the task 2 DENYs):

| Ref | Table | Columns (type, constraint) | Keys, CHECKs, notes |
|---|---|---|---|
| S7-1 | `tally_profiles` | `tally_profile_id int IDENTITY PK`; `profile_code varchar(30) NOT NULL UNIQUE`; `company_name nvarchar(200) NOT NULL`; `environment varchar(10) NOT NULL CHECK IN ('TEST','PRODUCTION')`; `endpoint_url nvarchar(200) NULL`; `default_delivery_mode varchar(10) NOT NULL DEFAULT 'FILE' CHECK IN ('FILE','HTTP')`; `payload_format varchar(10) NOT NULL DEFAULT 'XML' CHECK IN ('XML','JSON')`; `voucher_granularity varchar(20) NOT NULL CHECK IN ('PER_INVOICE','DAILY_SUMMARY')`; `party_policy varchar(20) NOT NULL CHECK IN ('NAMED_LEDGERS','SINGLE_LEDGER')`; `single_party_ledger nvarchar(200) NULL`; `tender_model varchar(20) NOT NULL CHECK IN ('IN_VOUCHER','CLEARING_LEDGER')`; `posting_model varchar(20) NOT NULL CHECK IN ('ACCOUNTING_ONLY','INVENTORY')`; `voucher_view varchar(30) NOT NULL CHECK IN ('ACCOUNTING','INVOICE')`; `posting_from_date date NULL`; `posting_to_date date NULL`; `tally_build_label nvarchar(100) NULL`; `company_identity_json NULL`; `is_enabled bit NOT NULL DEFAULT 1`; `production_enabled_utc NULL`; `production_enabled_by nvarchar(100) NULL`; `modified_by`; `modified_utc`; `change_reason nvarchar(500) NOT NULL` | UNIQUE (`tally_profile_id`,`environment`); `CK_tally_profiles_party_tender CHECK (NOT (party_policy='NAMED_LEDGERS' AND tender_model='IN_VOUCHER'))`; `CK_tally_profiles_production CHECK (production_enabled_utc IS NULL OR production_enabled_by IS NOT NULL)`; `single_party_ledger` NOT NULL when `party_policy='SINGLE_LEDGER'` (CHECK). Owner writes only. |
| S7-1 | `tally_profile_stores` | `tally_profile_id int NOT NULL`; `environment varchar(10) NOT NULL`; `store_code varchar(30) NOT NULL FK stores` | FK (`tally_profile_id`,`environment`) → `tally_profiles`; UNIQUE (`store_code`,`environment`). |
| S7-2 | `accounting_batches` (ALTER) | `tally_profile_id int NULL FK`; `batch_kind varchar(20) NOT NULL DEFAULT 'DAY_JOURNAL' CHECK IN ('DAY_JOURNAL','SALES_VOUCHERS')`; `selection_json NULL`; `selection_version int NOT NULL DEFAULT 1`; `mapping_version_set_json NULL`; `manifest_sha256 char(64) NULL` | `CK_accounting_batches_status` re-added with the task 3 list after legacy mapping. |
| S7-2 | `accounting_vouchers` | `accounting_voucher_id bigint IDENTITY PK`; `accounting_batch_id bigint NOT NULL FK`; `voucher_sequence int NOT NULL`; `component_role varchar(20) NOT NULL CHECK IN ('SALES','CREDIT_NOTE','RECEIPT')`; `voucher_type nvarchar(50) NOT NULL`; `store_code varchar(30) NOT NULL`; `invoice_year int NOT NULL`; `document_number nvarchar(80) NOT NULL`; `revision int NOT NULL DEFAULT 1 CHECK (revision>0)`; `sales_invoice_id bigint NULL FK sales_invoices`; `voucher_date date NOT NULL`; `expected_total decimal(19,4) NOT NULL`; `correspondence_key nvarchar(200) NOT NULL`; `source_sha256 char(64) NOT NULL`; `plan_sha256 char(64) NOT NULL`; `voucher_status varchar(30) NOT NULL DEFAULT 'PLANNED' CHECK IN ('PLANNED','BLOCKED','EXCLUDED','EXPORTED','SUBMITTED','OUTCOME_UNKNOWN','ACTUAL_LOCATED','RECONCILED','RECONCILED_WITH_WARNINGS','DIFFERENCE','CANCELLED')`; `blocked_reason nvarchar(1000) NULL` | UNIQUE (`accounting_batch_id`,`voucher_sequence`); `CK_accounting_vouchers_key CHECK (correspondence_key = CONCAT('ETP:',store_code,':',invoice_year,':',document_number,':',component_role,':',revision))`; trigger `trg_accounting_vouchers_approved` (task 3). |
| S7-2 | `accounting_entries` (ALTER) | `accounting_voucher_id bigint NULL FK`; `tax_rate decimal(5,2) NULL`; `quantity decimal(19,3) NULL`; `stock_item nvarchar(200) NULL` | `trg_accounting_entries_approved` re-created (task 3). |
| S7-2 | `accounting_voucher_reservations` | `reservation_id bigint IDENTITY PK`; `tally_profile_id int NOT NULL FK`; `store_code varchar(30) NOT NULL`; `invoice_year int NOT NULL`; `document_number nvarchar(80) NOT NULL`; `component_role varchar(20) NOT NULL`; `revision int NOT NULL DEFAULT 1`; `accounting_voucher_id bigint NOT NULL FK`; `reserved_by`; `reserved_utc`; `released_utc NULL`; `release_reason nvarchar(500) NULL` | Filtered UNIQUE `UX_accounting_voucher_reservations_active (tally_profile_id, store_code, invoice_year, document_number, component_role, revision) WHERE released_utc IS NULL`; UPDATE allowed only to set `released_utc`/`release_reason` once (trigger). |
| S7-3 | `accounting_status_history` | `history_id bigint IDENTITY PK`; `subject_type varchar(10) NOT NULL CHECK IN ('BATCH','VOUCHER')`; `subject_id bigint NOT NULL`; `from_status varchar(40) NULL`; `to_status varchar(40) NOT NULL`; `actor`; `reason nvarchar(1000) NULL`; `evidence_artifact_id bigint NULL`; `changed_utc DEFAULT SYSUTCDATETIME()` | Append-only. Written by `SaveAccountingBatchAsync` (first row) and `usp_accounting_batch_transition`. |
| S7-4 | `tally_artifacts` | `tally_artifact_id bigint IDENTITY PK`; `accounting_batch_id bigint NOT NULL FK`; `artifact_kind varchar(20) NOT NULL CHECK IN ('SOURCE_SNAPSHOT','EXPECTED_POSTINGS','VALIDATION','PAYLOAD_XML','HTTP_REQUEST','HTTP_RESPONSE','READBACK_XML','RECONCILIATION','RECOVERY_PLAN','APPROVAL','MANIFEST')`; `relative_path nvarchar(400) NOT NULL`; `sha256 char(64) NOT NULL`; `byte_length bigint NOT NULL CHECK (byte_length>0)`; `created_by`; `created_utc` | UNIQUE (`accounting_batch_id`,`relative_path`); `IX_tally_artifacts_hash (sha256)` non-unique; append-only. |
| S7-4 | `tally_attempts` | `tally_attempt_id bigint IDENTITY PK`; `accounting_batch_id bigint NOT NULL FK`; `tally_profile_id int NOT NULL FK`; `payload_artifact_id bigint NOT NULL FK tally_artifacts`; `delivery_mode varchar(10) NOT NULL CHECK IN ('FILE','HTTP')`; `payload_format varchar(10) NOT NULL CHECK IN ('XML','JSON')`; `attempt_status varchar(30) NOT NULL CHECK IN ('RECORDED','FILE_WRITTEN','SENT','RESPONDED','TIMED_OUT','TRANSPORT_FAILED','CANCELLED')`; `http_status int NULL`; `response_artifact_id bigint NULL FK`; `tally_diagnostics_json NULL`; `started_utc`; `completed_utc NULL`; `started_by` | UPDATE allowed only to advance `attempt_status`, `http_status`, `response_artifact_id`, `tally_diagnostics_json`, `completed_utc` from `RECORDED`/`SENT` (trigger); DELETE denied. |
| S7-5 | `accounting_validation_findings` | `finding_id bigint IDENTITY PK`; `accounting_batch_id bigint NOT NULL FK`; `accounting_voucher_id bigint NULL FK`; `rule_id varchar(30) NOT NULL`; `rule_version int NOT NULL`; `severity varchar(5) NOT NULL CHECK IN ('PASS','WARN','FAIL')`; `subject nvarchar(200) NULL`; `observed nvarchar(500) NULL`; `expected nvarchar(500) NULL`; `explanation nvarchar(1000) NOT NULL`; `corrective_action nvarchar(500) NULL`; `waived_by nvarchar(100) NULL`; `waived_utc NULL`; `waiver_reason nvarchar(1000) NULL`; `created_utc` | UPDATE allowed only to set the three `waiv*` columns once (Owner); DELETE denied. |
| S7-6 | `tally_readbacks` | `tally_readback_id bigint IDENTITY PK`; `tally_profile_id int NOT NULL FK`; `accounting_batch_id bigint NULL FK`; `source varchar(12) NOT NULL CHECK IN ('HTTP','MANUAL_FILE')`; `company_name_reported nvarchar(200) NULL`; `from_date date NOT NULL`; `to_date date NOT NULL`; `voucher_count int NOT NULL`; `is_complete bit NOT NULL`; `incomplete_reason varchar(30) NULL CHECK IN ('WRONG_COMPANY','COMPANY_NOT_REPORTED','COMPANY_NOT_OPEN','TALLY_ERROR','TRUNCATED','NOT_XML')`; `request_artifact_id bigint NULL FK`; `response_artifact_id bigint NOT NULL FK`; `adapter_version varchar(20) NOT NULL`; `acquired_utc`; `acquired_by` | Append-only. |
| S7-6 | `tally_actual_vouchers` | `tally_actual_id bigint IDENTITY PK`; `tally_readback_id bigint NOT NULL FK`; `voucher_type nvarchar(50) NOT NULL`; `voucher_date date NOT NULL`; `voucher_number nvarchar(100) NULL`; `reference nvarchar(200) NULL`; `narration nvarchar(1000) NULL`; `correspondence_key nvarchar(200) NULL`; `tally_guid nvarchar(100) NULL`; `tally_master_id bigint NULL`; `tally_alter_id bigint NULL`; `is_cancelled bit NULL`; `is_optional bit NULL`; `total_amount decimal(19,4) NOT NULL`; `fragment_sha256 char(64) NOT NULL` | Append-only; `IX (tally_readback_id, correspondence_key)`. |
| S7-6 | `tally_actual_ledger_entries` | `tally_actual_entry_id bigint IDENTITY PK`; `tally_actual_id bigint NOT NULL FK`; `line_number int NOT NULL`; `ledger_name nvarchar(200) NOT NULL`; `amount decimal(19,4) NOT NULL` (signed as Tally reports it); `is_deemed_positive bit NOT NULL`; `quantity_text nvarchar(50) NULL`; `rate_text nvarchar(50) NULL`; `quantity decimal(19,3) NULL`; `unit nvarchar(20) NULL`; `stock_item nvarchar(200) NULL` | UNIQUE (`tally_actual_id`,`line_number`); append-only. |
| S7-7 | `tally_reconciliation_runs` | `run_id bigint IDENTITY PK`; `accounting_batch_id bigint NOT NULL FK`; `tally_readback_id bigint NOT NULL FK`; `rule_set_version varchar(20) NOT NULL`; `outcome varchar(40) NOT NULL CHECK IN ('RECONCILED','RECONCILED_WITH_ACCEPTED_WARNINGS','FAILED_RECONCILIATION','RECONCILIATION_INCOMPLETE')`; `summary_json NOT NULL`; `evidence_artifact_id bigint NOT NULL FK`; `run_by`; `run_utc` | Append-only. |
| S7-7 | `tally_reconciliation_differences` | `difference_id bigint IDENTITY PK`; `run_id bigint NOT NULL FK`; `accounting_voucher_id bigint NULL FK`; `tally_actual_id bigint NULL FK`; `check_level varchar(20) NOT NULL CHECK IN ('COVERAGE','HEADER','ACCOUNTING','TAX','TENDER','INVENTORY','AGGREGATE','INTEGRITY')`; `difference_type varchar(30) NOT NULL CHECK IN ('MISSING','EXTRA','DUPLICATE','WRONG_COMPANY','AMOUNT_MISMATCH','TAX_MISMATCH','LEDGER_MISMATCH','QUANTITY_MISMATCH','STATUS_MISMATCH','SOURCE_CHANGED','ACTUAL_CHANGED','AMBIGUOUS_MATCH','NOT_VERIFIABLE')`; `severity varchar(5) NOT NULL CHECK IN ('WARN','FAIL')`; `value_a nvarchar(500) NULL`; `value_b nvarchar(500) NULL`; `value_c nvarchar(500) NULL`; `delta decimal(19,4) NULL`; `rule_id varchar(40) NOT NULL`; `rule_version int NOT NULL`; `match_rationale nvarchar(500) NOT NULL`; `evidence_reference nvarchar(400) NOT NULL`; `required_action nvarchar(500) NOT NULL`; `accepted_by nvarchar(100) NULL`; `accepted_utc NULL`; `accepted_reason nvarchar(1000) NULL` | UPDATE allowed only to set the three `accept*` columns once (Owner); DELETE denied. |
| S7-7 | `tally_reconciliation_tolerances` | `tolerance_id int IDENTITY PK`; `rule_id varchar(40) NOT NULL`; `check_level varchar(20) NOT NULL`; `absolute_tolerance decimal(19,4) NOT NULL DEFAULT 0 CHECK (absolute_tolerance>=0)`; `version int NOT NULL`; `approved_by nvarchar(100) NOT NULL`; `approval_request_id bigint NOT NULL FK` | UNIQUE (`rule_id`,`check_level`,`version`); no percentage column; Owner writes only. |
| S7-8 | `tally_master_snapshots` | `snapshot_id bigint IDENTITY PK`; `tally_profile_id int NOT NULL FK`; `company_name_reported nvarchar(200) NULL`; `acquired_utc`; `acquired_by`; `raw_artifact_id bigint NOT NULL FK tally_artifacts` | Append-only. |
| S7-8 | `tally_masters` | `tally_master_row_id bigint IDENTITY PK`; `snapshot_id bigint NOT NULL FK`; `master_kind varchar(20) NOT NULL CHECK IN ('LEDGER','STOCK_ITEM','UNIT','GODOWN','VOUCHER_TYPE')`; `master_name nvarchar(200) NOT NULL`; `parent_name nvarchar(200) NULL`; `attributes_json NULL`; `tally_guid nvarchar(100) NULL` | UNIQUE (`snapshot_id`,`master_kind`,`master_name`); append-only. |
| S7-8 | `tally_master_creation_plans` | `plan_id bigint IDENTITY PK`; `tally_profile_id int NOT NULL FK`; `plan_json NOT NULL` (name, group, dependency order, required import option); `approval_request_id bigint NOT NULL FK` (type `TALLY_MASTER_CREATION`); `status varchar(30) NOT NULL CHECK IN ('DRAFT','APPROVED','EXPORTED_AWAITING_IMPORT','VERIFIED','PARTIAL','REJECTED')`; `payload_artifact_id bigint NULL FK`; `created_by`; `created_utc` | Owner writes only. |
| S7-9 | `accounting_voucher_lines` (7b, D17 = `INVENTORY`) | `voucher_line_id bigint IDENTITY PK`; `accounting_voucher_id bigint NOT NULL FK`; `line_number int NOT NULL`; `line_identifier nvarchar(80) NOT NULL`; `item_code nvarchar(80) NOT NULL`; `stock_item nvarchar(200) NOT NULL`; `quantity decimal(19,3) NOT NULL`; `unit nvarchar(20) NOT NULL`; `rate decimal(19,4) NOT NULL`; `taxable_amount decimal(19,4) NOT NULL`; `godown nvarchar(200) NULL` | UNIQUE (`accounting_voucher_id`,`line_number`); protected by `trg_accounting_vouchers_approved`. |
| S7-10 | `tally_backup_evidence` (7d) | `evidence_id bigint IDENTITY PK`; `tally_profile_id int NOT NULL FK`; `backup_kind varchar(20) NOT NULL CHECK IN ('TALLY_COMPANY','ENGINE_DATABASE')`; `file_name_safe nvarchar(200) NOT NULL` (name only, no path); `sha256 char(64) NOT NULL`; `taken_utc NOT NULL`; `restore_verified_utc NULL`; `recorded_by`; `recorded_utc`; `note nvarchar(500) NULL` | Append-only except one UPDATE setting `restore_verified_utc` (Owner). |
| — | `product_settings` (ALTER) | `tally_evidence_root nvarchar(400) NOT NULL DEFAULT 'C:\ProgramData\EtpReporting\TallyEvidence'` | Owner-editable via `IAdministrationService.SaveProductConfigurationAsync`. |
| — | `usp_accounting_batch_transition` | `@batch_id bigint, @to_status varchar(40), @reason nvarchar(1000), @evidence_artifact_id bigint NULL` | Transition table of task 3; `E-TRANSITION`; EXECUTE granted to `etp_store_manager`. |

### Phase 8 — Collections reconciliation, Stage 3 (4–8 weeks, after Phase 7)
Goal: for every store, business date and tender mode the manager opens one Collections statement that states what money was due, what outside evidence (bank line, provider settlement, cash count, deposit) actually arrived, what is still outstanding and why — residuals never lost, differences never guessed, no sale ever edited to agree, and every accounting consequence routed through a Phase 7 batch.

Prerequisites:
- Decisions recorded OPEN in Section 3 now, frozen by the owner before slice 8b starts (8a builds contracts, validation and blocking states without them): **D19** channels and providers in scope per store (cash, UPI direct, Airpay, PhonePe, card acquirer, Paytm if used), one sanitised sample per bank account and provider with the meaning of every amount and date column, whether each provider file is gross-with-deductions or already net, and whether it lists transactions or only settlement summaries; **D20** settlement window per channel (cash deposits included), overdue threshold and escalation timing, cash shortage/excess tolerance and investigation policy; **D21** who approves write-offs, shortage resolutions and adjustment proposals, and whether an accounting-reviewer role exists. **Proposals for the owner, not yet registered:** D22 cash custody policy (approved movement types, who records each, which tender codes are physical till cash), D23 accounting treatment of provider fees, chargebacks and refunds (ledgers, GST on fees; depends on D15/D16), D24 period-close cadence and protection rule. Until the owner accepts them, tasks 6, 7, 14 and 17 treat these as sub-questions of D20/D21 and the screens cite "D20/D21 (custody / fee / period sub-question)"; if accepted, the three rows are added to Section 3 in the same plan revision and the citations change to D22–D24. Missing policy blocks only its own path; the screen names the D-number.
- Environment: the Phase 2 no-code task done — one month of sanitised bank statement exports, Airpay and PhonePe settlement reports, card acquirer statement (Paytm if used) under `..\ETP Source Data\collections-samples\<account or provider key>\` (never in the repo); synthetic fixtures under `tests-dotnet/fixtures/collections-sample/` (two stores, five business days, one shared bank account, ≤ 50 rows per file, invented names, masked references, no real account or UTR numbers; each bank statement also as a CSV twin of the XLSX; a `tally-readback/` folder in the Phase 7 manual-export shape) with hand-worked `expected-till.csv`, `expected-bridge.csv`, `expected-matches.csv`, `expected-residuals.csv`, `expected-cases.csv`, `expected-cycle.csv` written before the code (expected values never come from the implementation); the Phase 7 TEST company and profile for A8.13 on the audit PC.
- From earlier phases: Phase 1 `reporting_sales_tenders`, `sales_invoices`, `tender_modes` (D11, Owner-editable), current-file typed `etp_r008`/`etp_r009`/`etp_r014`/`etp_r020`/`etp_r029`, `import_files` period/supersede columns, `import_batches`, `source_lineage`, `import_row_outcomes`, `source_documents` + `ManagedDocumentRepository`, `ImportProfileMatcher.CreateHeaderSignature`; Phase 2 cash book with carry-forward, per-mode columns and the "cash sent for deposit / bank credit confirmed" pair; Phase 3 shell (`TaskNavigation` table, Today · Import · Reports · Stock · Settings) and the single Exceptions screen with filter chips; Phase 4 SQL day lock, folder ACLs, explicit Store Manager grants; Phase 5 Approvals queue, `controlled_adjustments` with the `ADJUSTMENT` mapping fixed, one Prepare → Review → Export accounting screen, `accounting_batches` statuses `DRAFT` / `BLOCKED` / `APPROVED_READY` / `EXPORTED_AWAITING_IMPORT`; Phase 7 `tally_components` (roles include `JOURNAL`, the existing Journal voucher path of `TallyXmlExportService`; `RECEIPT` reserved for this phase), component statuses `RECONCILED` / `DIFFERENCE` / `OUTCOME_UNKNOWN`, `tally_reconciliation_differences.difference_type`, `product_settings.tally_evidence_root`. If Phase 7's final text renames any of these, Phase 8 follows that text.

Tasks (slices 8a–8d, numbering continuous). Conventions for every task: money `decimal(19,4)`; governed tables carry `modified_by`, `modified_utc`, `change_reason nvarchar(500) NOT NULL` and a `_history` trigger as `manual_operational_inputs`; NULL means "not known", never zero; THROW numbers are the next unused 51xxx; every new table gets Phase 4-style explicit Store Manager grants, `DENY DELETE` to Store Manager, Viewer read-only; D3 columns (`customer_*`, `etp_r020.creditcardno`) are never selected by any Phase 8 query, grid, export or diagnostic. **One next-free migration per slice** — `00NN_collections_contracts.sql` (8a), `00NN_collections_matching.sql` (8b), `00NN_collections_workflow.sql` (8c), `00NN_collections_accounting.sql` (8d); an object a later slice needs goes in that slice's file, never into a committed one (rule 9); every CHECK-list extension (`CK_automation_runs_type`, `CK_operational_audit_type`, `CK_source_documents_source_type`, `CK_tender_modes_mode`) is a DROP CONSTRAINT / ADD CONSTRAINT with the full list in the slice that needs it. `docs/03_DATABASE_SCHEMA.md` and `PhaseZeroSqlTests.All_migrations_apply_once_…` updated per migration. Role refusals are tested as boundary tests (pattern `DistributionServiceBoundaryTests`, injected `ApplicationRole`), because a SQL integration test runs under one Windows login; every gated service writes its `Blocked` audit row through `OperationalAuditRepository.RecordAsync` before throwing.

**8a — Data contracts and imports**
1. **Masters and settlement rules** (contract `src/Etp.Reporting.Application/Collections/CollectionsContracts.cs`: `ICollectionsSetupService` with `SaveBankAccountAsync(BankAccountDraft, reason)`, `SaveProviderAsync(ProviderDraft, reason)`, `SaveMerchantIdAsync(MerchantIdDraft, reason)`, `SavePolicyAsync(CollectionPolicyDraft, reason)`; records `BankAccountDraft(AccountKey, DisplayName, BankName, AccountLast4?, StoreCode?, Active)`, `ProviderDraft(ProviderKey, DisplayName, Mode, SettlesVia, StatementKind, Granularity, BankAccountId?, RoundingTolerance?, Active)`, `MerchantIdDraft(ProviderKey, MerchantId, TerminalId?, StoreCode)`, `CollectionPolicyDraft(StoreCode?, Mode, ProviderKey?, SettlementWindowDays?, OverdueAfterDays?, CashTolerance?, EffectiveFrom, EffectiveTo?)`; `Infrastructure.SqlServer/CollectionsSetupRepository.cs` + `SqlServerCollectionsSetupService.cs`, every write `RequireOwnerAsync`).
   - `dbo.bank_accounts(bank_account_id int IDENTITY PK, account_key varchar(30) NOT NULL UNIQUE CHECK (account_key NOT LIKE '%[^A-Z0-9_]%'), display_name nvarchar(100) NOT NULL, bank_name nvarchar(100) NOT NULL, account_last4 char(4) NULL, store_code varchar(30) NULL FK stores (NULL = shared by both stores), currency_code char(3) NOT NULL DEFAULT 'INR', active bit NOT NULL DEFAULT 1, governed columns)`. `account_key` is the only identifier ever used in folder paths; no column for a full account number exists anywhere.
   - `dbo.collection_providers(provider_key varchar(30) PK, same CHECK, display_name nvarchar(100) NOT NULL, mode varchar(30) NOT NULL CHECK IN ('Cash','Card','UPI','CN','Gift Card','Bank','TC','Service Cash','Service Card','Service UPI') — the ten D11 modes verbatim, settles_via varchar(10) NOT NULL CHECK IN ('BANK','NONE'), statement_kind varchar(25) NOT NULL DEFAULT 'UNKNOWN' CHECK IN ('GROSS_WITH_DEDUCTIONS','NET','UNKNOWN'), granularity varchar(20) NOT NULL DEFAULT 'UNKNOWN' CHECK IN ('TRANSACTION','SETTLEMENT_SUMMARY','UNKNOWN'), bank_account_id int NULL FK, rounding_tolerance decimal(19,4) NULL, active bit, governed columns)`; seeds AIRPAY (UPI, BANK), PHONEPE (UPI, BANK), CARD_ACQUIRER (Card, BANK), PAYTM (UPI, BANK, `active=0`), CREDIT_NOTE (CN, NONE), GIFT_CARD (Gift Card, NONE); `statement_kind` and `granularity` stay `UNKNOWN` until D19.
   - `dbo.provider_merchant_ids(provider_key FK, merchant_id nvarchar(50) NOT NULL, terminal_id nvarchar(50) NULL, store_code varchar(30) NOT NULL FK, UQ(provider_key, merchant_id, terminal_id))` — store routing only by confirmed configuration, never by a display name.
   - `ALTER TABLE dbo.tender_modes ADD provider_key varchar(30) NULL FK collection_providers, is_till_cash bit NOT NULL DEFAULT 0`. If `CK_tender_modes_mode` does not yet admit the D11 modes `Gift Card` and `Bank` at Phase 8 start, this migration drops and re-adds it with the ten D11 values (0019 untouched). UPDATE `provider_key`: PAYMENTTYPE25 → AIRPAY; PHONEPE and PAYMENTTYPE20 → PHONEPE; PAYTM → PAYTM; CARD and SERVICE_CARD → CARD_ACQUIRER; CREDITNOTE_REDEEM and ISSUED_CREDITNOTE → CREDIT_NOTE; GIFTCARD → GIFT_CARD (mode set to `Gift Card` per D11); every other code (BHIMUPI, BHARATPE, RAZORPAY, PAYMENTTYPE21, PAYMENTTYPE23, …) stays NULL and is listed in `PHASE-8-REPORT.md` for D19. UPDATE `is_till_cash=1` for CASH and CASH_REFUND; NO_REFUND stays 0 (forfeited part of a return — no cash leaves the till); ROUND_OFF is set 1 only if the fixture check recorded in `PHASE-8-REPORT.md` shows R022 CASH + ROUND_OFF = amount tendered, else 0. Trigger `trg_tender_modes_provider_mode`: a row's `mode` must equal its provider's `mode` (THROW "The provider settles a different payment mode."). The Phase 1 tender modes screen gains Provider and "Physical cash" columns.
   - `dbo.collection_policies(policy_id int IDENTITY PK, store_code varchar(30) NULL FK, mode varchar(30) NOT NULL, provider_key varchar(30) NULL FK, settlement_window_days int NULL, overdue_after_days int NULL, cash_tolerance decimal(19,4) NULL, effective_from date NOT NULL, effective_to date NULL, governed columns)` shipped **empty**; no default window, threshold or tolerance exists in code. `CollectionPolicyResolver` (Reporting, pure) picks the most specific row (store+provider > store+mode > mode) effective on the business date; none → "Not set (D20)".
   - Settings → "Collections setup" (Owner; Store Manager and Viewer read-only) with tabs Bank accounts · Payment providers · File layouts (task 2) · Settlement rules. Empty rules read "Settlement rule not set — ask the owner (D20)".
   - Tests: boundary `CollectionsSetupServiceBoundaryTests` — injected `ApplicationRole.StoreManager` save throws `UnauthorizedAccessException` "Owner permission is required." after the service wrote `operational_audit` `CollectionsSetup`/`Blocked` (fake gateway asserts); SQL `CollectionsSetupSqlTests` — lowercase `account_key` rejected by CHECK; a UPI tender code pointed at CARD_ACQUIRER is refused by `trg_tender_modes_provider_mode`; a fresh database has zero `collection_policies` rows.
2. **File layouts (unknown formats as Owner-configured column maps).** `dbo.statement_layouts(layout_id int IDENTITY PK, layout_key varchar(30) UNIQUE, layout_kind varchar(10) CHECK IN ('BANK','PROVIDER'), bank_account_id int NULL FK, provider_key varchar(30) NULL FK (CHECK exactly one non-null), header_signature_sha256 char(64) hex CHECK, sheet_name nvarchar(100) NULL, header_row int NOT NULL, skip_footer_rows int NOT NULL DEFAULT 0, mapping_json nvarchar(max) CHECK (ISJSON(mapping_json)=1), version int NOT NULL, active bit, governed columns; UQ(layout_kind, header_signature_sha256, bank_account_id, provider_key); filtered UQ (bank_account_id) WHERE layout_kind='BANK' AND active=1 — one active layout per bank account, so one statement can never exist twice under two formats)`. Header signature = `ImportProfileMatcher.CreateHeaderSignature` verbatim (no second algorithm).
   - Allowed JSON keys, validated by `Etp.Reporting.Import/Collections/StatementLayout.cs` (`StatementLayout.Parse` throws on any unknown key): BANK — `value_date`, `posting_date` (at least one), `narration`, `reference`, `credit`, `debit`, `signed_amount`, `dr_cr_flag` + `credit_flag_values[]`, `running_balance`, `date_format`, `decimal_separator`, `thousands_separator`; PROVIDER — `txn_reference`, `invoice_reference`, `merchant_id`, `terminal_id`, `approval_code`, `txn_datetime`, `datetime_format`, `datetime_utc` (true|false, default false), `settlement_date`, `settlement_id`, `bank_reference`, `status`, `status_failed_values[]`, `line_kind_column` + `line_kind_values{}` (file words → the `line_kind` list in task 4), `line_kind_default` (SALE or UNKNOWN; used when no `line_kind_column`), `line_kind_from_sign` (true|false: a negative gross on a SALE row becomes REFUND only when true; otherwise it is a `ParseError` row, never silently a refund), `gross`, `fee`, `tax_on_fee`, `net`, `fee_source` ∈ `COLUMN|LINE|NONE` (a fee is read from the fee column of a SALE row or from FEE rows, never both).
   - A BANK layout must map `credit`+`debit`, or `signed_amount`, or `signed_amount`+`dr_cr_flag`; a PROVIDER layout must map `gross` or `net`.
   - `StatementLayoutReader.Read(stream, layout)` (Import project; OpenXml for `.xlsx`, own reader for `.csv`) returns `NormalisedStatementRow` records plus `raw_cells` per row; amounts are stored as absolute values with direction carried by `line_kind` (bank: by credit/debit column).
   - Screen "File layouts" (Owner): pick a sample file → header cells shown as a row → one dropdown per key → "Test on this file" shows the first 20 normalised rows with unparsed cells highlighted → Save; saving with no amount convention or no date key is refused inline.
   - Tests: unit `StatementLayoutReaderTests` over the synthetic fixtures for every convention (two amount columns, signed amount, DR/CR flag; `dd/MM/yyyy` and `dd-MMM-yy`; Indian grouping; a negative-refund CSV with and without `line_kind_from_sign`); a row whose date or amount fails to parse is returned with `ParseError` text, never as zero; an unknown key is refused; two layouts sharing one signature both resolve and are both offered.
3. **Bank statement import.** `dbo.bank_statement_lines(bank_line_id bigint IDENTITY PK, bank_account_id int NOT NULL FK, layout_id int NOT NULL FK statement_layouts, import_file_id bigint NOT NULL FK import_files, source_lineage_id bigint NOT NULL FK UNIQUE, value_date date NULL, posting_date date NULL, CHECK (value_date IS NOT NULL OR posting_date IS NOT NULL), narration nvarchar(500) NULL, bank_reference nvarchar(100) NULL, debit_amount decimal(19,4) NULL, credit_amount decimal(19,4) NULL, CHECK (exactly one non-null and > 0), running_balance decimal(19,4) NULL, line_identity char(64) hex CHECK, is_current bit NOT NULL DEFAULT 1, classification varchar(25) NOT NULL DEFAULT 'UNCLASSIFIED' CHECK IN ('UNCLASSIFIED','UPI_SETTLEMENT','CARD_SETTLEMENT','CASH_DEPOSIT','CUSTOMER_TRANSFER','CHARGEBACK_DEBIT','BANK_CHARGE','REVERSAL','NON_SALES','INTERNAL_TRANSFER'), classified_by nvarchar(100) NULL, classified_utc datetime2(3) NULL, classification_reason nvarchar(500) NULL, UQ(bank_account_id, line_identity))`; **no day-lock trigger** — bank facts are dated by the bank, not by the shop. `line_date` = `COALESCE(value_date, posting_date)` everywhere (identity, periods, windows).
   - `Etp.Reporting.Import/Collections/LineIdentity.cs`: `line_identity = SHA256(account_key | line_date yyyy-MM-dd | UPPER(TRIM(bank_reference)) — or, when the layout maps no reference, the narration trimmed, internal whitespace collapsed to one space, upper-cased | credit F4 invariant or empty | debit F4 invariant or empty | ordinal)`, fields joined by `|`, `ordinal` = 1-based position among lines in the same file whose other fields are identical; `running_balance` and `posting_date` are excluded so the CSV and XLSX export of one statement yield identical identities (unit test asserts it). Amount + date alone never identify a line.
   - `internal sealed class CollectionsImportPersistence(string connectionString)` (Infrastructure.SqlServer) writes `import_batches` (new row, status Completed), `import_files`, `source_lineage(import_file_id, sheet_name = the layout sheet name or 'CSV', source_row_number, source_record_type = 'BANK_LINE' | 'PROVIDER_LINE')` and `import_row_outcomes` (NEW / ALREADY_PRESENT / INVALID) directly with parameterised SQL in one transaction. It re-implements — does not call — the three Phase 1 decisions of `PhaseOneImportPersistence.PlanImportAsync` (which is `private static` and needs an ETP profile): (1) same `source_sha256` + `report_code` + account already present → "Already imported"; (2) every `line_identity` of the file already present for the account → "Duplicate content", zero rows; (3) otherwise MERGE on `line_identity` (existing `bank_line_id`s and their allocations survive) and, if the new file contains every identity of an earlier current file for the same account, set that file `is_superseded=1`, `superseded_by_import_file_id`, `restatement_reason='Later statement export'` directly — never via `prepare_import_restatement`, which refuses LOCKED days and deletes facts. An identity that disappears sets `is_current=0`; if it carries allocations the pass suspends the match (task 8) and opens a `MISSING_EVIDENCE` case. `SqlServerTransactionalImportStore` is not touched. Overlap guard: when the file's date range overlaps a range already covered for the account and any new identity has the same (`line_date`, amount, credit/debit) as a current line with a different identity, the import stops with "Possible duplicate lines — n matches", lists them, and the Owner chooses Restate (supersede the earlier file) or Import as new; the choice is audited. Overlap without such collisions is normal for bank exports and is never rejected.
   - `import_files` row per file: `report_code='BANK_STATEMENT'`, **`store_code` NULL** (the store lives on `bank_accounts.store_code`; `trg_import_files_protect_locked` joins on `import_files.store_code`, so it never fires for statement files and the supersede UPDATE passes — the 0017 trigger is neither altered nor disabled), `business_date` NULL, `period_start/end` = min/max `line_date`, `data_truth_version=1`.
   - Classification at import: none — every line, debit or credit, lands `UNCLASSIFIED`. The pass (task 16) sets `UPI_SETTLEMENT`/`CARD_SETTLEMENT` when an R1 PROVIDER_BANK match is accepted, `CASH_DEPOSIT` on an accepted DEPOSIT_BANK match, `CHARGEBACK_DEBIT`/`BANK_CHARGE` when a debit's reference equals a provider CHARGEBACK/FEE line reference (R1 rule); `IBankLineService.ClassifyAsync(bankLineId, classification, reason)` (Store Manager) allows `CASH_DEPOSIT`, `CUSTOMER_TRANSFER`, `REVERSAL`, `NON_SALES`, `INTERNAL_TRANSFER`; only `NON_SALES` and `INTERNAL_TRANSFER` are excluded from matching.
   - The file is retained with `ManagedDocumentRepository.StoreAsync` (`.csv` added to `AllowedExtensions`; message becomes "Select a supported document, spreadsheet or CSV file.") under `<collections evidence root>\BankSources\<account_key>\<yyyy>\<MM>\<sha256>.<ext>` and registered in `source_documents` with new `source_type` values `'BANK_STATEMENT','PROVIDER_STATEMENT','COLLECTION_EVIDENCE'` (`CK_source_documents_source_type` dropped and re-added).
   - Import → "Bank and settlement files" tab (Store Manager): drop file → layouts matched by header signature; exactly one → account/provider shown read-only; several → "Which account is this statement for?" is a mandatory choice and Import stays disabled until chosen; the chosen `layout_id` is written on every line → Import → per-file result list (Imported n lines / Already imported / Duplicate content / Possible duplicate lines / Unknown layout — ask the owner to add a layout). Audit type `CollectionsImport`.
   - Tests: SQL `BankStatementImportSqlTests` — importing the fixture statement twice yields the same `COUNT(*)` and `SUM(credit_amount)`; the CSV twin after the XLSX adds zero lines; a superset with three extra lines adds exactly three and keeps earlier ids; a statement whose range covers a LOCKED day imports and its superset supersedes it with no 51021; two identical lines in one file get ordinals 1 and 2 and both persist; every line lands `UNCLASSIFIED`; the same period re-imported through a second layout for the account is refused by the filtered UQ / overlap guard.
4. **Provider settlement import.** `dbo.provider_settlement_lines(provider_line_id bigint IDENTITY PK, provider_key FK, layout_id int NOT NULL FK, import_file_id FK, source_lineage_id FK UNIQUE, store_code varchar(30) NULL FK (resolved from provider_merchant_ids, else NULL and flagged "store unknown"), merchant_id nvarchar(50) NULL, terminal_id nvarchar(50) NULL, provider_txn_reference nvarchar(100) NULL, invoice_reference nvarchar(80) NULL, approval_code nvarchar(30) NULL, txn_datetime datetime2(0) NULL, txn_business_date date NULL, settlement_date date NULL, settlement_id nvarchar(100) NULL, bank_reference nvarchar(100) NULL, provider_status nvarchar(30) NULL (raw text), is_failed bit NOT NULL DEFAULT 0 (set at import from status_failed_values), line_kind varchar(15) NOT NULL DEFAULT 'UNKNOWN' CHECK IN ('SALE','REFUND','CHARGEBACK','FEE','TAX_ON_FEE','HOLDBACK','RELEASE','ADJUSTMENT','UNKNOWN'), gross_amount, fee_amount, tax_on_fee_amount, net_amount decimal(19,4) NULL each (NULL when the file has no such column; stored as absolute values, direction is the line_kind; only ADJUSTMENT keeps the file sign in gross_amount), raw_json nvarchar(max) NOT NULL CHECK ISJSON (every raw cell by header), line_identity char(64), is_current bit, UQ(provider_key, line_identity))`.
   - Timestamps are read as India Standard Time wall-clock; a layout with `datetime_utc: true` is converted with `TimeZoneInfo.FindSystemTimeZoneById("India Standard Time")`; `txn_business_date` = the IST calendar date, never the upload date. A transaction after midnight belongs to the next business date (task 9 R2b handles the early-morning shift; otherwise the manager matches it manually) — recorded in `PHASE-8-REPORT.md`.
   - Same identity, duplicate, supersede, overlap and retention rules as task 3 (`SettlementSources\<provider_key>\<yyyy>\<MM>\<sha256>.<ext>`); `import_files.report_code='PROVIDER_STATEMENT'`, `store_code` NULL, `period_start/end` = min/max `COALESCE(settlement_date, txn_business_date)`; no day-lock trigger.
   - `UNKNOWN` line kinds are imported, shown and excluded from matching — never guessed. Until D19 approves a real layout for a provider, the Import tab shows "Layout not approved for <provider> (D19)" for that provider and only the synthetic layouts work in tests; nothing claims a live connector.
   - Tests: SQL `ProviderSettlementImportSqlTests` — Airpay-shaped NET fixture leaves `fee_amount NULL` and fills `net_amount`; card-shaped GROSS fixture creates `FEE` and `TAX_ON_FEE` lines; a status in `status_failed_values` keeps its mapped kind with `is_failed=1`; a REFUND row stores a positive `gross_amount`; `raw_json` holds every header; a file covering LOCKED days imports.
5. **Collection obligations (one business fact, derived — no second copy of tenders).** `dbo.collection_obligations(obligation_id bigint IDENTITY PK, store_code FK, business_date date NOT NULL, mode varchar(30) NOT NULL, provider_key varchar(30) NULL FK, source_kind varchar(15) CHECK IN ('SALES_TENDERS','SERVICE_INPUT'), direction varchar(3) NOT NULL CHECK IN ('IN','OUT'), expected_amount decimal(19,4) NOT NULL CHECK (>= 0) (ABS of the netted sum; OUT when the netted sum is negative — refunds through the provider exceeded sales), currency_code char(3) DEFAULT 'INR', evidence_expected bit NOT NULL, basis_locked bit NOT NULL (day LOCKED at compute time), snapshot_version int NOT NULL, snapshot_sha256 char(64), computed_utc, computed_by, due_date date NULL (= business_date + settlement_window_days; NULL while D20 unset), state varchar(20) CHECK IN ('NO_OUTSIDE_EVIDENCE','UNMATCHED','PARTIALLY_MATCHED','MATCHED','PENDING','OVERDUE','DISPUTED'), state_as_of date NOT NULL, disputed_reason nvarchar(500) NULL, is_current bit NOT NULL, superseded_by_obligation_id bigint NULL FK self; filtered UQ (store_code, business_date, mode, provider_key, source_kind) WHERE is_current=1)`; **no day-lock trigger**; settlement never requires `daily_reporting_days` to change.
   - `CollectionsReconcilePass.RecomputeObligationsAsync` (task 16) reads `reporting_sales_tenders` t JOIN `sales_invoices` i ON i.sales_invoice_id = t.sales_invoice_id JOIN `tender_modes` m ON m.source_tender_code = t.tender_type (the `LoadAccountingSourceAsync` join), excluding rows whose `m.mode` starts with `Service`, grouped by i.store_code, i.transaction_date, m.mode, m.provider_key (source-signed sums: refunds reduce the day exactly once); `SERVICE_INPUT` rows use mode `Service Cash` / `Service Card` / `Service UPI` (the `tender_modes` rows for codes `SERVICE_CASH`, `SERVICE_CARD`, `SERVICE_UPI`) and that row's `provider_key`, `expected_amount` = the input value (NULL input → no row: missing is not zero). A tender code with no `tender_modes` row yields **no** obligation and one `MISSING_EVIDENCE` case "Tender code not mapped: <code>".
   - `evidence_expected = 0` only when the mode is in (`Cash`, `CN`, `TC`, `Gift Card`, `Service Cash`) or the mapped provider has `settles_via='NONE'`; for `UPI`, `Card`, `Bank`, `Service Card`, `Service UPI` it is 1 even when `provider_key` is NULL, and the pass opens one `MISSING_EVIDENCE` case per (store, tender code) "Provider not set for <code> (D19)" — no matching rule runs for such rows, but the money stays visible as outstanding. Mode `Cash` is settled through the till (task 7) and deposits (R3), never by obligation allocations.
   - `snapshot_sha256` = SHA-256 over the sorted list of `(document_number, invoice_year, tender_type, source_amount F4 invariant)` tuples of the eligible tenders of the key plus the service input values, joined by `|` — business content, not lineage, so a routine re-export with new `source_lineage_id`s leaves every hash unchanged. When the hash changes for a current row (later R022 export with different content, restatement, reopen): the row gets `is_current=0`, `superseded_by_obligation_id` = the new row (`snapshot_version+1`); when Σ ACCEPTED allocations ≤ new `expected_amount` the pass writes the original match `REVERSED` and a new ACCEPTED match `rule_code='SUPERSESSION'`, `rule_version=1`, `proposed_by='ENGINE'`, `decision_reason='Sales restated; allocation carried to obligation <new id>'`, copying every allocation to the new obligation id (allocations themselves are immutable, task 8); otherwise the old match stays ACCEPTED on the superseded row and one `MISSING_EVIDENCE` case "Sales for this day changed after matching — move or reverse the allocations" opens; every supersession writes audit `CollectionObligation`. The statement compares the stored hash with the live hash on load and shows "Sales changed after this statement — refresh" when they differ.
   - Residual = `expected_amount` − Σ ACCEPTED allocations whose EXPECTED and ACTUAL sources are all `is_current=1`; when a source becomes non-current the pass sets the match `SUSPENDED` (task 8), the residual grows and the `MISSING_EVIDENCE` case names the `match_id`; re-appearance of the identity restores ACCEPTED.
   - `ObligationStateResolver.Resolve(row, allocations, policy, DateOnly asOf)` (Reporting, pure; `asOf` = the pass's run date in IST, stored in `state_as_of`), in this order: NO_OUTSIDE_EVIDENCE if `evidence_expected=0`; DISPUTED if an open `REFUND_CHARGEBACK`/`FAILED_REVERSED_PAYMENT` case points at it; MATCHED if residual = 0; OVERDUE if residual > 0, `due_date` set and `asOf > due_date + overdue_after_days`; PENDING if residual > 0, `due_date` set, no allocation and `asOf ≤ due_date + overdue_after_days`; PARTIALLY_MATCHED if residual > 0 and allocations > 0; else UNMATCHED. NO_OUTSIDE_EVIDENCE rows are excluded from `collection_residuals`, `outstanding_total`, ageing, the AWAITING/OVERDUE case rules and the period-close checklist; the Expected-by-mode panel shows them as "No outside evidence for this mode" and, for Cash, "Checked by the till (panel 3)". Screen label for `expected_amount`: "tender due"; OUT rows read "refund due to customers via <provider>".
   - Tests: unit `ObligationSnapshotTests` (same tenders in a different order → same hash; same tenders re-imported with new lineage ids → same hash), `ObligationStateResolverTests` (all seven outcomes; `asOf` on both sides of the overdue boundary; a Cash and a CN row → NO_OUTSIDE_EVIDENCE contributing 0 to outstanding; a UPI row with NULL provider → UNMATCHED with `evidence_expected=1`; an OUT row); SQL `CollectionObligationSqlTests` (restating one R022 file for a matched day with changed content: SUPERSESSION match when it fits, else old match stays and one case opens; identical re-import creates no superseded rows; withdrawing a matched bank line moves the obligation from MATCHED to PARTIALLY_MATCHED/UNMATCHED with residual = the withdrawn amount).
6. **Cash counts and cash movements (custody and banking views).** `dbo.cash_counts(cash_count_id bigint IDENTITY PK, store_code FK, business_date date NOT NULL, count_kind varchar(10) CHECK IN ('OPENING','CLOSING','SPOT'), counted_at datetime2(0) NOT NULL, counted_by nvarchar(100) NOT NULL, total_amount decimal(19,4) NOT NULL, denominations_json nvarchar(max) NULL CHECK ISJSON, remarks nvarchar(500) NULL, governed columns; filtered UQ (store_code, business_date, count_kind) WHERE count_kind IN ('OPENING','CLOSING'))`.
   - `dbo.cash_movements(cash_movement_id bigint IDENTITY PK, store_code FK, business_date date NOT NULL (day the cash moved; for DEPOSIT_CONFIRMED = event_date, the bank credit date), movement_type varchar(20) CHECK IN ('OPENING_FLOAT','FLOAT_RETAINED','CASHIER_HANDOVER','SAFE_TRANSFER','DEPOSIT_SENT','DEPOSIT_CONFIRMED','PETTY_CASH_IN','PETTY_CASH_OUT','EXPENSE','ADJUSTMENT'), amount decimal(19,4) NOT NULL CHECK (amount > 0), adjustment_sign char(1) NULL CHECK IN ('+','-') (ADJUSTMENT only), reference nvarchar(100) NULL (deposit slip or bag number; joins etp_r008.deposit_slipno / bankdepositnumber and etp_r029.bankdepositno for the ETP cross-check), bank_account_id int NULL FK, related_movement_id bigint NULL FK self, event_date date NULL, confirmation_source varchar(10) NULL CHECK IN ('MANUAL','STATEMENT') (DEPOSIT_CONFIRMED only), bank_line_id bigint NULL FK bank_statement_lines, evidence_document_id bigint NULL FK source_documents, status varchar(10) NOT NULL DEFAULT 'RECORDED' CHECK IN ('RECORDED','VOID'), void_reason nvarchar(500) NULL, governed columns)`; trigger `trg_cash_movements_deposit_link`: a `DEPOSIT_CONFIRMED` must point at a `DEPOSIT_SENT` of the same store, and a `DEPOSIT_SENT` may carry at most one RECORDED confirmation (THROW).
   - Lock rule: `trg_cash_movements_protect_locked` refuses every type on a LOCKED day **except `DEPOSIT_CONFIRMED`** (banking-side facts arrive after the lock); `trg_cash_counts_protect_locked` refuses all — shop-side facts stay inside the Phase 4 freeze.
   - `ICashCustodyService` / `SqlServerCashCustodyService`: `RecordCountAsync`, `RecordMovementAsync`, `VoidMovementAsync(reason)`, `ConfirmDepositAsync(depositSentId, eventDate, reference, bankAccountId)` (the Phase 2 manual "bank credit confirmed" tick, `confirmation_source='MANUAL'`, kept for accounts with no statement import); Store Manager records every type except `ADJUSTMENT` (Owner; the custody policy may widen).
   - Migration order: create tables → back-fill → create the two `*_protect_locked` triggers last (historical days are LOCKED; the 0017 DISABLE/ENABLE pattern is unnecessary because the triggers do not yet exist). Back-fill from `manual_operational_inputs`: rows with `numeric_value = 0` are not migrated (a zero movement is no movement) and their count is written to `operational_audit` `CashMovement`/`Succeeded`; `CASH_DEPOSIT > 0` → `DEPOSIT_SENT`, `EXPENSES > 0` → `EXPENSE`; negative `CASH_DEPOSIT`/`EXPENSES` → `ADJUSTMENT` with `amount = ABS`, `adjustment_sign` from the sign, `change_reason='Migrated negative manual input — review'`; `CASH_ADJUSTMENT <> 0` → `ADJUSTMENT` (`ABS`, sign); `CLOSING_CASH_COUNTED` → `cash_counts` CLOSING (`counted_by=entered_by`, `counted_at=entered_utc`); each `change_reason='Migrated from manual input'`; the Phase 2 "cash sent for deposit / bank credit confirmed" rows become `DEPOSIT_SENT`/`DEPOSIT_CONFIRMED` (MANUAL) pairs; then `manual_input_definitions.is_active=0` for `CASH_DEPOSIT`, `EXPENSES`, `CASH_ADJUSTMENT`, `CLOSING_CASH_COUNTED` (the `SALES_TARGET` precedent); `OPENING_CASH` stays active as the editable-with-reason override. Finalise readiness (`DailyReportingWorkflowRepository`, the Close-day tab) replaces the two retired required inputs with one rule: a `cash_counts` row with `count_kind='CLOSING'` for the store-day is required ("Count the till before closing the day"); deposits and expenses are no longer required to finalise; `SqlServerDailyWorkflowService` tests updated; `OperationalReportRepository.LoadCashReconciliationAsync` and the Phase 2 cash book read the new tables and use the same Blocked source.
   - Today → Cash gets "Count the till" (denomination grid, big number keys) and "Cash movement" (type, amount, slip number, photo attach) buttons. Audit types `CashCount`, `CashMovement`.
   - Tests: SQL `CashCustodySqlTests` — `DEPOSIT_CONFIRMED` on a LOCKED day succeeds, `EXPENSE` on it throws the new 51xxx; `DEPOSIT_CONFIRMED` without `related_movement_id` throws; a second confirmation for one `DEPOSIT_SENT` throws; back-fill on a test database loaded from `tests-dotnet/fixtures/etp-sample/` plus manual inputs inserted by the test (including a LOCKED historical day, a zero deposit and a negative adjustment) preserves every non-zero amount to the paisa and creates no row for zeros; Finalise without a CLOSING count is refused with "Count the till before closing the day" and succeeds with one.

**8b — Matching**
7. **Till roll-forward (versioned formula).** `Etp.Reporting.Reporting/Collections/CashTillPolicy.cs` (pure, `Version = 1`) owns the arithmetic: `Evaluate(CashTillInputs(decimal? Opening, string OpeningSource, decimal CashTenders, decimal? ServiceCash, bool ServiceCashRequired, decimal PettyIn, decimal Expenses, decimal PettyOut, decimal DepositsSent, decimal Adjustments, decimal? Counted))` → `CashTillResult(Expected?, Variance?, Outcome, PolicyVersion, BlockedReason?)`; `expected_closing = opening + cash tenders + service cash + PETTY_CASH_IN − EXPENSE − PETTY_CASH_OUT − DEPOSIT_SENT ± ADJUSTMENT`; cash tenders = source-signed sums of tender codes with `tender_modes.mode='Cash' AND is_till_cash=1` (CASH and CASH_REFUND; each counted once; NO_REFUND never moves the till); `opening` = the day's `OPENING_CASH` manual input if one exists (the Owner override, reason mandatory, shown "Opening overridden"), else today's `OPENING` count, else yesterday's `CLOSING` count, else the day's `OPENING_FLOAT` movement, else `Blocked("No opening cash")`; missing `SERVICE_CASH` blocks only when `manual_input_definitions.SERVICE_CASH.is_active=1` ("Service cash not entered"); `variance = counted_closing − expected_closing` (signed; negative = short); outcome `Balanced` / `Short` / `Excess` / `Blocked("Till not counted")` — never zero when not counted.
   - `CASHIER_HANDOVER`, `SAFE_TRANSFER`, `FLOAT_RETAINED` change custody, not the store total, and appear on the custody view only; deposits in transit = `DEPOSIT_SENT` rows (status RECORDED) with no RECORDED `DEPOSIT_CONFIRMED` and no ACCEPTED `DEPOSIT_BANK` match. `CashBalanceReconciliationService.Reconcile` becomes an adapter over `CashTillPolicy` (its const `Formula` deleted); `CashReconciliationReport` (`ReportQueryContracts.cs`) gains `int PolicyVersion, string OpeningSource, decimal DepositsInTransit`; the cash book footer shows "Formula v1".
   - Tests: unit table-driven from `expected-till.csv` (day 1: opening float 5,000 + cash 20,000 − expense 2,000 − deposit 15,000 = 8,000, count 7,900 → −100; a return day with NO_REFUND ≠ 0 changing expected closing by the cash refunded only; an override day; a two-deposit day; a not-counted day; a service-cash-missing day with the definition active and inactive).
8. **Match and allocation records.** `dbo.settlement_matches(match_id bigint IDENTITY PK, match_type varchar(20) CHECK IN ('OBLIGATION_PROVIDER','OBLIGATION_BANK','PROVIDER_BANK','DEPOSIT_BANK'), rule_code varchar(30) NOT NULL, rule_version int NOT NULL, status varchar(10) CHECK IN ('PROPOSED','ACCEPTED','SUSPENDED','REJECTED','REVERSED'), proposed_by nvarchar(100) NOT NULL ('ENGINE' or actor), proposed_utc, decided_by, decided_utc, decision_reason nvarchar(500) NULL, reversal_of_match_id bigint NULL FK self, recognised_period date NULL (first day of the month; task 17))` and `dbo.settlement_match_allocations(allocation_id bigint IDENTITY PK, match_id FK, side varchar(8) CHECK IN ('EXPECTED','ACTUAL'), source_table varchar(30) CHECK IN ('collection_obligations','provider_settlement_lines','bank_statement_lines','cash_movements','IMPLIED_DEDUCTION'), source_id bigint NULL (NOT NULL unless IMPLIED_DEDUCTION), component varchar(12) NOT NULL DEFAULT 'PRINCIPAL' CHECK IN ('PRINCIPAL','FEE','TAX_ON_FEE','REFUND','CHARGEBACK','HOLDBACK','RELEASE','ROUNDING'), rounding_sign char(1) NULL CHECK IN ('+','-'), invoice_reference nvarchar(80) NULL (task 9 R1b), allocated_amount decimal(19,4) CHECK (> 0))`. `IMPLIED_DEDUCTION` is allowed only with `component IN ('FEE','TAX_ON_FEE','ROUNDING')`, on the ACTUAL side, when the provider's `statement_kind='NET'`, capped by the obligation residual (CHECK + service).
   - Trigger `trg_settlement_allocations_no_overallocation` (AFTER INSERT): for every touched `(source_table, source_id, side)` the sum of allocations on PROPOSED, ACCEPTED and SUSPENDED matches may not exceed the source amount — `credit_amount` (or `debit_amount`) for bank lines, `expected_amount` for obligations, `amount` for cash movements, and for provider lines the amount of its kind (`SALE`/`REFUND`/`CHARGEBACK`/`HOLDBACK`/`RELEASE`/`ADJUSTMENT` → `COALESCE(gross_amount, net_amount)`; `FEE` → `fee_amount`; `TAX_ON_FEE` → `tax_on_fee_amount`); a NULL source amount refuses with THROW "This line has no amount to allocate"; exceeding → THROW "This receipt is already fully allocated"; `ROUNDING` allocations capped by the provider's `rounding_tolerance` (NULL = 0). The same provider line may be allocated once per side (ACTUAL in OBLIGATION_PROVIDER, EXPECTED in PROVIDER_BANK).
   - `MatchBalance.IsBalanced(match)` (Reporting, pure), enforced by the service on ACCEPT and by trigger `trg_settlement_matches_balanced` (fires on status → ACCEPTED): OBLIGATION_PROVIDER and OBLIGATION_BANK — Σ EXPECTED PRINCIPAL = Σ ACTUAL PRINCIPAL − Σ ACTUAL (REFUND, CHARGEBACK) + Σ ACTUAL (FEE, TAX_ON_FEE) [implied deductions] ± ROUNDING; PROVIDER_BANK — Σ EXPECTED PRINCIPAL + Σ EXPECTED RELEASE − Σ EXPECTED (FEE, TAX_ON_FEE, REFUND, CHARGEBACK, HOLDBACK) = Σ ACTUAL PRINCIPAL ± ROUNDING; DEPOSIT_BANK — Σ EXPECTED PRINCIPAL = Σ ACTUAL PRINCIPAL. An unbalanced match cannot be ACCEPTED ("The two sides do not agree").
   - `trg_settlement_allocations_immutable` (AFTER UPDATE, DELETE; THROW): allocations are never changed or deleted; matches are never deleted — a re-match writes `REVERSED` pointing at the original; only `status`, `decided_*`, `decision_reason`, `recognised_period` of a match may change.
   - View `dbo.collection_residuals` (source_table, source_id, store_code, date, amount, allocated, residual, oldest open date) over obligations with `evidence_expected=1`, bank lines not `NON_SALES`/`INTERNAL_TRANSFER`, provider lines, and `DEPOSIT_SENT` movements, counting only ACCEPTED allocations whose sources are all current.
   - Tests: SQL `SettlementAllocationSqlTests` — second allocation exceeding a bank credit by 0.01 throws; the same provider line fully allocated once per side succeeds; a FEE line allocated above `fee_amount` throws; an unbalanced OBLIGATION_PROVIDER match cannot become ACCEPTED; residual view equals the hand-computed CSV; UPDATE of `allocated_amount` throws.
9. **Deterministic matching rules.** `Etp.Reporting.Reporting/Collections/SettlementMatchingService.cs` (pure; inputs are candidate lists, output is proposals), `RuleSetVersion = 1`, run by the reconcile pass (task 16). Rules run in order R1 → R2 → R2b → R3 → R4 within one pass; every rule sees only sources whose residual after PROPOSED, ACCEPTED and SUSPENDED allocations is > 0, and R2 groups only lines not allocated by R1 in this or an earlier pass; a rule that would exceed a residual proposes nothing and opens no case. Scope: never across stores, never across bank accounts or providers, never beyond `rounding_tolerance`, never for `UNKNOWN` line kinds, never for bank lines classified `NON_SALES`/`INTERNAL_TRANSFER`, never for obligations with `provider_key` NULL and `evidence_expected=1` (they wait for D19).
   - **R1 REF_EXACT (a) PROVIDER_BANK** — group current provider lines by (`provider_key`, `settlement_id` else `bank_reference`); compute the group's bridge (task 10; `NotComputable` while `statement_kind='UNKNOWN'` → R1a does not run for that provider); when the group reference equals the bank line's `bank_reference` or is contained in its narration (trimmed, case-insensitive), |bridge net − `credit_amount`| ≤ `rounding_tolerance`, and the reference is unique on both sides (exactly one bank line, exactly one group) → **ACCEPTED** (`proposed_by='ENGINE'`, `decided_by='ENGINE'`) with one EXPECTED allocation per provider line (PRINCIPAL for SALE, component by kind for FEE/TAX_ON_FEE/REFUND/CHARGEBACK/HOLDBACK/RELEASE) and one ACTUAL allocation of `credit_amount`; the bank line is classified. A debit whose reference equals a provider CHARGEBACK/FEE line reference is accepted the same way and classified `CHARGEBACK_DEBIT`/`BANK_CHARGE`. The manager may reverse any engine acceptance with a reason. **(b) OBLIGATION_PROVIDER by approval code** — when exactly one current `etp_r020` row (`import_files.is_superseded=0`; key `store_code`, `invdate`, `approvalnumber`) equals the provider SALE line's (`store_code`, `txn_business_date`, `approval_code`) and exactly one provider line carries that code, the line's PRINCIPAL (`gross_amount` for GROSS files, `net_amount` for NET) is allocated to the obligation of (`etp_r020.store_code`, `invdate`, provider mode, `provider_key`); several R1b lines accumulate on one obligation; `settlement_match_allocations.invoice_reference` = `etp_r020.invnumber`; `creditcardno` is never selected. Whether `approvalnumber` is filled for UPI agencies is verified on the live R020 and recorded in the report — until then UPI relies on R2.
   - **R2 PROVIDER_SUM** — runs only when the provider's `granularity='TRANSACTION'`; groups SALE and REFUND lines of the same (`store_code`, `txn_business_date`, `provider_key`) with signed gross (SALE +, REFUND −) when every line has `gross_amount NOT NULL`, else (NET file without gross) with `net_amount`. Netted group equal to the obligation → PROPOSED with PRINCIPAL per SALE line and REFUND per refund line; smaller → partial, residual visible; larger → no proposal, one `MISSING_EVIDENCE` case "Provider reports more than the sales export for this day" naming the group. For a `net_amount` group that is the only settlement for the key, the engine adds one PROPOSED `IMPLIED_DEDUCTION` FEE allocation = obligation expected − Σ net, and opens one `DOCUMENTED_DEDUCTION` case whose `ADJUSTMENT_PROPOSED` resolution proposes `MDR_CHARGE`. For `granularity='SETTLEMENT_SUMMARY'` the obligation row reads "Provider file has no per-transaction lines — checked at bank level only" and only R1a applies. OUT obligations match REFUND lines (component REFUND) against |expected|. **R2b DAY_SHIFT** — when the exact-day pass leaves day D short by exactly the gross of unallocated SALE lines dated D+1 before 03:00 IST, propose them to D (PROPOSED, never ACCEPTED), `decision_reason` records the shift.
   - **R3 DEPOSIT_REF** — `DEPOSIT_SENT.reference` appears in a bank credit narration and amounts equal → PROPOSED `DEPOSIT_BANK`; on acceptance the service inserts `DEPOSIT_CONFIRMED` (`confirmation_source='STATEMENT'`, `bank_line_id`, `event_date` = `line_date`, `business_date` = `event_date`) or, if a MANUAL confirmation already points at the `DEPOSIT_SENT`, updates it to STATEMENT with `bank_line_id`/`bank_account_id`/`event_date` filled — never a second row; the bank line is classified `CASH_DEPOSIT`.
   - **R4 AMOUNT_DATE_UNIQUE** — exactly one candidate of the right scope with equal amount inside `[date, date + settlement_window_days]` → PROPOSED (OBLIGATION_BANK for direct-to-bank UPI included); runs only when D20 has set the window.
   - **Ambiguity, both directions** — one evidence line with two or more expected candidates of equal amount, or one expected item with two or more evidence lines of equal amount, inside the same scope and window, without a strong reference → nothing proposed, one `AMBIGUOUS_DUPLICATE_MATCH` case naming every candidate id. Two stores paying equal deposits into the shared account is the fixture case. Amount + date alone is never accepted by the engine.
   - `ISettlementMatchService` / `SqlServerSettlementMatchService`: Store Manager accepts, rejects or creates a manual match (`rule_code='MANUAL'`, reason required; balance enforced); reversing an ACCEPTED match whose EXPECTED-side obligation has a `collection_reviews` row in state APPROVED for the same store and business_date (any version) is Owner-only. Obligation state is re-derived after every change.
   - Tests: unit `SettlementMatchingServiceTests` from `expected-matches.csv` — UPI settled next day (R2 full); NET UPI day 50,000 gross, 49,850 net → MATCHED with implied deduction 150 and one case; card batch across two days (two partial allocations, residual per day; one UTR covering 12 SALE and 2 FEE lines → R1a ACCEPTED); a day with two R1b-matched lines and one unmatched → R2 proposes only the unmatched amount; a refund day netted; a provider-larger day → case; two equal bank credits (no proposal, one case); two stores' equal deposits into one account (no proposal, one case); unknown line kind ignored; R4 silent while no policy row; R2b shift.
10. **Settlement bridge.** `Etp.Reporting.Reporting/Collections/SettlementBridge.cs` `Compute(lines, statement_kind, fee_source)` → `expected_net = Σ gross(SALE) − Σ REFUND − Σ CHARGEBACK − Σ HOLDBACK − Σ FEE − Σ TAX_ON_FEE + Σ RELEASE ± ADJUSTMENT (stored sign)`, each component counted once by `line_kind` or by column per `fee_source`; `statement_kind='NET'` → the bridge uses `net_amount` and reports any mapped fee column as "informational, already inside net" (no second subtraction); `'UNKNOWN'` → `NotComputable("Provider statement kind not set (D19)")`. No tax rate is ever applied to a fee; a missing `tax_on_fee` column shows "not in file".
   - Bridge difference = `credit_amount` − `expected_net`; outside `rounding_tolerance` → no proposal, the residual stays on the provider group, one `MISSING_EVIDENCE` case "Bank credit differs from documented net by <amount>"; `DOCUMENTED_DEDUCTION` is never derived from a bank difference (fee lines are already inside `expected_net`).
   - Card settlement bridge view per provider and settlement date: gross due · deductions by kind · net expected · bank credit matched · residual.
   - Tests: unit from `expected-bridge.csv` (gross file 30,000 − 180 = 29,820, bank 29,820 → 0; bank 29,800 → difference −20 and no deduction case; net file; net file with an informational fee column; a negative-sign refund row stored positive; unknown kind → not computable).

**8c — Manager workflow**
11. **Exception cases.** `dbo.collection_cases(case_id bigint IDENTITY PK, case_key AS 'CC-' + RIGHT('000000' + CAST(case_id AS varchar(10)), 6) PERSISTED UNIQUE (the only identifier used in paths), store_code FK, business_date date NULL, mode varchar(30) NULL, provider_key NULL, case_type varchar(30) CHECK IN ('AWAITING_SETTLEMENT','OVERDUE_SETTLEMENT','CASH_SHORT_EXCESS','DOCUMENTED_DEDUCTION','FAILED_REVERSED_PAYMENT','REFUND_CHARGEBACK','UNIDENTIFIED_BANK_CREDIT','AMBIGUOUS_DUPLICATE_MATCH','MISSING_EVIDENCE','ACCOUNTING_MISMATCH'), amount decimal(19,4) NULL (signed where stated), currency_code char(3) DEFAULT 'INR', subject_table varchar(30) NULL, subject_id bigint NULL, subject_key nvarchar(80) NULL (for subjects without an id, e.g. a tender code), related_ids_json nvarchar(max) NULL CHECK ISJSON, opened_utc, opened_by nvarchar(100) ('ENGINE' or actor), assigned_to nvarchar(100) NULL, next_action nvarchar(500) NULL, due_date date NULL, status varchar(15) CHECK IN ('OPEN','IN_PROGRESS','RESOLVED','CLOSED_NO_ACTION'), resolution_kind varchar(20) NULL CHECK IN ('MATCHED','EXPLAINED','ADJUSTMENT_PROPOSED','WRITE_OFF','DUPLICATE_CASE','SOURCE_CORRECTED','NOT_A_COLLECTION'), resolution_reason nvarchar(1000) NULL, resolved_by, resolved_utc, accounting_status varchar(15) NOT NULL DEFAULT 'NOT_REQUIRED' CHECK IN ('NOT_REQUIRED','PROPOSED','APPROVED','SUBMITTED','VERIFIED','FAILED','UNKNOWN'), controlled_adjustment_id bigint NULL FK controlled_adjustments)`; filtered UQ (case_type, subject_table, subject_id, subject_key) WHERE status IN ('OPEN','IN_PROGRESS').
   - `dbo.collection_case_events` (append-only: `case_id`, `event_type varchar(20) CHECK IN ('OPENED','ASSIGNED','NOTE','EVIDENCE','NEXT_ACTION','RESOLVED','REOPENED','ACCOUNTING')`, `detail nvarchar(1000)`, `actor`, `event_utc`); `dbo.collection_case_documents(case_id, source_document_id, UQ)` — files stored via `ManagedDocumentRepository` under `Exceptions\<case_key>\`.
   - Opening rules in `CollectionCaseRules.cs` (Reporting, pure), run by the pass, one open case per (`case_type`, subject); subjects: `AWAITING_SETTLEMENT`/`OVERDUE_SETTLEMENT` → `collection_obligations` (an `evidence_expected` obligation not MATCHED: AWAITING until `due_date + overdue_after_days`, then OVERDUE; both need D20 — before it the row reads "Settlement window not set" and no case opens; AWAITING cases are created with `assigned_to='ENGINE'`, `next_action='Wait for settlement until <due_date>'`); `OVERDUE_SETTLEMENT` also → `cash_movements` for every RECORDED `DEPOSIT_SENT` with neither a RECORDED `DEPOSIT_CONFIRMED` nor an ACCEPTED `DEPOSIT_BANK` match after the Cash policy's `settlement_window_days` (no policy → the panel shows age, no case, report names D20); `CASH_SHORT_EXCESS` → `cash_counts` CLOSING row when the till variance is outside `cash_tolerance` (no policy → any non-zero), `amount` = counted − expected (signed; negative = short); the pass updates the amount with a `NOTE` event when the variance changes and closes `CLOSED_NO_ACTION` when it returns within tolerance; `DOCUMENTED_DEDUCTION` → one per (`provider_key`, `settlement_id` else `settlement_date`, kind FEE/TAX_ON_FEE/HOLDBACK), `subject_table='provider_settlement_lines'`, `subject_id` = lowest line id of the group, `amount` = Σ of that kind, `related_ids_json` = every line — it exists to route the task 14 adjustment, never hundreds per batch; `REFUND_CHARGEBACK` → `provider_settlement_lines` for every CHARGEBACK line, and for a REFUND line only when no negative tender of that mode/provider exists on that store within `business_date ± 1 day`; `FAILED_REVERSED_PAYMENT` → lines with `is_failed=1`; `UNIDENTIFIED_BANK_CREDIT` → `bank_statement_lines` for every `UNCLASSIFIED` credit with zero allocations after the pass; an `UNCLASSIFIED` debit older than the widest `settlement_window_days` → `MISSING_EVIDENCE`; `AMBIGUOUS_DUPLICATE_MATCH` → the obligation (or the bank line when the ambiguity is on the bank side), candidates in `related_ids_json`; `MISSING_EVIDENCE` → the tender code (`subject_table='tender_modes'`, `subject_key` = code), the obligation, the suspended match, the provider group, or the review (prepared without a CLOSING count; without any bank file covering the date for an account that has a layout — this one opens only on a pass run after `business_date + settlement_window_days`, before D20 the Sources panel shows "Bank file: not yet imported" and no case opens); `ACCOUNTING_MISMATCH` → `controlled_adjustments` (task 15).
   - `ICollectionCaseService`: `AssignAsync`, `SetNextActionAsync`, `AttachEvidenceAsync`, `ResolveAsync(kind, reason)` (Store Manager for MATCHED — requires an ACCEPTED allocation on the subject — EXPLAINED, DUPLICATE_CASE, SOURCE_CORRECTED, NOT_A_COLLECTION, ADJUSTMENT_PROPOSED); `WRITE_OFF` Owner-only until D21 names another approver; a Store Manager attempt throws "Owner permission is required." after writing `CollectionCase`/`Blocked`. Age = days since `opened_utc`.
   - Exceptions screen gets chips per case type; columns: case, store, date, mode, amount, age, owner, next action, due, status, accounting.
   - Tests: unit `CollectionCaseRulesTests` against `expected-cases.csv` (including one deduction case per settlement group, an ENGINE-assigned awaiting case, an overdue deposit); boundary `CollectionCaseServiceBoundaryTests` (Store Manager `WRITE_OFF` refused and audited); SQL (a second identical opening is a no-op; MATCHED without an allocation refused).
12. **Daily statement and review (frozen versions).** `dbo.collection_reviews(review_id bigint IDENTITY PK, review_key AS 'CR-' + store_code + '-' + CONVERT(char(8), business_date, 112) + '-V' + CAST(version AS varchar(5)) PERSISTED UNIQUE, store_code FK, business_date date NOT NULL, version int NOT NULL, UQ(store_code, business_date, version), state varchar(20) CHECK IN ('DRAFT','PREPARED','MANAGER_REVIEWED','APPROVED','RETURNED'), outcome varchar(30) NULL CHECK IN ('FULLY_SETTLED','APPROVED_WITH_OUTSTANDING'), expected_total, matched_total, outstanding_total, cash_variance decimal(19,4) NULL, deposits_in_transit decimal(19,4), open_case_count int, formula_version int, rule_set_version int, snapshot_json nvarchar(max) CHECK ISJSON (per mode: expected, matched, residual; till result; source completeness), snapshot_sha256 char(64), prepared_by/utc, reviewed_by/utc, decided_by/utc, decision_reason nvarchar(1000) NULL)`; `expected_total` = Σ `expected_amount` over all current rows of the day; `outstanding_total` = Σ residual over current rows with `evidence_expected=1` plus `deposits_in_transit`; `dbo.collection_review_events` append-only (`event_type` in `PREPARED`, `REVIEWED`, `APPROVED`, `RETURNED`, `SETTLEMENT_AFTER_APPROVAL`, `REPREPARED`; `detail`, `amount NULL`, `match_id NULL`, `actor`, `event_utc`); trigger `trg_collection_reviews_frozen`: a row in APPROVED or RETURNED may not change (THROW).
   - `ICollectionReviewService` / `SqlServerCollectionReviewService`: **Prepare** (Store Manager) checks `daily_reporting_days.status='LOCKED'` for (store, date) — "Finalise the day first" (the Phase 4 lock is a precondition, never the settlement state) — and that a current `import_files` row with `report_code='R022'` covers the store and date — "Import the sales export first"; then runs the pass, creates version 1 DRAFT if none exists, writes the snapshot and moves DRAFT → PREPARED; **Mark reviewed** (Store Manager) requires every open case of the day not assigned to `ENGINE` to carry `assigned_to` and `next_action` → MANAGER_REVIEWED; **Approve** (Owner, reason) → APPROVED with `outcome = FULLY_SETTLED` only when `outstanding_total = 0`, `cash_variance = 0` and `open_case_count = 0`, else `APPROVED_WITH_OUTSTANDING` and the amount appears on later days under "Carried forward" (task 13); **Return** (Owner, reason) → RETURNED and a new DRAFT version. An APPROVED version is frozen; a later ACCEPTED allocation to one of its obligations appends `SETTLEMENT_AFTER_APPROVAL` (amount, match_id) and reduces Carried forward on later days — no new version; a new version is created only by Return, by an obligation supersession (task 5), by a new case on that date, or by the Owner pressing "Re-prepare" (`REPREPARED` event).
   - Approval writes `<collections evidence root>\Collections\<store_code>\<yyyy-MM-dd>\<review_key>\statement.pdf` + `manifest.json` (per-file SHA-256) recorded in `dbo.collection_evidence(evidence_id, review_id, relative_path nvarchar(400), sha256 char(64) UNIQUE, manifest_json, created_by, created_utc)` with an `AFTER UPDATE,DELETE` THROW trigger.
   - Today → "Collections" tab is the statement for the header store and date, six panels: (1) Expected by mode — one row per D11 mode with "tender due", "tender received", "outstanding", state text, plus the three Carried-forward figures; (2) Sources — Sales export / Till count / Bank file / Provider file each shown as Imported · Not yet imported · Not expected · Empty, never a zero; (3) Till — opening (with source), cash in, cash out, sent to bank, expected closing, counted, difference, "Formula v1"; (4) Cash on its way to the bank — deposits in transit with age, oldest first; (5) Cases — the day's open cases; (6) Review — state, version, buttons Prepare · Mark reviewed · Approve · Return · Re-prepare · Refresh, gated by role, disabled reason inline. Audit type `CollectionReview`.
   - Tests: SQL `CollectionReviewSqlTests` — UPDATE of an APPROVED row throws; Prepare on an unlocked day refused with "Finalise the day first", on a locked day without R022 with "Import the sales export first"; Mark reviewed refused while a person-owned case lacks an owner, allowed with only ENGINE-assigned cases; a later allocation adds one `SETTLEMENT_AFTER_APPROVAL` event and no version; version 1 `snapshot_sha256` unchanged after version 2 exists; boundary test: Approve as Store Manager refused.
13. **Views, ageing and carried forward.** `ICollectionsQuery` (`SqlServerCollectionsQuery.cs`, `RequireViewAsync`) methods `LoadStatementAsync(store, date)`, `LoadDailyExpectedActualAsync(scope)`, `LoadTillAsync(scope)`, `LoadUpiSettlementsAsync(scope)`, `LoadCardBridgeAsync(scope)`, `LoadBankMatchesAsync(scope)`, `LoadDepositsInTransitAsync(scope)`, `LoadAgeingAsync(scope)`, `LoadSignoffsAsync(scope)`, `LoadAdjustmentQueueAsync(scope)`, `LoadCarriedForwardAsync(store, date)`. Carried forward (defined once, used by tasks 12 and 17) = Σ residual of current obligations with `evidence_expected=1`, `business_date < @date`, state not in (MATCHED, NO_OUTSIDE_EVIDENCE); plus Σ `DEPOSIT_SENT` in transit with `business_date < @date`; plus the count of OPEN/IN_PROGRESS cases with `business_date < @date`; the month view is the same as of the first day of the month. Reports → "Collections" group, each view with Excel and PDF export through the Phase 2 exporters and an amount-basis label in the header ("tender due", "tender received", "bank credit", "cash counted", "variance"): Daily expected vs actual (multi-day, per mode); Till roll-forward (month; custody and banking columns); UPI settlements (pending / settled / overdue by provider); Card settlement bridge (task 10); Bank match and unapplied credits (every line with classification, allocated, residual; narration only as the bank supplied it); Deposits in transit; Exception ageing (open cases and obligations by age band 0–2, 3–7, 8–30, >30 days × channel, gross amounts and residuals, never a percentage alone); Manager sign-offs (reviews by state, version, outcome); Adjustment queue (task 14). A source not imported shows "—". Tests: SQL `CollectionsQuerySqlTests` — every view over the loaded fixture equals its `expected-*.csv`; no view selects `customer_name`, `customer_phone` or `creditcardno` (asserted by column list).

**8d — Accounting consequences, cycle and period**
14. **Adjustment proposals into Phase 7 batches.** Migration `00NN_collections_accounting.sql`: `ALTER TABLE dbo.controlled_adjustments ADD adjustment_kind varchar(20) NULL CHECK IN ('MDR_CHARGE','BANK_CHARGE','TAX_ON_CHARGE','CASH_SHORT','CASH_EXCESS','PROVIDER_HOLDBACK','CHARGEBACK','WRITE_OFF','OTHER'), collection_case_id bigint NULL FK collection_cases, settlement_business_date date NULL, reversal_of_controlled_adjustment_id bigint NULL FK self` (existing rows stay NULL); `ALTER TABLE dbo.accounting_batches ADD batch_kind varchar(25) NOT NULL DEFAULT 'DAILY_SALES' CHECK IN ('DAILY_SALES','COLLECTIONS_ADJUSTMENT'), settlement_business_date date NULL`; `daily_report_generation_id` relaxed to NULL with CHECK ((batch_kind='DAILY_SALES' AND daily_report_generation_id IS NOT NULL) OR (batch_kind='COLLECTIONS_ADJUSTMENT' AND daily_report_generation_id IS NULL AND settlement_business_date IS NOT NULL)). COLLECTIONS_ADJUSTMENT batches key on (`store_code`, `settlement_business_date`, `accounting_generation`), need no final report generation (a settlement date may be a Sunday or an unfinalised day) and never touch a DAILY_SALES batch. If Phase 7's final text restructures batches, follow it and keep the rule "no dependency on a final report generation".
   - `SubmitAdjustment` gains optional `AdjustmentKind`, `CollectionCaseId`, `SettlementBusinessDate`; `ProductisationRepository.CreateAdjustmentRequestAsync` gains the same parameters. Phase 8 rows: `business_date` = the sales or count day the case refers to, `adjustment_type` = `adjustment_kind`, `amount` always > 0 (direction comes from the `ADJ_<kind>` mapping's debit/credit ledgers), `settlement_business_date` = the settlement or count date. From a case, "Propose adjustment" (Store Manager; `WRITE_OFF` Owner-only per D21) chooses the kind from the case: `CASH_SHORT_EXCESS` → `CASH_SHORT`/`CASH_EXCESS` by sign; `DOCUMENTED_DEDUCTION` → `MDR_CHARGE`/`TAX_ON_CHARGE`/`BANK_CHARGE` by line kind, `PROVIDER_HOLDBACK` for HOLDBACK; `REFUND_CHARGEBACK` → `CHARGEBACK`; `WRITE_OFF` only when `resolution_kind='WRITE_OFF'`; else `OTHER`; it creates the `controlled_adjustments` + `approval_requests` pair and sets the case `accounting_status='PROPOSED'`; Owner approval → `APPROVED`.
   - `ProductisationRepository.LoadAccountingSourceAsync`: the Phase 5 `ADJUSTMENT` event adds `AND adjustment_kind IS NULL`; a COLLECTIONS_ADJUSTMENT batch emits, per APPROVED row with `settlement_business_date = @date`, one debit line and one credit line, both `business_event = 'ADJ_<kind>'`, ledgers from the mapping, `source_reference = 'ADJ-' + controlled_adjustment_id`, Phase 7 business key (`store_code`, FY-end year, `'ADJ-' + controlled_adjustment_id`) and component role `JOURNAL` (the existing Journal voucher path; the voucher type name is verified on the installed build and recorded in `PHASE-8-REPORT.md`); each `ADJ_<kind>` needs an APPROVED `accounting_mappings` row — absent → the component is BLOCKED "No approved ledger mapping for ADJ_<kind> (D23)" and the case stays PROPOSED; independent work continues.
   - When D16 fixes clearing ledgers, an ACCEPTED `OBLIGATION_BANK`/`PROVIDER_BANK` match proposes a `RECEIPT` component (clearing ledger → bank ledger, amount = allocated principal); until then the case shows `NOT_REQUIRED`.
   - A bank settlement never creates a sale: the only writers of `sales_*` remain the Phase 1 imports, and manager approval never creates revenue or deletes an invoice.
   - Tests: SQL — an APPROVED `CASH_SHORT` adjustment appears as exactly two `accounting_entries` rows across all batches (`SELECT COUNT(*) … WHERE source_reference LIKE 'ADJ-%'` = 2, one debit and one credit of the same amount), in a COLLECTIONS_ADJUSTMENT batch for its `settlement_business_date` that has no `daily_reporting_days` row; the sales day's `ADJUSTMENT` total excludes it; a PENDING adjustment never appears; `sales_invoices` count unchanged through the cycle.
15. **Accounting status sync and mismatch.** `CollectionsReconcilePass.SyncAccountingStatusAsync` maps the Phase 7 component that holds the adjustment: batch `EXPORTED_AWAITING_IMPORT` or later → `SUBMITTED`; component `RECONCILED` → `VERIFIED`; `DIFFERENCE` → `FAILED` and an `ACCOUNTING_MISMATCH` case carrying the `tally_reconciliation_differences.difference_type`; `OUTCOME_UNKNOWN` → `UNKNOWN`. Operational closure (case `RESOLVED`) and accounting closure (`VERIFIED`) are two columns and two chips; a resolved case with `FAILED`/`UNKNOWN` stays on the Adjustment queue. Tests: unit `AccountingStatusMapTests` (every Phase 7 component status → exactly one Phase 8 status, exhaustive switch).
16. **Engine pass and job.** `CollectionsReconcilePass.RunAsync(store, dateRange, asOf = today IST)` (Infrastructure.SqlServer) = recompute obligations → run bridge and rules (a match and its allocations are inserted in one transaction) → classify bank lines from accepted matches → derive states with `asOf` → open/close cases → sync accounting; each step one transaction. It never resolves a case by itself; it may set `CLOSED_NO_ACTION` on `AWAITING_SETTLEMENT` when the obligation becomes MATCHED, on `UNIDENTIFIED_BANK_CREDIT` when the line receives an ACCEPTED allocation or a classification, on `AMBIGUOUS_DUPLICATE_MATCH` when a person accepts one candidate, on `CASH_SHORT_EXCESS` when the variance returns within tolerance, and on `ACCOUNTING_MISMATCH` when the component returns to `RECONCILED`; every other case needs a person. Invoked by "Refresh" on the Collections tab; after every collections import for the account's store (every active store when the account is shared) over [`period_start` − 45 days, `period_end` + 45 days] (45 replaced by the largest `settlement_window_days` once D20 is set); and by `--automation-once` as `automation_runs.run_type='COLLECTIONS_RECONCILE'` (extend `CK_automation_runs_type`), one run row (`store_code` = the store, `business_date` = range end) and `AutomationRun` audit per store per run.
   - Idempotent: running twice on unchanged data with the same `asOf` writes zero rows. A DayReopened or Restatement event marks nothing directly — the next pass detects the hash change (task 5).
   - Tests: SQL `CollectionsReconcilePassSqlTests` — idempotence (row counts and `operational_audit` count unchanged on the second run with the same `asOf`); a thrown exception after the obligations step leaves no PROPOSED match without allocations and no case without its OPENED event.
17. **Period boundary.** `dbo.collection_periods(store_code FK, period_month date (first day), PK both, state varchar(10) CHECK IN ('OPEN','CLOSED'), carried_outstanding_total decimal(19,4) NULL, carried_case_count int NULL, closing_note nvarchar(1000) NULL, closed_by, closed_utc, reopened_by, reopened_utc, reopen_reason nvarchar(500) NULL)`; `dbo.collection_period_carried(store_code, period_month, source_table, source_id, PK)` records each ticked item.
   - "Close month" (Owner, Settings → Collections setup; period-close policy): lists every review of the month not APPROVED, every `evidence_expected` obligation not MATCHED and every case not RESOLVED/CLOSED_NO_ACTION; each must be ticked "carry forward" (or the missing days acknowledged in the note) or closing is refused; `carried_outstanding_total`/`carried_case_count` written from `LoadCarriedForwardAsync` as of the first day of the next month and never rewritten; carried items keep their original `business_date` and appear on the next month's statements under "Carried forward".
   - Triggers while CLOSED, all THROW "The month is closed. Ask the owner to reopen it.": `trg_cash_movements_period_closed` (new movements dated in the month, except `DEPOSIT_CONFIRMED` whose `related_movement_id` is a carried `DEPOSIT_SENT`), `trg_cash_counts_period_closed`, `trg_collection_reviews_period_closed` (new versions for a date in the month), `trg_controlled_adjustments_period_closed` (`settlement_business_date` in the month), `trg_settlement_allocations_period_closed` (evidence dated in the month — `bank_statement_lines.line_date`, `provider_settlement_lines.settlement_date`, `cash_movements.business_date` by `source_table`; `IMPLIED_DEDUCTION` uses the match's `proposed_utc` IST date — allocated to an item that is **not** carried). Allocations to a carried obligation or deposit are always allowed whatever the evidence date; the match records `recognised_period` = the first OPEN month at acceptance. Owner reopen with a reason (`CollectionPeriod` audit) lifts all five. D24 may tighten this; the report records the rule in force.
   - Tests: SQL — an August-dated credit allocated to a carried August obligation after close succeeds with `recognised_period` = September; the same credit to a non-carried MATCHED August obligation throws; confirming a carried August deposit in September succeeds; an August `EXPENSE` after close throws; reopen then allocate succeeds and both events are recorded.
18. **Multi-day cycle golden test.** `tests-dotnet/Etp.Reporting.SqlServer.IntegrationTests/CollectionsCycleTests.cs` loads the synthetic fixture end to end (five days, two stores, one shared bank account: cash short with a deposit in transit confirmed on day 3; UPI pending then settled next day; a NET UPI day with an implied deduction; card batch across two days with MDR and tax lines and one UTR; grouped bank credit covering two days; the same bank statement imported twice, once as CSV; a refund and a chargeback; two equal-amount credits; two stores' equal deposits; a review approved with outstanding, then settled without a new version; an R022 restatement on day 2 with changed content; a Store Manager write-off attempt; a simulated restart between import and the pass; a database restore between day 3 and day 4) and asserts `collection_residuals`, the case list and review states against `expected-cycle.csv`; the day-4 adjustment batch's exported file is compared by the Phase 7 engine against the fixture read-back XML under `tests-dotnet/fixtures/collections-sample/tally-readback/` (manual-export fallback shape) and returns `RECONCILED` — the live TEST-company round trip is A8.13 on the audit PC only. `PHASE-8-REPORT.md` carries the cycle table (day, event, expected, observed, evidence path).
19. **Evidence root, roles, navigation, hygiene.** `product_settings.collections_evidence_root nvarchar(400)` (default `C:\ProgramData\EtpReporting\CollectionsEvidence`, Owner-editable via `IAdministrationService.SaveProductConfigurationAsync`, added to the Phase 4 `icacls` list; sibling of the Phase 7 root, no second root inside this phase) with sub-folders `Collections\<store_code>\<yyyy-MM-dd>\<review_key>\`, `BankSources\<account_key>\<yyyy>\<MM>\`, `SettlementSources\<provider_key>\<yyyy>\<MM>\`, `Exceptions\<case_key>\`; `CollectionsPaths.Validate` checks every folder segment against `^[A-Z0-9_-]{1,40}$` and the file name against `^[0-9a-f]{64}\.[a-z0-9]{1,5}$` or the fixed names `statement.pdf`, `manifest.json`; anything else is refused, so no customer name, account number or UTR can enter a path; SQL stays the authoritative state.
   - Audit types `CollectionsSetup`, `CollectionsImport`, `CashCount`, `CashMovement`, `CollectionObligation`, `SettlementMatch`, `CollectionCase`, `CollectionReview`, `CollectionPeriod` added to `CK_operational_audit_type` and `OperationalAuditRepository.EventTypes` in the slice that first uses each; `safe_detail` constant text only.
   - Support package and diagnostics exclude `raw_json`, `narration`, `denominations_json`.
   - Desktop `Modules/Collections/` (presentation session, workspace view, setup view, import view) registered in the `TaskNavigation` table: `collections-today` (Today, role 2), `collections-import` (Import, role 2), `collections-setup` (Settings, role 3), and Reports ids `collections-daily`, `collections-till`, `collections-upi`, `collections-card-bridge`, `collections-bank`, `collections-transit`, `collections-ageing`, `collections-signoffs`, `collections-adjustments` (role 1); plain-word copy ≤ 12 words per message.
   - Regression: full `dotnet test`, Phase 2 golden reports and Phase 7 golden tests re-run after each Phase 8 migration.

Acceptance (auditor evidence in brackets):
- A8.1 The four slice migrations apply on an empty database and on a copy of the shop database; the tables of tasks 1–6, 8, 11, 12, 17 exist with the CHECK lists as written; as Owner in SSMS `UPDATE dbo.collection_reviews SET state='DRAFT' WHERE state='APPROVED'` and `UPDATE dbo.settlement_match_allocations SET allocated_amount = allocated_amount + 1` fail with the tasks' THROW numbers; `manual_input_definitions` shows `CASH_DEPOSIT`, `EXPENSES`, `CASH_ADJUSTMENT`, `CLOSING_CASH_COUNTED` inactive; Σ migrated amounts per (store, date, type) in `cash_movements`/`cash_counts` equals Σ non-zero manual inputs, and the skipped-zero count in the `CashMovement` audit row equals `SELECT COUNT(*) FROM manual_operational_inputs WHERE field_code IN (…) AND numeric_value = 0`; `sys.triggers` shows `trg_import_files_protect_locked` unchanged. [SQL results; error screenshots]
- A8.2 Settings → Collections setup as Owner: add bank account `HDFC_WLMHW` (last4 only), provider `AIRPAY` (UPI, settles via BANK, statement kind and granularity UNKNOWN), a merchant id for Titan; pointing tender code PAYMENTTYPE25 at CARD_ACQUIRER is refused by the provider-mode trigger; as Store Manager the screen is read-only and the boundary test shows "Owner permission is required." with one `CollectionsSetup`/`Blocked` audit row; a fresh database has `SELECT COUNT(*) FROM dbo.collection_policies` = 0 and the Settlement rules tab reads "Settlement rule not set — ask the owner (D20)". [screenshots both roles; SQL; test output]
- A8.3 File layouts: pick the synthetic bank fixture, map value date, narration, reference, withdrawal, deposit, balance, press "Test on this file": 20 rows shown, no unparsed cell; pick the signed-amount CSV fixture and map `signed_amount` + `dr_cr_flag`: credits show positive; saving with no amount convention or no date key is refused inline; a second active BANK layout for the same account is refused. [screenshots; `statement_layouts.mapping_json`]
- A8.4 Bank import: the fixture statement (its range covers finalised days) → "Imported n lines" (n = the fixture's row count) with no 51021; again → "Already imported", `COUNT(*)` unchanged; its CSV twin → "Duplicate content"; the superset copy with three added lines → exactly three new `bank_statement_lines`, earlier ids unchanged, the first file `is_superseded=1`, `import_files.store_code` NULL; every line is `UNCLASSIFIED` (none `NON_SALES` until a person classifies it); the file exists under `BankSources\HDFC_WLMHW\<yyyy>\<MM>\<sha256>.xlsx` with the hash in `source_documents`. [result list screenshots; SQL; `Get-FileHash`]
- A8.5 Provider import: the Airpay-shaped NET fixture loads with `fee_amount IS NULL` on every line and `net_amount` filled; the card-shaped GROSS fixture loads `FEE` and `TAX_ON_FEE` lines and a REFUND line with positive `gross_amount`; a FAILED-status line shows in the grid with its mapped kind and `is_failed=1`; the bridge tab reads "Provider statement kind not set (D19)" while UNKNOWN and, once set to GROSS_WITH_DEDUCTIONS, bridges 30,000 − 180 = 29,820 with difference 0 against the fixture bank credit; no grid, export or support package contains `creditcardno`. [SQL; screenshots; support package inspected]
- A8.6 Obligations: after the fixture sales import and Refresh, `collection_obligations` has one current row per store/date/mode/provider whose `expected_amount` equals |SUM(source_amount)| of `reporting_sales_tenders` joined to `sales_invoices` for that key (auditor recomputes) with `direction` by sign, Service rows equal the `SERVICE_*` inputs, Cash/CN rows carry `evidence_expected=0`, state `NO_OUTSIDE_EVIDENCE` and read "No outside evidence for this mode"; a UPI tender code with no provider shows `evidence_expected=1`, state UNMATCHED and one `MISSING_EVIDENCE` "Provider not set" case; a tender code without a `tender_modes` row yields no obligation and one `MISSING_EVIDENCE` case. Re-importing the identical day-2 R022 creates no superseded rows; restating it with one extra invoice after matching leaves the old row `is_current=0` with `superseded_by_obligation_id` set, the original match `REVERSED` and a `SUPERSESSION` match ACCEPTED on the new row, and the banner "Sales changed after this statement — refresh" shown before the pass, gone after it. [SQL before/after; screenshots]
- A8.7 Till: on fixture day 1 (no OPENING count, no previous CLOSING) enter OPENING_FLOAT 5,000, EXPENSE 2,000, DEPOSIT_SENT 15,000 with a slip reference on the day with cash sales 20,000; count 7,900 → expected 8,000, difference −100, footer "Formula v1", one `CASH_SHORT_EXCESS` case with amount −100, deposits in transit 15,000; a return with NO_REFUND 100 changes expected closing by the cash refunded only; an uncounted day shows "Till not counted", not ₹0; a `DEPOSIT_SENT` older than the Cash window with no confirmation shows one `OVERDUE_SETTLEMENT` case with the slip reference. A `DEPOSIT_CONFIRMED` for a LOCKED day saves; an `EXPENSE` for it is refused with "The business date is finalised"; Finalise without a CLOSING count is refused with "Count the till before closing the day". [screenshots; SQL]
- A8.8 Matching: the UPI day settled next morning is MATCHED via `PROVIDER_SUM` with `settlement_date` one day after `business_date`; the NET UPI day is MATCHED with one `IMPLIED_DEDUCTION` allocation and one `DOCUMENTED_DEDUCTION` case; the card batch spanning two days shows two obligations PARTIALLY_MATCHED → MATCHED with allocations summing to the batch gross, the PROVIDER_BANK bank-side allocation equals the batch net, gross − net equals the FEE + TAX_ON_FEE lines, and one `DOCUMENTED_DEDUCTION` case per settlement group and kind with amount = Σ of that kind; the bank credit whose UTR covers the group is ACCEPTED with `proposed_by='ENGINE'` and the line classified `CARD_SETTLEMENT`; the two equal bank credits produce no proposal and one `AMBIGUOUS_DUPLICATE_MATCH` case naming both `bank_line_id`s; the two stores' equal deposits likewise; a manual allocation of ₹0.01 over a credit's residual is refused with "This receipt is already fully allocated"; a manual unbalanced match cannot be accepted. [SQL over matches/allocations; screenshots]
- A8.9 Residuals and partial settlement: `SELECT * FROM dbo.collection_residuals` after the full cycle equals `expected-residuals.csv` row for row; `HAVING SUM(allocated) > credit_amount` returns zero rows; a 50,000 UPI obligation with 49,000 credited shows received 49,000, outstanding 1,000, PARTIALLY_MATCHED with no policy row, and with a policy row PENDING inside the window and OVERDUE after it — shown by `ObligationStateResolverTests` with `asOf` on both sides of the boundary and on the audit PC on the first day after it; withdrawing the matched bank line by a later export suspends the match and the residual returns. [SQL diff; test output; screenshots]
- A8.10 Cases: every fixture case in `expected-cases.csv` exists once with the expected type, subject and amount; "Mark reviewed" is refused while any person-owned case lacks owner or next action and allowed when only ENGINE-assigned awaiting cases remain; the Store Manager `WRITE_OFF` refusal writes `CollectionCase`/`Blocked` (boundary test and audit row); as Owner, resolving the `CASH_SHORT_EXCESS` case as `ADJUSTMENT_PROPOSED` creates a PENDING `controlled_adjustments` row with `adjustment_kind='CASH_SHORT'`, `amount` 100, and the case shows accounting `PROPOSED`; resolving another as `WRITE_OFF` yields `adjustment_kind='WRITE_OFF'`. [screenshots both roles; SQL]
- A8.11 Review: Prepare on an unlocked day is refused with "Finalise the day first"; after finalising, Prepare → Mark reviewed → Approve with a reason on a day with 1,000 outstanding and one open case yields `APPROVED_WITH_OUTSTANDING`, the statement still shows 1,000, the next day's tab shows it under "Carried forward"; when the settlement arrives version 1 is unchanged (`snapshot_sha256` equal, direct UPDATE throws), one `SETTLEMENT_AFTER_APPROVAL` event exists and the next day's Carried forward drops to 0; the R022 restatement of that day creates version 2; Return then Prepare creates the next version; `daily_reporting_days.status` is unchanged throughout. [screenshots; SQL on both versions]
- A8.12 Evidence: after approval `<collections evidence root>\Collections\WLMHW\<date>\<review_key>\manifest.json` exists, its SHA-256 equals `collection_evidence.sha256`, `review_key` ends in `-V1`, and no path under the root contains a customer name, phone, full account number or UTR (auditor greps the tree); `icacls` on the root shows no BUILTIN\Users entry. [directory listing; `Get-FileHash`; `icacls`]
- A8.13 Accounting loop: the approved `CASH_SHORT` adjustment appears as exactly two `ADJ_CASH_SHORT` lines (debit total = credit total = the adjustment amount) in a COLLECTIONS_ADJUSTMENT batch for its `settlement_business_date` (a Sunday in the fixture; no DAILY_SALES batch touched), `SELECT COUNT(*) FROM accounting_entries WHERE source_reference = 'ADJ-<id>'` = 2 across all batches and the sales day's `ADJUSTMENT` total excludes it; the batch blocks with "No approved ledger mapping for ADJ_CASH_SHORT (D23)" until the Owner approves a mapping, then reaches `EXPORTED_AWAITING_IMPORT` in the TEST profile; after import into the TEST company and read-back the Phase 7 compare returns `RECONCILED` and the case shows operational RESOLVED and accounting VERIFIED; in the TEST company alter the imported voucher amount by ₹1, read back and compare again: the component becomes `DIFFERENCE` with `difference_type='AMOUNT_MISMATCH'`, the case accounting status FAILED and one `ACCOUNTING_MISMATCH` case carrying that type exists; restore the voucher, re-compare → `RECONCILED`, case VERIFIED, mismatch case `CLOSED_NO_ACTION`; `sales_invoices` count is unchanged before and after. [SQL; Phase 7 evidence folder; Tally TEST company voucher; screenshots]
- A8.14 Views: each Reports → Collections view opens read-only for a Viewer, exports to Excel and PDF with the amount-basis label in the header, and Exception ageing shows gross amounts per age band × channel equal to the auditor's SQL over `collection_cases`/`collection_obligations`; no view or export selects `customer_name`, `customer_phone` or `creditcardno` (asserted by column list), bank narration appears only as the bank supplied it and is never joined to ETP customer data, and the support package and diagnostics contain no `narration`, `raw_json` or `denominations_json` values. [screenshots; exports opened; column lists; support package inspected]
- A8.15 Cycle and recovery: `CollectionsCycleTests` green in CI (fixture read-back, no Tally); on the audit PC the fixture ends with every row of `expected-cycle.csv` matching; stopping the app between a bank import and Refresh, then reopening, shows the imported lines and zero PROPOSED matches without allocations; Refresh twice on unchanged data with the same `asOf` leaves row counts and `operational_audit` count unchanged; restoring the backup taken after day 3, re-importing the day-4 and day-5 files and running the pass reproduces the day-5 `expected-residuals.csv` row for row. [test summary line; SQL before/after]
- A8.16 Period and honesty: closing the fixture month as Owner refuses while an OVERDUE obligation is unticked, succeeds when carried with `carried_outstanding_total` equal to `LoadCarriedForwardAsync` for the first of the next month; an August-dated credit allocated to the carried obligation succeeds with `recognised_period` = the next month, the same credit to a non-carried matched item is refused, confirming the carried August deposit in September succeeds, an August `EXPENSE` is refused; reopen with a reason, allocate, and both events appear in `collection_periods` and the audit. `sys.triggers` shows no `*_protect_locked` trigger on any settlement table (obligations, bank lines, provider lines, matches, allocations, cases, reviews, periods). `PHASE-8-REPORT.md` shows the D19–D21 status and the D22–D24 proposals, the settlement rules still "Not set" if so, whether `etp_r020.approvalnumber` is filled for UPI agencies on live data, the ROUND_OFF till check, `statement_kind`/`granularity` per provider, the Journal voucher type name verified on the installed build, the tender codes still without a provider, and for every provider whether a real layout was verified against a sanitised sample or only against the synthetic fixture — nothing is labelled "connector working" without such a sample. [screenshots; SQL; report text]

Deferred inside this phase (built only if 8a–8d prove the need, or out):
- Direct bank or provider APIs, SMS/e-mail parsing, any paid gateway or cloud sync: out.
- Automatic acceptance of grouped (R2, R2b), deposit (R3) or amount-window (R4) matches: people accept; only unique strong-reference (R1) matches are engine-accepted. No auto-match-rate or hours-saved measure is set or reported.
- Paytm and any provider without a D19-approved layout; multi-currency and foreign-card settlements (`currency_code` stored, INR only exercised).
- Clearing-ledger `RECEIPT` proposals (task 14) stay `NOT_REQUIRED` until D16 says clearing ledgers exist; provider fee GST input-credit logic beyond mapped `TAX_ON_FEE` columns (D23 proposal and the accountant).
- Denomination analytics beyond storing `denominations_json`; cash forecasting; per-cashier accountability beyond `CASHIER_HANDOVER` rows; staff-recovery or payroll consequences of shortages.
- A shared or OneDrive evidence folder as state or lock; SQL is the only authority.
- Any default settlement window, overdue threshold or cash tolerance — these ship unset until D20.

## 6. Sequence and estimate

| Phase | Codex effort | Depends on | Owner decisions |
|---|---|---|---|
| 0 Stabilise | 1–2 days | — | none |
| 1 Data truth | 5–7 days | 0 | D1, D3, D11 (decided) |
| 2 Six reports | 7–10 days | 1 | D2, D4, D5, D8, D10 (decided) |
| 3 Touch shell | 5–7 days | 2 (can start UI scaffolding after 0) | D7 (decided) |
| 4 Security & ops | 3–5 days | 0 (independent of 1–3) | D9 (decided) |
| 5 Secondary modules | 8–12 days (all four kept) | 3 | — ; D12–D18 must be frozen at its close |
| 6 Release | 2–3 days | all of 0–5 | — |
| 7 Tally transfer (Stage 2) | 6–10 weeks (7a 1–2, 7b 2–3, 7c 2–3, 7d 1–2; 7e optional) | 6 released; D12–D18 frozen; TallyPrime 7.1 TEST company on the D18 machine | D12–D18 |
| 8 Collections reconciliation (Stage 3) | 4–8 weeks (8a 1–2, 8b 1–2, 8c 1–2, 8d 1–2) | 7 (accounting consequences route through Phase 7 batches); Phase 2 task 15 samples; D19–D21 frozen | D19–D21 |

Phases 1 and 4 can run in parallel on separate branches. Stage 1 (Phases 0–6) stays at roughly 6–8 weeks of Codex time; Phase 7 adds 6–10 weeks and Phase 8 adds 4–8 weeks, so the whole programme is five to six months of Codex time plus Claude audit turnaround after each phase and slice. Phase 8 slice 8a (contracts and imports) may start on its own branch once Phase 7 slice 7a has passed audit; 8d waits for 7c. No phase starts before the previous one is closed except where this table says otherwise.

## 7. Open items log
- 14 Sep fixes uncommitted — Phase 0 task 1.
- Version 1.0 of this plan, 15 Sep 2026: all six audits incorporated. Changes to this document happen only through Claude's phase audits.
- **Phase 0 CLOSED 15 Sep 2026** (`PHASE-0-AUDIT.md` section 8, commit `6791c9e`, 580 tests green, five green CI runs). Not yet done on the shop PC: `EtpReporting` is still at 14 migrations; run `Etp.Reporting.Desktop.exe --initialize-database` with `settings.json` pointing at `EtpReporting` to apply 0015–0016 (indexes, last-Owner guard, restored audit write). `EtpReportingHelios` (two-year Helios corpus, created 15 Sep) carries the final 0016 checksum (verified) but holds the 272 dropped-row import; Phase 1's whole-folder test drops and rebuilds it.
- Phase 1 branch: `phase-1/data-truth` from `main` after the fast-forward merge of `phase-0/stabilise`.
- **Version 1.4, 16 Sep 2026** (owner-accepted assessment of the two roadmap documents): Phases 7 and 8 added after Phase 6; D12–D21 recorded OPEN in Section 3; Phase 2 tasks 12–15 and A2.6–A2.10; Phase 4 task 1 note, task 3 receipt row counts, task 6 rewritten (no certificate purchase), A4.4/A4.4a/A4.7; Phase 5 Accounting row extended with A5.5–A5.7; Section 6 calendar; Section 8 crosswalk. No Phase 0–1 text changed.
- **Roadmap documents filed** under `docs/roadmap/THREE-STAGE-ROADMAP-2026-09-15.md` and `docs/roadmap/STAGE-2-TALLY-ANNEX-2026-09-15.md`, each prepended with the banner in Section 8. They are reference material only; this plan governs; the prompts embedded in them are never given to Codex.
- **Decisions D12–D21 OPEN.** D12–D18 (Tally company, voucher granularity, party, GST, tender, inventory, installed build) are frozen by the owner and the accountant when Phase 5 closes; D19–D21 (channels and samples, settlement windows and tolerances, approval roles) when Phase 7 closes. Until frozen, the dependent Phase 7/8 tasks build validation, fixtures and `BLOCKED` states only (rule 7).
- **Branch state at v1.4 (verified 16 Sep 2026):** `main` still at `9a028a4` (Phase 0). `phase-1/data-truth` `84df74c`: Phase 1 **CLOSED** (`claude-audit-2026-09/PHASE-1-AUDIT.md`, migrations 0017–0020), not yet merged. `phase-2/evening-reports` `5535ba0` (migration 0024), `phase-3/touch-shell` `1c8eb1b`, `phase-4/security-operations` `d730839` (0021–0023): implemented and **REOPENED** by the 16 Sep audits (`PHASE-2/3/4-AUDIT.md`). `phase-5/secondary-modules` `0a0c38f` (0025–0026) started; `integration/phase-2-3-4-fixes` `123e1d4` collects cross-phase fixes. The day-to-day handoff for those branches is `docs/audit/SESSION-HANDOFF-2026-09-16.md` on `phase-1/data-truth`. Migration numbers were allocated per branch; the merge keeps them unless a database already applied a conflicting number, in which case the unmerged branch adds the next free numbers (rule 9). Phase 4 `0022` still references `document_extractions`, dropped by `0020` — a Phase 4 reopen item.
- **Plan corrections owed to the 16 Sep audits (apply at the next audit-driven revision, not silently):** A2.1 MTD invoice counts are 178 (Titan) and 37 (Helios) INV-only, not 182/38; A2.2 cannot be met from source data (owner's sheet carries three paper-only unit adjustments and a hybrid brand/cluster layout) — owner decision; A4.4 is blocked because **SQL Server Express does not support backup encryption**, so D9 (certificate-encrypted backups) needs an owner revisit: edition upgrade, or file-level encryption of the backup after `BACKUP` (e.g. certificate-protected 7-Zip/AES) with the same custody rules. Phase 4 task 6 (self-signed publisher) replaces Codex's purchase-dependent signing.

## 8. Roadmap crosswalk

The two roadmap documents (`docs/roadmap/`) use their own milestone IDs (S1-01…S3-04) and the Stage 2 annex its own phases 0–9. Neither set is tracked; this table is the only mapping and the plan's Phase/A-numbering governs.

| Roadmap item | Plan home | Note |
|---|---|---|
| S1-01 Report catalogue, source coverage, business rules | Phases 0–1; Section 3; Phase 2 task 13 (report acceptance record) | Audits 01–06 are the evidence-based matrix the roadmap asks for. |
| S1-02 Canonical imports, calculations, controls, finalisation, output consistency | Phase 1 (A1.1–A1.11), Phase 2 tasks 1–12 | Golden tests against 25 Aug 2026 and the two-year Helios corpus. |
| S1-03 Installed workflow/UI acceptance and defect repair | Phase 3 (new two-level shell), Phase 5 | The roadmap said "resume the redesign"; the plan replaces the shell. |
| S1-04 Report-ready release accepted | Phase 4 + Phase 6 | Installer, encrypted backups, recovery drill, `docs/USER-GUIDE.md`, `docs/OPERATIONS.md`. |
| S2-01 Audit Tally module; approve accounting/stock scope and mapping contracts | Phase 5 Accounting row; D12–D18 freeze at Phase 5 close; Phase 7 prerequisites | Statuses, business key, export receipt with company and TEST/PRODUCTION label. |
| S2-02 One safe invoice from ETP to actual Tally read-back | Phase 7 slice 7a | XML file export, TEST company, HTTP read-back, three-way comparison, duplicate replay. |
| S2-03 Return/inventory profiles and local TDL audit | Phase 7 slice 7b; local TDL → optional 7e | Returns and BC, tender split, multi-line, approved missing-master creation, goldens. |
| S2-04 Failure recovery; accept accounting-transfer scope | Phase 7 slices 7c and 7d | Reconciliation engine and difference types (7c); connected mode, hardening, UAT, production gate (7d). |
| S3-01 Actual-collection inputs and channel/cash policies | Phase 2 task 15; D19–D21; Phase 8 slice 8a | Samples first, layouts second, imports third. |
| S3-02 Cash, settlement, bank matching, residual balances | Phase 8 slice 8b | Strong-reference, grouped/partial, ambiguity, settlement bridge, residuals. |
| S3-03 Manager review, evidence, exceptions, ageing, escalation | Phase 8 slice 8c | Daily statement, exception taxonomy, three separate state families. |
| S3-04 Multi-day operation and adjustment loop into Stage 2 | Phase 8 slice 8d | Adjustment proposals routed to Phase 7 batches; period boundary. |
| Annex phase 0 Discover, baseline, map gaps | Done — audit 05, plan §4.5, the accounting module map behind Phase 7's prerequisites | No `00_CURRENT_STATE_AND_GAPS.md` is written (rule 2). |
| Annex phase 1 Lock the contract, verify capabilities | D12–D18; Phase 5 Accounting row; Phase 7 slice 7a capability probe | Protocol decision: XML file first; JSON only if verified on the installed build. |
| Annex phase 2 One complete vertical slice | Phase 7 slice 7a | |
| Annex phase 3 Durable batches, import controls | Phase 7 slice 7a (attempt records, duplicate replay) and 7d (connected-mode attempts, no write retry) | |
| Annex phase 4 Approved mappings, master readiness | Phase 7 slice 7b | |
| Annex phase 5 Local TDL audit | Optional Phase 7 slice 7e — deferred; built only if 7a–7d prove the need | 7a correlates through narration and reference fields; no UDF metadata. |
| Annex phase 6 Reconciliation and operator workflow | Phase 7 slice 7c | |
| Annex phase 7 Supported formats and delivery alternatives | Phase 7 slice 7d | No cross-format write fallback; native JSON only if verified on the installed build. |
| Annex phase 8 Hardening, regression, recovery rehearsal | Phase 7 slice 7d | |
| Annex phase 9 Windows/Tally UAT and production gate | Phase 7 slice 7d | Owner authorises the PRODUCTION label per company and scope. |
| Annex future option: Account TDL distribution | Out of scope | |

**Statements in the roadmap documents that no longer hold** (recorded here so nobody re-derives work from them):
1. **Snapshot.** Both documents describe `main` at `8e35d83…` and branch `ui/uiux-v4-touch-first-redesign` at `8bf8ac15…` with the 14 Sep UI-redesign handoff as the resumption record. Phase 0 closed at `6791c9e`; the baseline is `phase-1/data-truth`; the corrupt remote ref was pruned; the redesign branch is not resumed.
2. **Deleted or archived files.** `AGENTS.md` graphify instructions, `knowledge/AI-CONTEXT.md`, `knowledge/AI-ROUTER.md`, `graphify-out/`, `docs/PENDING_INPUT_AND_DEFERMENT_REGISTER.md`, the sprint ledger and every earlier handoff are gone or under `docs/_archive-2026-09/`. Codex reads this plan, `docs/audit/IMPORT-FAILURE-REGISTER.md` and the phase reports only (rule 2).
3. **`NETVALUE` "primary GST-inclusive sales value"** (roadmap §5.2) is wrong. `NETVALUE = NETAMOUNT − TAX` is ex-GST; per D1 every "Value" is NETAMOUNT (GST-inclusive) and NETVALUE/TAX are separate GST columns.
4. **"Customer names and phone numbers are excluded from canonical facts"** (roadmap §5.2) is superseded by D3: names and full phones are stored (R022/R024) and shown in the Customer-wise invoices report; they never appear in diagnostics, the support package, fixtures or evidence folder paths.
5. **"Resume the existing desktop/tablet design programme … category/task navigation, breadcrumbs, search"** (roadmap §6) and its "four phases and G1–G4 gates": Phase 3 replaces the shell with five rail buttons and tabs; tiles, breadcrumbs and the context sidebar are deleted.
6. **"29 reports"**: 22 real queries existed at audit; Phase 2 delivers the six evening reports plus one Exceptions screen; alias catalogue entries are removed.
7. **Code signing.** The roadmap's cost decision stands (no purchase); the annex and the archived registers that make an Authenticode certificate a release gate do not. Phase 4 task 6 = self-signed publisher trusted on the shop PC, SmartScreen "More info → Run anyway", published SHA-256 hashes.
8. **Milestone IDs S1-01…S3-04 and annex phases 0–9** are not tracked as work items; the table above is the only mapping.
9. **Annex placement and prompts.** The annex asks to be placed under `docs/tally-integration/` and to give Codex an embedded instruction; it is filed under `docs/roadmap/` and none of its prompts (nor the roadmap's) is given to Codex.
10. **Annex architecture "native JSON/XML exchange + local TDL audit"**: Phase 7 is XML file export first, HTTP read-back read-only, connected writes only in 7d; native JSON only if verified on the installed build; the local TDL and UDF metadata are deferred to optional 7e; Account TDL is out.
11. **"Unknown tender codes such as TC"** (roadmap §5.2): D11 fixed the cash-book modes (Cash, Card, UPI, CN, Gift Card, Bank, TC, Service Cash/Card/UPI); PAYMENTTYPE25 = Airpay UPI, PAYMENTTYPE20 = PhonePe UPI; `tender_modes` is the master.
12. **Version and test counts** ("1.8.8-r11", "669 passing tests"): historical. Phase 1 audit ran 670 tests green; the release is version 2.0.0 at Phase 6.

**Roadmap document banner** (prepended to both files under `docs/roadmap/`):

> **Reference material — not the plan.** This document was written on 15 September 2026 against a snapshot of the
> repository that no longer exists: `main` at `8e35d83`, branch `ui/uiux-v4-touch-first-redesign`, the `knowledge/` vault,
> `AGENTS.md` graphify instructions, the 14 September UI handoff, "29 reports" and the PENDING_INPUT register — all archived
> or deleted by Phase 0 of `docs/audit/ETP-MASTER-AUDIT-AND-PHASED-PLAN.md`.
> The plan governs. Its Section 8 maps this document's stages, milestones (S1-01…S3-04) and annex phases 0–9 to plan phases
> and lists the statements here that no longer hold (NETVALUE is ex-GST; customer names and phones are stored per D3;
> Phase 3 replaces the shell; no code-signing purchase; milestone IDs are not tracked).
> **Do not give Codex any prompt embedded in this document.** Codex works only from the plan's phase sections and the
> owner decisions D1–D21 in the plan's Section 3. Where this document and the plan disagree, the plan is right and this
> document is not amended — record the difference in the plan's Section 7 instead.
